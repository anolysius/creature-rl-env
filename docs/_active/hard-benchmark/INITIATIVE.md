# Initiative: hard-benchmark

> **절대 난이도** — "PPO/강한 agent가 oracle에 *못 닿는*" 진짜 hard benchmark. difficulty-scaling이
> *변별 분해능*(oracle−blind spread↑)과 *headroom robustness*(강한 baseline에도 21–41% plateau)를
> 입증했으나, gap register의 **"a hard benchmark" ❌(toy)**는 "강한/SOTA agent에도 hard"가 미해결로 남음.
> 본 이니셔티브 = env를 *구조적으로* 더 어렵게(부분관측·메모리·호라이즌·전략 깊이) 만들어 절대 난이도를 올린다.
>
> **마일스톤 SSOT**: roadmap.md · milestones.md. **활성: M3 신뢰성 자산** (난이도=신뢰성). 공개는 맨 마지막.

## 왜 지금 / 선행 결과

- **headroom-baseline-strength (difficulty-scaling #4)**가 진단(Q1) 완료: "hard-and-learnable" headroom이
  *cheap feedforward 스케일링(width/depth/budget)에 robust* — 약한-net 산물 아님, capacity/compute로 안 닫힘.
  **그러나 robust=cheap-scaling 한정**, recurrent/SOTA 미배제. → 절대 난이도(Q2)는 *spec 레버*가 필요.
- **부분관측 scout (2026-06-25)**: "시야 줄이기"(patch_radius↓ at fixed grid)는 **falsify**(feedforward 안
  나빠짐 + obs_dim 오염). 대신 **지도 크기/호라이즌**(grid16, 작은 고정 시야)이 강한 레버 신호 —
  feedforward PPO가 oracle의 **11–16%**(grid10 ~41% 대비 급락), oracle은 여전히 winnable(2.81).
  **단 entangled**(부분관측+호라이즌) + **feedforward only**(hard-but-learnable 미확인).

## 핵심 질문 (순서)

1. **메모리가 load-bearing인가?** (recurrent-baseline) — grid16의 난이도가 "hard-but-learnable"(메모리 있는
   agent는 더 잘함=진짜 능력)인지 "그냥 sparse-hard"(메모리 무관)인지. **recurrent vs feedforward** 대조.
2. (조건부) 메모리 load-bearing이면 → 호라이즌/부분관측을 깨끗이 분리한 hard config 정식화 + JAX 재포트.
3. (조건부) 강한 recurrent baseline의 oracle headroom 측정 (절대 난이도 = SOTA-class에도 hard 입증).

## 북극성 (CLAUDE.md 종속)

1. 능력 측정 복무. 2. RLVR(부분관측도 boolean subgoal 보존). 3. **procgen+seed split 비협상**(난이도 점도
   split·재현성·결정론 유지 — 부분관측이 결정론/RLVR 깨면 안 됨). 4. fast/vectorizable. 5. seeded·pinned.
   **정직성 > 헤드라인**(hard "입증" 과대 금지 — proxy·budget·seed 라벨).

## Task 목록

| # | slug | 상태 | 한 줄 |
|---|---|---|---|
| 1 | `recurrent-baseline` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/01-recurrent-baseline/`) | **메모리는 load-bearing인가** — JAX에 GRU recurrent A2C 추가(feedforward 무변경), 부분관측 commit world(grid10·**5×5 view**·3 gyms)서 recurrent vs feedforward A2C를 matched greedy eval로 대조. **결과(3 seed)**: feedforward 18%(0.50/2.81) vs **recurrent 46%(1.29/2.81)** of oracle, memory effect **+0.79>max std 0.33 → LOAD-BEARING robust**. recurrent net이 *더 좁은데도*(h128<h256) ~2.5× → 이득=memory(capacity 아님). **Q1 정직 보정**: robust headroom=feedforward 한정, recurrence가 부분관측 headroom 크게 회복(18%→46%) → headroom 상당부분이 no-memory 한계. env가 메모리 agent 변별(벤치마크 virtue). **경계**: recurrent도 46%서 잔존(미해결), **A2C 한정**(recurrent PPO=후속·Q1 PPO 깨끗 연결 미확정), 3 seed·CPU·param-match 아님·oracle proxy. scout가 grid16(더 큰 지도)=A2C 학습불가 inconclusive 확인 → grid10/patch2가 sweet spot. 419→423(+4, 회귀 0). |
| 2 | `recurrent-ppo` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/02-recurrent-ppo/`) | **recurrence가 *PPO* headroom을 닫는가** — #1은 A2C 내 메모리 효과만 보였으므로 Q1(PPO)과 깨끗이 연결. JAX에 **recurrent GRU PPO** 추가(`recurrent_replay`/`make_recurrent_ppo_rollout`/`recurrent_ppo_loss`/`train_recurrent_ppo`; feedforward/A2C/tuned-PPO/recurrent-A2C 전부 byte-identical). **고난도 sequence-preserving minibatch**: PPO의 시간축 셔플이 recurrence를 깨므로 **env축(B)만 셔플·T 보존·`h0`서 GRU 재생**. **correctness 먼저**(망가진 recurrent PPO=misleading): 결정론 게이트 2종 — (a) rollout logits==replay logits(tol 1e-4, ratio가 1서 시작), (b) **env-축 permutation 불변**(시간축 셔플 0 입증). **실측**(CPU·3 seed·250 iter, **Q1 exact default config** grid10·5×5·vary num_types8): feedforward PPO(h256) **24%(0.46±0.08/1.94)** vs **recurrent PPO(GRU h128) 53%(1.02±0.19)**, memory effect **+0.56>max std 0.19 → (a) recurrence-helps-PPO robust**. recurrent net이 *더 좁은데도* 2배+ → 이득=memory. **A2C 결과가 알고리즘 산물 아님 입증**(ff 18→24%, rec 46→53%; PPO가 양쪽 올려도 메모리 gap 유지). recurrent PPO도 **53%<oracle**(0.75·oracle 한참 아래) → **headroom-CLOSES(c) 미발동**(헤드라인 헤드룸 유지, 메모리가 일부만 회복으로 qualify·A2C+PPO robust). **경계**: PPO(SOTA 아님)·CPU·3 seed·single config·param-match 아님(의도적 더 좁음)·oracle proxy·A2C↔PPO는 config 다름(within-config gap만 읽기). 423→427(+4, 회귀 0). |
| 3 | `memory-headroom` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/03-memory-headroom/`) | **env가 강한 *메모리* agent에게도 hard한가** (절대 난이도) — #1·#2가 "memoryless에 hard"를 보였고 grid10서 recurrent PPO 53%(절반 회복)였으므로, env를 더 깊게(grid16·5gym·420step·**동일 5×5 view**) 만들어 *가장 강한 agent(recurrent PPO)에게도* oracle headroom robust한지 측정. JAX env config-driven이라 re-port 불요, 단 새 shape parity 검증(`hard_env_spec` 헬퍼 추가, 기존 byte-identical). **parity 게이트 먼저**(oracle=numpy·agent=JAX 같은 env): `test_jax_hard_config_parity.py` **0 mismatch**(obs 전 key+reward+term+trunc, random+gym-clearing, train·held-out). **scout(1 seed)**: grid16서 recurrent PPO 학습(recurrent-baseline scout가 A2C 학습불가로 둔 지점) → grid16 확정. **freeze 전 pilot**(--quick 2 run): parity0·rec 학습(24%)·winnable. **실측**(CPU·**5 seed**·300 iter, 사전약정 `classify_headroom` frac0.75 frozen): oracle **4.69**(winnable), feedforward PPO **0.53±0.44=11%**, recurrent PPO **2.01±1.05=43%**, opt-bound(mean+std) **3.06 ≪ 0.75·oracle 3.52** → **(a) hard-for-memory-agent ROBUST**. 더 깊은 config가 메모리 agent에게도 더 hard(43%<grid10 53%, 절대 headroom↑[~2.7/4.69 gym 미달]), memoryless는 11%로 더 floor, memory 여전히 load-bearing(+1.49≈4× ff). **(b)memory-CLOSES 미발동**(헤드라인 reframe 없음). **3 seed(52%±0.97 경계선)→5 seed(43%±1.05 여유)**로 robust 굳힘(single-run 노이즈 교정 문화). **경계**: 강한 agent=recurrent PPO(SOTA 아님 — 대형/better-algo는 닫을 수도)·CPU·5 seed **고분산(std 1.05)**·grid16 단일 config·oracle=scripted proxy·param-match 아님. competitive-analysis "Difficulty(absolute)" ❌toy→◐ 격상. 427→**442**(+15 parity·smoke, 회귀 0). |

| 5 | `multitype-boss-headroom` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/05-multitype-boss-headroom/`) | **다중-타입 보스 헤드룸 — 다중-seed 사전약정 측정** — #4 scout 본측정. 사전약정 freeze: (A) classify_headroom(frac=0.75) on 다중-config rec PPO, (B) 신규 `classify_depth`(fraction 정규화, gap>max std ∧ winnable→deeper / gap≤0→not-deeper); runs 3→5 확충(임계 불변). **실측(5 seed·250 iter·CPU)**: oracle single 5.00/multi **3.00**(winnable), fraction **32%±8% vs 30%±12%, gap +2.4pp** → **(A) hard-for-memory-agent ROBUST**(1.25≪2.25), **(B) inconclusive — scout 1-seed Δ+3.4pp 미생존**(+9.0→+2.4pp 축소). 정직 결론: 숨은 타입=**천장-낮춤 레버**(절대 난이도↑)이지 상대-깊이 미입증, "더 깊다" 주장 금지(#17→#18 패턴 재현). 깊이 질문 open(새 scout 선행 조건). 6 테스트, 620→**626**, L3 2/2 APPROVE. |
| 9 | `attrition-closure` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/09-attrition-closure/`) | **commit-mode가 순환-attrition 우회로를 닫는가** (#7 후속·§5(i) 계보 종결점) — #7(SE-only)이 부분만 축소한 원인=non-commit party-cycling. **엔진 0**, 기존 knob(commit_battles+super_effective_only)+arm 조합. 무추론 type_blind를 {commit,non-commit}×{default,strict,SE-only} 6칸 측정(hard grid16), probe=luck floor. **실측(16seed·3조건 freeze)=정직 (b) NOT CLOSED**: type_blind non-commit SE-only 3.75→commit **2.19**(순환이득 −1.56, default −2.56)→cycling-removed=True(commit이 순환 죽임) BUT 2.19>floor 1.25+0.5→at-floor=False. **2 성분 발견**: 순환 성분=닫힘·고정리드(creature0) 커버리지 성분(~44%>random 25%)=잔존. oracle winnable(5.00), 추론 gap 2.81=최대(commit+SE-only=eval-validity 최대 config). 완전폐쇄엔 리드 무작위화=별개 축. 5 결정론 메커니즘 테스트, 758→**763**(회귀0). L3 2/2 APPROVE. 부분성과 과장 안 함. |
| 8 | `sota-headroom` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/08-sota-headroom/`) | **더 강한 memory agent에도 hard한가 — #3 capacity confound 재측정** — #3(rec PPO h128=oracle 43%, "hard ROBUST")이 recurrent net을 의도적 좁게(h128<ff h256) 만들어 남긴 confound("hard인가 덜 키웠나")를 침: 같은 hard_env_spec(grid16)에서 rec PPO를 capacity+budget sweep(h128/h256/h384×300/600it)으로 키워 재측정. 판정=신규 순수함수 `headroom.classify_scaled_headroom`(best-non-tiny+classify_headroom 위임+non_vacuous/exceeds guard, 기존 무변경, numpy CI테스트). **실측(full 3-run·frac0.75/k1.0 freeze)=정직 VACUOUS**: tiny h128 **2.12(42%, #3 재현)** > h256 1.56(31%) > h384 0.83(17%) — **넓힐수록 나빠짐**(best 1.56<tiny 2.12→non_vacuous=False→verdict 보류). 순수 width 확대가 CPU·batch48·중간budget서 underfit(hidden-replay=width²) → **값싼 capacity 레버로 더 강한 agent 실패**. non-vacuity guard가 거짓 robust 차단(guard 작동=산출). **"SOTA에도 hard"=답 못함·OPEN 유지**(더 강한 agent=더 큰 batch/budget/LR/GPU 필요). #3 재현이 부수 소득. 12 테스트, 746→**758**(회귀 0). L3 2/2 APPROVE. 원하던 robust 대신 반증 그대로. |
| 7 | `super-effective-economy` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/07-super-effective-economy/`) | **배틀경제 widening — opt-in super_effective_only** (#6 후속, strict 의 strict superset) — 중립(eff==NEUTRAL)까지 데미지 0, super-effective(eff>NEUTRAL)만 통과. 엔진 3지점(battle.py `damage()` `eff<=NEUTRAL` 클램프·critter_env passthrough·jax_env `_gym_damage` compile-time const `eff<=1.0`, default off=byte-identical, strict_battle 선례 대칭). **scout(16 seed·정직 partial SIGNAL)**: (Q1) spread(oracle-blind) SE-only 가 완만히↑(grid16 2.56→**2.81**, grid10 1.62→1.69). (Q2) oracle **winnable 유지**(5/5·3/3=공정 레버 생존). attrition: strict **불변**(5→5·3→3) vs SE-only **부분** 축소(5→3.75) but **~0 미달=완전 폐쇄 아님**(non-commit cycling 이 운으로 SE 멤버 조우; 완전 폐쇄=commit-mode 별개 축). 경계: 단일타입 winnable 보장(#15), but SE-only+secondary 는 super×resisted=NEUTRAL 천장→구조적 unwinnable 가능(버그 아님·Q2 falsifiable, seed 로 증명). scripted·1-seed·no-robust-threshold=헤드라인 금지, 학습/LLM=money-gated 후속. 28 테스트, 718→**746**(회귀 0). L3 2/2 APPROVE. knob=판매 티어 레버 후보(`boss_pool_size`·`strict_battle` 병렬). |
| 6 | `strict-battle` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/06-strict-battle/`) | **전투 경제 confound 상환 — opt-in strict_battle** (M3-EC4 논문 §5 한계(i)) — 비효과(<NEUTRAL) 공격 데미지 0(양방향 대칭·유효타 기존 그대로·default off=byte-identical). numpy `Battle.damage` 단일 choke point + `CritterEnv` kwarg + JAX `JaxEnvConfig.strict_battle` `_gym_damage` 4지점(off=jaxpr 문자 그대로). 매치업 보장(#15) strict 커버 실검증(200 seed×{single,multi} unwinnable 0) + parity 0({commit,noncommit}×{single,multi} 16). **scout = 정직 FALSIFY**: commit spread delta +0.00(resisted 커밋은 legacy 도 이미 패배) + non-commit always-attack 무추론이 전 gym 클리어·strict 무차이(attrition=중립 chip+순환+재입장 풀힐 — strict 는 비효과타만 건드림) → **§5(i) confound 는 strict 로 안 닫힘, "confound closed" 주장 금지**. 더 강한 변형(SE-only 유효타·풀힐 제거)=기본 규칙 변경=사람 결정 후속; learned/LLM 효과 미측정 open. 27 테스트, 650→**677**(회귀 0). L3 2/2 APPROVE. evergreen `docs/reference/strict-battle.md`. |
| 4 | `multitype-boss-scout` | ✅ done (→ `_archive/2026-Q2/hard-benchmark/04-multitype-boss-scout/`) | **다중-타입 보스 — 더 깊은 추론 레버** (env+parity+scout, de-risked) — 공간 레버 소진(grid>16 학습불가) → 추론 깊이 레버: gym 보스에 **숨은 2번째 방어 타입**(eff=곱, obs primary만→secondary 추론). opt-in default off byte-identical(secondary draw 뒤, `Region.boss_secondary_types`). numpy 전투 이미 다중타입(무변경, 보스만 2타입). JAX: jax_env 자체 전투만 다중타입화(`gym_type2` sentinel + `_boss_def_eff` 곱, player→boss 2지점; jax_battle.py/full 범위밖). oracle=전체타입(chart expert)·infer/probe=primary(hidden 격리). **parity 0**(14 케이스). **scout(1 seed)**: oracle single 5.00→multi **3.62**(곱이 천장도 낮춤, winnable), rec 28%→24%, Δ+3.4pp — **1-seed RAW·multi-seed 측정=후속** 강한 라벨. 22 테스트, 592→**614**, L3 2/2 APPROVE. evergreen `docs/reference/multitype-boss.md`. 후속 `multitype-boss-headroom`. |

(이후: SOTA-class agent, family 확장, 상대-깊이 질문 재도전(더 무는 config — **새 scout 선행 조건**), attrition confound 완전 폐쇄의 남은 축(재입장 풀힐 제거·commit-mode 순환 제거 — 기본 규칙 변경이라 **사람 결정 선행**). recurrence axis(#1·#2)·메모리 agent 절대 난이도(#3)·다중타입 env+scout(#4)·다중타입 헤드룸 측정(#5, B=inconclusive 정직 종결)·strict_battle 엔진 규칙+scout falsify(#6)·super_effective_only widening+scout partial(#7, attrition 부분 축소·oracle winnable·완전 폐쇄엔 commit-mode 필요) settled.)

## 정직성 문화 (계승)

freeze 전 pilot이 전제 검증(falsify시 reframe — scout가 "시야 줄이기"를 이미 falsify). 사전약정 결정규칙으로
p-hacking 차단. recurrent 구현은 **correctness 먼저 입증**(망가진 recurrent=misleading "메모리 무용"). 다층
검증. proxy·budget·seed·CPU 라벨. 헤드라인보다 정직성.
