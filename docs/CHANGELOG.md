# Changelog

All notable task-level changes are appended here by `/task-end` (one line per task).

## eval-product
- **2026-06-28** — [`battle-legibility`](_archive/2026-Q2/eval-product/07-battle-legibility/) (standard, **전투 obs 가독성 — LLM이 숨은 타입표 추론 루프에 진입하게 #7**): #6(render 수정) 후에도 stateful probe 0% floor → **existence probe**(작은 5×5 grid·체육관1·`num_types`3·긴 호라이즌, 탐색 벽 제거)로 진단: 에이전트가 **체육관엔 잘 도달(battle-entries 19)하나 보스전을 전부 패배**. 같은 config에 scripted 대조 — **oracle 클리어(3턴) / type_blind 59턴 전패(0 gym) / claude-opus-4-8 = type_blind처럼 행동**(transcript: move 0만 반복·다른 무브 0~3[다른 타입] 미탐색·파티 교체(action4)·커밋 윈도우 미사용·2턴 사망·체육관을 생물로 착각해 Catch 반복). 즉 **승리는 숨은 타입표 추론을 요구**하는데(type_blind=0이 증거) LLM이 그 *추론 루프에 진입조차 못 함* — 전투 메커닉(무브가 여러 타입·교체·커밋·재시도)을 obs/시스템이 안 알려줘서. 수정: **`DEFAULT_SYSTEM`** 에 전투 전략(공격 무브 0~3은 서로 다른 **HIDDEN type** → 시도하며 적 hp 낙폭으로 super-effective 무브 발견·기억 / `spamming move 0 usually loses` / 나쁜 매치업이면 action4 Switch / 패배 시 파티 회복되니 같은 체육관 재진입해 배운 걸로 재시도) + **catch 명확화**(wild creature C 타일에 **정확히** 서야 동작·gym/빈 타일선 무동작 → catch-루프 낭비 차단). **`render_obs`** 전투 분기에 한 줄 Tip(무브 0~3 다양화·적 hp 변화 관찰·Switch). **벤치마크 정직성(핵심)**: 어떤 무브가 super-effective인지 **정답을 떠먹이지 않음** — "시도→관찰→기억"의 *방법*만 안내, 정답은 LLM이 추론(이게 env가 측정하려는 in-context hidden-rule 추론). **obs 스키마 무변경**(commit_window 상태·개별 무브 타입 노출은 env 변경=별도 task로 분리; 시스템 프롬프트가 메커닉을 *설명*하되 obs에 없는 실시간 상태 날조 안 함). **점수 보장 아님**(2턴 사망·num_types 등 잔존 → 재측정해도 floor 가능). **무회귀**: scripted agent는 `render_obs`/`DEFAULT_SYSTEM` 미사용 → `score_agent` 수치 불변(oracle frac_of_oracle=1.0). **다층 검증**: 4 테스트(전투 전략 가이드[정답 미노출 포함]·전투 render Tip+스탯 유지·오버월드 누출 없음·catch 명확화) + L3 2/2 APPROVE. 480→**484**(+4, 회귀 0), mypy(30)/ruff/build clean. INITIATIVE(#7) 갱신. 후속: 수정된 전투 가독성으로 existence probe 재측정 → 클리어 여부 확인. Acceptance AC1–AC5. _(commit pending)_
- **2026-06-27** — [`render-obs-legibility`](_archive/2026-Q2/eval-product/06-render-obs-legibility/) (standard, **render_obs 가독성 수정 — LLM 오도 제거 #6**): #5 stateful probe(claude-opus-4-8, claude-cli 구독, 봉인 3월드×40스텝)가 무상태·stateful **둘 다 0% of oracle**로 floor → 1 에피소드 **transcript 진단**(seed 1506920)으로 원인이 **`render_obs`의 오도**임을 확정(난이도 verdict도 파싱 버그도 아님): (1) **0-마스킹 오도(핵심)** — env(`critter_env.py:354`)는 `player_hp/type/level`을 *전투 중에만* 채우고 오버월드선 0-마스킹하는데 `render_obs`가 이를 그대로 `"Your creature: hp 0, type 0, level 0"`로 찍어 LLM이 step 1부터 *"나는 생물이 없다"* 오판(실제론 `reset()`이 `starter_party()` 부여) (2) **gym/creature 혼동** — LLM이 체육관(G) 타일을 생물(C)로 착각해 진입·보스전 패배 후 `(3,7)↔(3,8)` 루프로 40스텝 소진(gym0/caught0). 수정: **`render_obs`** 가 `in_battle` 분기 — 전투 중엔 player/enemy 스탯 표시(정확), 오버월드엔 오도 줄 제거 + `"Your party: 스타터 파티 보유(스탯은 전투 중 표시)"` 정직 표현 + 신규 `_nearest_in_view`(center 제외 Manhattan 최근접)·`_dir_phrase`(N/S/E/W 방향구)로 **시야 내 gym(G)/creature(C) 살라언스**("A gym (G) is visible 2 north", "wild creature (C) is visible 2 east") + 중앙 타일 gym/creature 플래그("You are ON a gym — 진입=보스전" / "on your tile — Catch"). **`DEFAULT_SYSTEM`** 에 스타터 파티 보유·목표(G 보스 격파)·catch 흐름(C 타일서 Catch) 1~2문장 추가. **정직 경계(과대 금지)**: obs에 파티 마릿수 필드가 없으므로 **거짓 수치 날조 금지** — 진실 한도("파티 보유, 스탯은 전투 중")만; **렌더 수정 ≠ 점수 보장**(호라이즌 40스텝·전투 난이도 잔존 → 재측정해도 floor일 수 있음, docstring/plan/report 명시); 재측정 probe는 **사용자 로컬**(구독 CLI/API), 나오는 frac_of_oracle은 그대로 기록·reframe 금지. **무회귀**: scripted agent(oracle/type_blind/random)는 `render_obs`를 안 쓰므로 `score_agent` 수치 **불변**(렌더↔채점 분리; oracle frac_of_oracle=1.0 확인) — 렌더는 LLM 텍스트일 뿐. **다층 검증**: 6 테스트(오버월드 오도 부재·전투 스탯·gym 살라언스+플래그·creature 살라언스·결정론+코어필드·DEFAULT_SYSTEM) + 합성 obs 헬퍼 + L3 2/2 APPROVE. 474→**480**(+6, 회귀 0), mypy(30)/ruff/build clean. INITIATIVE(#6) 갱신. 후속(사용자 실행): 수정된 렌더로 stateful probe 재측정 → 0% floor가 풀리는지 확인. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-27** — [`stateful-llm-agent`](_archive/2026-Q2/eval-product/05-stateful-llm-agent/) (standard, **기억 가진 LLM agent(스텝 간 history+윈도잉) — 공정한 재측정 도구 #5**): 무기억 `LLMAgent`는 부분관측(국소 5×5 view)서 본 지도를 즉시 잊어 floor한다 — 우리가 이미 입증한 **memory load-bearing**(`recurrent-baseline`)과 일관된 현상이지 "프런티어 LLM도 못 푼다"는 난이도 verdict가 **아니다**. 그래서 *공정하게* 재측정하려면 agent에게 기억을 줘야 한다. 신규 **`llm_eval.StatefulLLMAgent`**: 한 에피소드 안에서 `(obs 한 줄 digest, 취한 action)` 쌍을 **슬라이딩 윈도우**(`window` 기본 8)로 누적해 매 프롬프트에 prepend(과거는 full render 아닌 한 줄 digest로 토큰 절감; window 초과분 oldest-first drop; `window=0`=무기억 등가) → 같은 `Agent` Protocol(`act(obs)->int`) 만족 → #1 `score_agent`로 봉인 set 채점. **가장 중요한 정합성 게이트=월드 간 기억 격리**: `eval_harness.Agent` Protocol에 **선택적 `reset()` 훅** 도입 — `score_agent`가 `getattr(submission,"reset",None)`로 duck-typing 추출, `_play_once`가 `env.reset()` **직후 1회** 호출(에피소드당 1회, per-step 아님) → 월드 A의 transcript가 월드 B로 누수 불가. 러너 `scripts/llm_eval_run.py`에 `--stateful --window K`(미지정 시 무상태 경로 그대로) + stateful 시 "프롬프트 길어져 호출당 토큰↑" 경고. **무회귀 비협상**: 기존 `LLMAgent` 본문·`render_obs`·`parse_action` 무변경, reset 없는 submission은 훅 분기 skip → **byte-identical**(`runtime_checkable` Agent는 `act`만 검사하므로 기존 `isinstance` 무영향; `test_score_agent_stateless_submission_unaffected`로 고정). **정직 경계(과대 금지)**: 본 task=**기억 메커니즘**이지 측정 결과 아님(CI/테스트는 결정론 **stub** `complete`, 무-API); 실측 probe는 **사용자 로컬 실행**(구독 claude CLI 또는 API, 키=사용자·채팅 금지) — 나오는 frac_of_oracle은 그대로 기록, "프런티어 LLM이 푼다/못 푼다"로 reframe 금지; "구독으로 과금 0" 주장 금지("API 키 없이 claude CLI 파이프라인이 돈다"까지만). **다층 검증**: 9 테스트(history 누적·window 상한·window=0 무기억·reset 격리·invalid window·end-to-end / reset 에피소드당 1회·무상태 무회귀) + L3 2/2 APPROVE. 465→**474**(+9, 회귀 0; 무상태 byte-identical), mypy(30)/ruff/build clean. INITIATIVE(#5) 갱신. 후속(사용자 실행): `python scripts/llm_eval_run.py --provider claude-cli --stateful --window 8 --worlds N --max-steps M` → "기억 줬을 때 프런티어 LLM이 봉인 환경서 N% of oracle" 실측 회수. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-27** — [`claude-cli-provider`](_archive/2026-Q2/eval-product/04-claude-cli-provider/) (standard, **구독(claude CLI) provider + score_agent 콜 절반(이중-실행 제거) #4**): (1) **구독으로 실측**(API 과금 회피): 신규 `llm_eval.claude_cli_complete` — 로컬 **Claude Code CLI**(`claude -p`, 중립 tempdir cwd로 repo CLAUDE.md 미로드)를 LLM 백엔드로 → **Claude Code 구독 인증 사용, API 키/과금 불요**; binary 미존재 시 명확 `FileNotFoundError`. 러너에 `--provider {anthropic, claude-cli}`(기본 anthropic; claude-cli=구독·키 불요·`--model`은 anthropic만 적용). (2) **콜 절반(버그 픽스)**: `score_agent`가 submission을 *두 번* 돌리던 결함(`run_episode` 메인 채점 1회 + `_caught_rate` 재실행 1회 = per-step LLM 콜 2배; probe서 30예상→60실제 확인) 제거 → 신규 `_play_once(factory, policy, seed)->(gyms, caught, evolved)`로 한 에피소드서 세 subgoal을 **한 번에** 읽어 seed당 1 패스. submission·reference arm 모두 `_play_once` 1회. **메트릭 byte-equivalent**(동일 env 상태원 `info["subgoals"]`=`sum(_gym_defeated)`/`_caught`/`_evolved`) → 기존 #1/#2 테스트(oracle>random·rate∈[0,1]·end-to-end) 그대로 PASS. **정직 경계(과대 금지)**: 구독은 *대화형 용도*·rate limit 있음 → 자동 다수 호출은 **소규모 probe 권장**·`claude -p`는 **느림(~s/call, full agent 기동)** → 큰 run은 API 권장(docstring 명시). **다층 검증**: 단일-패스 콜-카운팅 테스트 + claude_cli 미설치 에러 테스트 + 기존 metric 테스트 무회귀 + L3 2/2 APPROVE. 463→**465**(+2, 회귀 0; 메트릭 byte-equivalent), mypy(30)/ruff/build clean. 후속: 구독 provider로 적정 호라이즌 실측 → "프런티어 LLM이 봉인 환경서 N% of oracle" 기록. Acceptance AC1–AC5. _(commit pending)_
- **2026-06-27** — [`llm-eval-run`](_archive/2026-Q2/eval-product/03-llm-eval-run/) (standard, **실제 LLM 비용-제한 sealed-eval 러너 — 프런티어 LLM이 우리 환경서 몇 %? #3**): "우리 시뮬 난이도가 *프런티어 LLM* 기준 어느 정도?"를 실측하는 도구. #2 `LLMAgent`+`anthropic_complete`로 실제 모델(`claude-opus-4-8` 기본)을 #1 봉인 set서 `score_agent` 채점 → **frac_of_oracle**(전문가 대비 %). 매 스텝 LLM 호출이라 비용/시간 폭증 → `SealedEvalSet`에 **`max_steps` 노브 추가**(기본 200=**byte-identical**·env_factory가 CritterEnv에 전달; 작은 값으로 per-step 호출 비용 상한) + 신규 **`scripts/llm_eval_run.py`**(argparse model/worlds/max-steps/num-types/master-seed + 시작 시 **예상 콜 수·⚠️ 비용 경고** + frac_of_oracle/oracle/blind/cleared/caught 출력; 기본 3월드×40스텝≈120콜 작은 첫 probe). **실행=사용자 로컬**(🔑 키=사용자·**채팅/커밋 금지**·SDK가 env `ANTHROPIC_API_KEY`서 읽음; 러너는 키를 인자로 안 받음). 이 환경엔 SDK·키 없음 → 키/SDK 부재 시 명확 `ImportError`(확인). **정직 경계(과대 금지)**: 러너=*도구*(상한+경고)이지 측정 결과 아님; 결과 해석은 **작은 probe·step 상한·proxy oracle·단일 run·signal not definitive** 경계 동반(출력/docstring/report). **다층 검증**: max_steps 캡 테스트(무-API) + 기본 200 무변경 테스트 + 키 없을 때 명확 에러 + L3 2/2 APPROVE. SealedEvalSet 기본 byte-identical → 461→**463**(+2, 회귀 0), mypy(30)/ruff/build clean. 후속(사용자 실행): `python scripts/llm_eval_run.py --model claude-opus-4-8 --worlds 3 --max-steps 40` → "프런티어 LLM이 봉인 환경서 N% of oracle" 실측 회수. Acceptance AC1–AC4. _(commit pending)_
- **2026-06-26** — [`llm-eval-adapter`](_archive/2026-Q2/eval-product/02-llm-eval-adapter/) (standard, **오염 방지 agentic-LLM 평가 어댑터 — M5 eval 제품 #2**): eval-product의 가장 유망한 갈래(시장 통증 #1 = 오염 안 되는 *agentic-LLM* 능력 평가)를 #1 봉인 하니스 위에 어댑터로 실현 — "처음 보는 세계서 숨은 규칙 추론해 체육관 깨기, RLVR 채점, 봉인 held-out이라 못 외움"을 동작 코드로. 신규 **`critter_gym.llm_eval`**(순수 numpy, core·CI 테스트, eval_harness/env import만): **`render_obs`**(env obs→LLM이 읽는 텍스트: position·in_battle·gyms·party/enemy·5×5 ascii 로컬뷰·문맥별 action 범례; 결정론) + **`parse_action`**(LLM free-text→action: 숫자/키워드[north~wait]/쓰레기·빈→fallback(5=Wait)/범위밖→클램프, 항상 [0,n)) + **`LLMAgent`**(provider-agnostic `complete(prompt)->reply` 주입 → #1 `Agent` Protocol `act(obs)->int` 만족 → #1 `score_agent`[봉인 `SealedEvalSet`]로 그대로 채점) + **`anthropic_complete`**(**옵션** Anthropic hookup, lazy `import anthropic`, `claude-opus-4-8`·thinking 생략·claude-api 레퍼런스 준수, 미설치/키 없음 시 명확 ImportError). **demo**(16 sealed worlds): render_obs 예시 출력 + stub-LLM 에이전트를 봉인 set서 `score_agent` end-to-end 채점. **정직 경계(과대 금지)**: 어댑터 = *메커니즘*(렌더→LLM→파싱→봉인 RLVR 채점 연결)이지 **실측 LLM 능력 결과 아님**(CI/테스트/demo는 결정론 **stub**[무-API]; 실측은 API key+비용 별도 run·사람)·프로토타입(docstring/demo/report 일관 명시). **다층 검증**: 11 테스트(render 결정론·필드·numpy scalar / parse 5종 강건성 / LLMAgent Protocol·end-to-end score / anthropic lazy-import) + demo + L3 2/2 APPROVE. 기존 src 무변경 → 450→**461**(+11, 회귀 0), mypy **30**/ruff/build clean. 후속(사람/비용 게이트): 실측 LLM run(`anthropic_complete`)·서버측 봉인 인프라·hosted eval-as-a-service. Acceptance AC1–AC5. _(commit pending)_
- **2026-06-26** — [`sealed-eval-harness`](_archive/2026-Q2/eval-product/01-sealed-eval-harness/) (standard, **봉인 held-out + agent 제출 → RLVR 검증 채점 — M5 비공개 eval 제품 기능 토대 프로토타입**): 사용자와 moat 논의 끝에 "기능적 moat의 유일 후보=비공개 재생성 eval 제품(M5)"으로 결론 → 그 *메커니즘*("외울 수도 오염될 수도 없는 eval")을 **동작하는 코드로 입증**. 신규 **`critter_gym.eval_harness`**(순수 numpy, **core·CI 테스트 가능**, 기존 env/learnability/region 심볼만 import·무변경): **`SealedEvalSet`**(secret `master_seed`로 held-out 구역 상단[base=1.1M, 공개 `heldout_seeds` 1M~ 비충돌] 비공개 블록 선택 — 같은 seed→동일 블록[감사], 다른 seed→신선·disjoint 블록[재생성]) + **`verify_sealed`**(오염 가드=moat 핵심: 제출 declared-train이 sealed eval과 **disjoint** ∧ 전부 train 구역[<1M]이어야 ok; leak/구역밖 검출) + **`score_agent`**(verifiable **subgoal로만** 채점: mean_gyms_cleared·cleared/caught/evolved rate·frac_of_oracle; hand-tuned 0) + **`Agent` Protocol**(`act(obs)->int` — 학습/scripted/LLM-agent 공통; EnvPolicy도 수용). **실측 demo**(16 sealed worlds): oracle **1.88(100% of-oracle·94% cleared)** > type_blind **0.94(50%)** > random **0.38(20%·56% caught)** 능력 사다리(RLVR) + **오염 가드**: honest(train 0..50k)→ok·overlap0 / **leak 시도(sealed seed 학습)→ok=False·overlap16 검출·거부**. **moat 메커니즘 end-to-end 입증** = "테스트로 학습 못 함"이 *검증 가능*(고정 벤치마크가 못 주는 신뢰=파는 희소재). **정직 경계(과대 금지)**: **프로토타입**·"봉인"=**in-process 컨벤션**(실제 제품은 서버측 secret seed+제출 샌드박스 필요)·단일 머신·numpy·단일 config·**hosted 서비스/고객/매출/공개 아님**(docstring·INITIATIVE·demo·report 일관 명시). moat "확립" 주장 0 — *메커니즘 데모*. **다층 검증**: 8 테스트(봉인 disjoint·결정론·재생성·오염 가드 3종·RLVR 채점·obs-only 인터페이스) + demo end-to-end + L3 2/2 APPROVE. 기존 src 무변경 → 442→**450**(+8, 회귀 0), mypy **29**/ruff/build clean. **신규 이니셔티브 `eval-product`**(M5, 기능적 moat 유일 후보). 후속(일부 사람/전략 게이트): 서버측 봉인 인프라·agentic-LLM 어댑터·hosted eval-as-a-service. Acceptance AC1–AC5. _(commit pending)_

## hard-benchmark
- **2026-06-25** — [`memory-headroom`](_archive/2026-Q2/hard-benchmark/03-memory-headroom/) (standard, **env가 강한 *메모리* agent에게도 hard한가 — 절대 난이도**): #1·#2가 "memoryless에 hard"(부분관측 메모리 load-bearing)를 보였고 grid10서 가장 강한 agent(recurrent PPO)가 oracle의 53%(절반 회복)였으므로, env를 **더 깊게**(grid16·5gym·420step·**동일 5×5 view**) 만들어 "*메모리 agent에게도* oracle headroom robust한가"(competitive-analysis "a hard benchmark ❌toy"의 미해결 조각) 측정. JAX env가 config-driven이라 **re-port 불요**(`hard_env_spec()` 헬퍼 추가, 기존 spec/train/eval/PPO/recurrent **byte-identical**), 단 새 shape는 **parity 게이트 먼저**(oracle=numpy·agent=JAX 같은 env 보장): `test_jax_hard_config_parity.py` **parity 0 mismatch**(13 obs key+reward+term+trunc, random+gym-clearing+held-out, num_creatures 양쪽 일치). **scout**(1 seed): grid16서 recurrent PPO 학습(recurrent-baseline scout가 "grid16 A2C 학습불가"로 둔 지점 — memory+PPO 조합이 학습 가능케) → grid16 확정. **freeze 전 pilot**(--quick 2 run ~27s): parity0·rec 학습(24%)·oracle winnable·timing. **실측**(공식 script, CPU·**5 seed·300 iter**, 사전약정 `classify_headroom` frac0.75·k1.0 **freeze 고정**): oracle **4.69**(winnable, ≥0.5·5), feedforward PPO **0.53±0.44=11%**, recurrent PPO **2.01±1.05=43%**, opt-bound(mean+std) **3.06 ≪ 0.75·oracle 3.52** → **(a) hard-for-memory-agent ROBUST**(`hard-and-learnable`). **의미**: 더 깊은 config가 *메모리 agent에게도* 더 hard — recurrent PPO가 grid10(53%)보다 *낮은* 43%만 회복하고 **절대 headroom은 훨씬 큼**(~2.7/4.69 gym 미달 vs grid10 ~0.9/1.94), memoryless는 11%로 더 floor, memory 여전히 load-bearing(rec−ff **+1.49 ≈ 4× ff**). **(b)memory-CLOSES 미발동**(헤드라인 reframe 없음·사람보고 stop 불요). **3 seed(52%±0.97, opt-bound 3.41 vs 3.52 경계선)→5 seed(43%±1.05, opt-bound 3.06 여유)**로 robust 굳힘(single-run 노이즈 교정 문화; 임계 고정·seed만 확충=p-hacking 아님). competitive-analysis "Difficulty(absolute)" **❌toy→◐**(현 baseline class엔 메모리 agent 포함 hard) 격상. **경계(과대 금지)**: 강한 agent=recurrent PPO(**SOTA 아님** — 대형/better-algo/HP-튜닝은 닫을 수도)·CPU·5 seed **고분산(std 1.05)**·grid16 **단일 config**·oracle=scripted proxy·recurrent net param-match 아님(의도적 더 좁음). **다층 검증**: parity 게이트 + pilot + 3→5 seed robust + L3 2/2 APPROVE. 427→**442**(+15 parity·smoke, 회귀 0; 기존 byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(memory-headroom Update)+competitive-analysis(gap+matrix)+INITIATIVE(#3) 갱신. recurrence axis(#1·#2)·메모리 agent 절대 난이도(#3) settled. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-25** — [`recurrent-ppo`](_archive/2026-Q2/hard-benchmark/02-recurrent-ppo/) (standard, **recurrence가 *PPO* headroom을 닫는가 — #1 A2C 메모리 효과의 PPO 연결**): #1 `recurrent-baseline`이 *A2C 내*에서만 메모리 load-bearing을 보였고 Q1 headroom은 *PPO*로 측정됐으므로, **Q1 exact `default` config**(grid10·**5×5 view**·vary num_types8·1–3 gyms)서 recurrent PPO vs feedforward PPO를 깨끗이 대조. **고난도 sequence-preserving minibatch**: PPO가 (T,B)를 평탄화·**시간축 셔플**(recurrence 깸)하므로 **env축(B)만 셔플·T 보존·`h0`서 GRU 재생**. `jax_train`에 추가(전부 추가만, feedforward/A2C/tuned-PPO/recurrent-A2C **byte-identical**): `recurrent_replay`(loss·테스트 공유 GRU 재생) / `make_recurrent_ppo_rollout`(hidden 스레딩 + h0 노출 + last_value bootstrap) / `recurrent_ppo_loss`(env-축 minibatch clipped surrogate) / `train_recurrent_ppo`. **correctness 먼저**(망가진 recurrent PPO=misleading "메모리 무용"): 결정론 게이트 2종 — (a) rollout logp/values == replay(tol 1e-4, clipped ratio가 1서 시작) + (b) **env-축 permutation 불변**(`replay(perm)==replay()[:,perm]`, 시간축 셔플 0 입증). **freeze 전 pilot**(--quick 2-run ~23s): correctness 통과·rec 학습·timing 현실적(ff 11% vs rec 34%, +0.44>0.16), falsify 없음. **실측**(공식 script, CPU·**3 seed·250 iter**): feedforward PPO(h256) **0.46±0.08=24% of oracle(1.94)** vs recurrent PPO(GRU **h128**) **1.02±0.19=53%**, memory effect **+0.56 > max(std) 0.19 → (a) recurrence-helps-PPO robust**. recurrent net이 *더 좁은데도*(h128<h256) 2배+ → **이득=memory(capacity 아님)**. **A2C 결과가 알고리즘 산물 아님 입증**(ff 18→24%, rec 46→53%; PPO가 양쪽 올려도 메모리 gap 유지). recurrent PPO도 **53%<oracle**(0.75·oracle 1.45 한참 아래) → **(c) headroom-CLOSES 미발동**(헤드라인 헤드룸 유지·사람보고 stop 불요; 메모리가 *일부만* 회복으로 qualify, A2C+PPO 둘 다 robust). **경계(과대 금지)**: PPO≠SOTA·CPU·3 seed·single config·param-match 아님(의도적 더 좁음)·oracle=scripted proxy·**A2C↔PPO는 config 다름**(#1 fixed-3-gym oracle 2.81 vs Q1 vary'd oracle 1.94 → within-config ff-vs-rec gap만 읽기). **다층 검증**: pilot + correctness 게이트 2종 + 4 테스트 + non-vacuity(FF 더 넓은데 floor) + L3 2/2 APPROVE. 423→**427**(+4, 회귀 0; 기존 경로 byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(recurrent-ppo Update)+competitive-analysis(gap register)+INITIATIVE(#2) 갱신. recurrence axis는 이제 **A2C+PPO 둘 다 settled**. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-25** — [`recurrent-baseline`](_archive/2026-Q2/hard-benchmark/01-recurrent-baseline/) (standard, **메모리는 load-bearing인가 — 부분관측 하 recurrent vs feedforward (Q1 헤드라인 정직 보정)**): `headroom-baseline-strength`(Q1)가 "headroom이 cheap *feedforward* 스케일링에 robust"를 입증하며 남긴 명시 caveat **"recurrent 미배제"**를 직접 측정. 부분관측 scout(시야 줄이기 falsify → 지도-스케일 시사, grid16은 A2C 학습불가 inconclusive)를 거쳐 **정확한 질문=부분관측 하 메모리 load-bearing 여부**로 reframe. `jax_train`에 **recurrent(GRU) A2C** 정식 추가(`gru_init_params`/`gru_step`/`recurrent_policy_value`/`make_recurrent_rollout`/`recurrent_a2c_loss`/`train_recurrent`/`evaluate_gym_clears_recurrent` — GRU hidden을 rollout `lax.scan`에 thread, done시 per-env reset; loss는 in-order (T,B) trajectory에 hidden 재생[A2C는 full rollout·shuffle 없음→정확]). **feedforward 경로 전부 무변경**. `scripts/recurrent_baseline.py`가 부분관측 commit world(grid10·**5×5 view**·3 gyms)서 ff vs rec를 **matched greedy eval**(동일 protocol)로 비교 + 사전약정 규칙(load-bearing iff `rec−ff>max(std)`). **freeze 전 pilot**(3 seed matched): ff 9% vs rec 50%, +1.17 std-separated. **실측**(공식 script, CPU·3 seed): feedforward A2C(h256) **0.50±0.14=18% of oracle(2.81)** vs recurrent A2C(GRU **h128**) **1.29±0.33=46%**, memory effect **+0.79 > max(std) 0.33 → LOAD-BEARING robust**. recurrent net이 *더 좁은데도*(h128<h256) ~2.5× → **이득=memory(capacity 아님)**. **Q1 정직 보정**: robust headroom은 *feedforward 한정*이었고 **recurrence가 부분관측 headroom 크게 회복(18%→46%)** → headroom 상당부분이 *no-memory* 한계. env가 메모리 있는/없는 agent를 선명히 변별(벤치마크 virtue). **경계(과대 금지)**: recurrent도 **46%서 잔존**("풀림/이미 hard" 아님); **A2C 한정**(recurrent PPO=후속, Q1 PPO 깨끗 연결 미확정); 3 seed·CPU·단일 config·param-match 아님·oracle proxy. **다층 검증**: pilot(correctness+3seed matched) + 4 테스트 + non-vacuity(FF 더 넓은데 floor) + L3. 419→**423**(+4, 회귀 0; feedforward byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(memory Update)+competitive-analysis+difficulty-scaling INITIATIVE(Q1 보정) 갱신. 신규 이니셔티브 `hard-benchmark`. Acceptance AC1–AC5. _(commit pending)_

## difficulty-scaling
- **2026-06-25** — [`headroom-baseline-strength`](_archive/2026-Q2/difficulty-scaling/04-headroom-baseline-strength/) (standard, **강한 baseline에도 oracle headroom이 살아남나? — 절대 난이도 진단 Q1**): 헤드라인 "hard-and-learnable"(PPO≈oracle 21–28%)가 *tiny MLP·짧은 훈련* 산물인지, *강한 baseline*에도 살아남는 진짜 난이도인지 진단 — **비싼 Q2(부분관측 등 spec 변경·JAX 재포트) 전에 가장 싼 진단**(JAX 엔진이 강한 baseline을 싸게 훈련케 함). `jax_train`에 **default-preserving depth 노브**(`init_params`/`apply_policy`가 `depth`; depth=1 byte-identical → A2C/기존 PPO 무변경, depth≥2 trunk 레이어 compile-time 추론) + `PPOConfig.depth`. `ppo_baseline.py --strong`이 capacity×budget 스윕(width 64→256·depth 1→2)의 **best-of-sweep**를 강한 baseline으로, 동일 사전약정 `classify_headroom`(frac=0.75)로 3-branch((a)robust/(b)closes/(c)exceeds) + **non-vacuity 가드**(best-strong>tiny — 망가진 깊은 net의 공허한 robust 차단). **freeze 전 pilot**: config-only 스케일이 held-out 약 2배지만 ~28% plateau, branch (a) 신호, falsify 없음. **실측**(CPU·3 seed): best strong PPO(wide d1/h256)가 oracle의 **41%(default 0.76/1.84)/25%(hard 1.83/7.28)**에서 plateau(opt-bound 0.89/2.05 ≪ 0.75·oracle 1.38/5.46) → 양 config **(a) headroom-ROBUST**. **핵심 정직 발견**: depth 1→2는 **하락**(d2 0.33<wide 0.76)·budget은 i600서 plateau(**i4000≈20M steps도 i600보다 안 나음**) → **병목은 baseline의 capacity/compute 아님**(transfer-capacity-budget "bigger net hurts" 재현). non-vacuity 가드가 깊은 net의 vacuous robust 실제 포착. **경계(과대 금지)**: robust=*cheap feedforward 스케일링 한정* — recurrent/메모리(POPGym/Craftax)·대형·better-algo(RND/world-model)·HP-튜닝 **미배제**(닫을 수 있음), oracle=scripted proxy, 3 seed·CPU·1–2층. **함의**: headroom 실재·약한-net 산물 아님 → 현 baseline class엔 trivially toy 아님(헤드라인 강화); "SOTA agent에도 hard"는 미해결 → **Q2(부분관측)가 더 강한 agent 대비 절대 난이도엔 여전히 동기**(Q1이 "cheap 스케일링부터 배제"라는 순서 정직하게 닫음). **다층 검증**: pilot + budget-plateau 검증(i600–4000) + non-vacuity 가드 + 4 depth 테스트 + L3 2/2 APPROVE. 415→**419**(+4 depth, 회귀 0; depth=1 byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(PPO robust Update)+competitive-analysis(gap register)+INITIATIVE 갱신. Acceptance AC1–AC5. _(commit pending)_
- **2026-06-25** — [`ppo-headroom-rigor`](_archive/2026-Q2/difficulty-scaling/03-ppo-headroom-rigor/) (standard, **tuned PPO oracle-headroom을 multi-run robust로 — "hard-and-learnable" 헤드라인 신뢰도↑(자율런 KR1-hardening)**): `jax-ppo-tuned`의 single-run "PPO가 oracle의 15–32%만 도달"을 ≥5-run + 사전약정 std-across-runs 결정규칙으로 굳힘(과거 (B) 스레드가 single-run을 4회 노이즈로 교정한 학습 적용). 신규 `src/critter_gym/headroom.py` `classify_headroom`(순수·**numpy-only CI**, frac=0.75·k=1.0 데이터 전 고정: `mean+k·std≤frac·oracle`→`hard-and-learnable` / `mean−k·std≥frac·oracle`→`ppo-closes` / else `inconclusive`) + `HeadroomVerdict` + `ppo_baseline.py --runs` robust verdict(single-run fallback 보존). **freeze 전 pilot**: classify property 7종(3 verdict 경계+ratio+빈입력/oracle≤0 가드) + multi-run end-to-end 입증, 임계 데이터 전 고정. **실측**(CPU·5-run): default PPO **0.52±0.06=oracle 1.84의 28%**(낙관상한 mean+std 0.58 ≪ 임계 1.38), hard PPO **1.52±0.28=oracle 7.28의 21%**(낙관상한 1.80 ≪ 5.46) → 양 config **`hard-and-learnable` robust**(seed 노이즈 아님 입증; single-run 32%/15%이 multi-run 28%/21%로 일관). gap≈0(+0.20/+0.12)·R2 PPO≥A2C 양 config·hard서 PPO(1.52)<type_blind(2.03) 유지. **`ppo-closes` 미발동**(reframe 불요). **정직 caveat**: 5-run(대규모 sweep 아님)·작은 net·CPU·이 예산·oracle=scripted proxy. **다층 검증**: classify property 7 + L3 2/2 APPROVE. 365→372(+7 headroom 단위, CI numpy-only; jax_train/jax_env 무변경), mypy(28)/ruff/build clean. jax-throughput.md(robust 갱신)+DESIGN §3.1.1+INITIATIVE 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-24** — [`difficulty-dynamic-range`](_archive/2026-Q2/difficulty-scaling/02-difficulty-dynamic-range/) (standard, **(A) hard 쪽 — 변별 *분해능*↑(동적 범위)**): #43이 *hard*(변별 난이도)로 옮긴 질문 공략. 먼저 `discriminating-difficulty` slug의 freeze 전 pilot이 그 메커닉("oracle 천장/스타터 다양화")을 **falsify**(winnability 정상=oracle이 존재 gym 거의 다 클리어, "천장 0.6"은 max-3 대비 분수·실제 평균 2 gym / 변별 이미 +1.0/gym / 다양화는 랜덤 토너먼트 구조상 무력=고정 챔피언 ~절반 우연 카운터 / 재출현↓는 in-episode 추론 불가하게 해 금지) → 정직 reframe·새 slug. **확실한 레버=동적 범위(gym 수)**. `region.generate_region(min_gyms=None)` opt-in(기본 None=byte-identical 무회귀, 명시 시만 검증, 재출현 pool 보존) + `critter_env min_gyms` + `difficulty_generalization` `discrimination_resolution`/`classify_resolution`(사전약정 spread@max≥2.0·단조·win≥0.70)/`--resolution`(scripted numpy)/`--range-gap`([rl]). **실측**(scripted, held-out, gym-clear-only): oracle−type_blind spread **+1.31(3)→+2.56(5)→+4.88(8)** 단조↑ + winnability 0.88 → **`resolution-up`**. 학습 gap@8(PPO 60k,3run): held-in 1.67/held-out 1.85/**gap −0.19±0.60=`gap≈0-signal`**(분해능↑하며 일반화 유지) — PPO 1.67≪oracle 7.06=변별 headroom 큼. **정직 범위 경계**: 변별 *분해능*(점수 범위↑)이지 "PPO 못 푸는 hard-benchmark"는 **명시적 범위 밖**(future work). **JAX 무변경**(jax_env 3-gym 하드코딩)→후속 `jax-difficulty-report`(R5). **다층 검증**: pilot(메커닉 falsify→reframe) + property 테스트 + L3. 287→294(+7, 회귀 0; min_gyms=None byte-identical), mypy(26)/ruff/build clean. DESIGN §3.1.1 + competitive-analysis 갱신. supersedes `discriminating-difficulty`(pilot-falsified, 미구현 정리). Acceptance AC1–AC8. _(commit pending)_
- **2026-06-24** — [`difficulty-gap-rigor`](_archive/2026-Q2/difficulty-scaling/01-difficulty-gap-rigor/) (standard, **(A) "hard-and-gap≈0" — #24 약한신호를 multi-run rigor로 정밀화**): M4 JAX 입증 후 갭 register 또 다른 축(난이도)으로 피벗. #24(difficulty-generalization)가 남긴 honest limit(난이도 점 gap이 전부 *한 run 내 per-seed std* 안=저예산 40k·단일run 약한 증거) 해소. `scripts/difficulty_generalization.py`에 `classify_gap`(사전약정 순수함수, `floor=0.3`·`k=1.0` 데이터 보기 전 고정) + `MultiRunGap`(std-**across-runs**) + `train_and_gap_multirun` + `--runs N`. **핵심 통찰**: "gap이 real이냐"의 불확실성은 run간 std지 #24가 쓴 한 run내 per-seed std 아님. env 무변경(JAX 재포트 없음). **freeze 전 pilot이 사전약정 임계 고정 + 타이밍(1.21s/1k step→100k×3×5run≈30분 feasible)**. **실측**(100k, 5run, N16): held-in **floor 훨씬 위**(1.10/1.21/1.54=정책 유능, generalist-mediocrity 아님) + 세 점 모두 **`gap≈0-signal`**(d0 −0.225±0.350/d1 −0.237±0.489/**d2 −0.400±0.896**), d2(12타입·강보스)서 held-in *상승*(1.54)하며 gap≈0 robust(5run). **#24 대비 진짜 업그레이드**(per-seed std→std-across-runs, 비floor held-in). **real-gap 미출현**. **정직 caveat**: gap 약한 음수(난이도 비대칭, std 안 부호 불안정)·**std가 난이도와 함께 커짐**(d2 0.90≈gap 2배, 작은 gap 정밀 배제 못 함)·held-out~1.9/3=현 knob 변별력 부족 가능. **AC7 분기 (a)**: gap-correctness 문제 아님→"hard-and-gap≈0"의 *hard* 쪽(변별 난이도)이 미해결 = 재설계 동기 재정의(단 env 메커닉=JAX 재포트). 281→283(+2: classify 단위[CI numpy-only]+multirun smoke, 회귀 0), mypy(25)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC8. _(commit pending)_

## jax-throughput
- **2026-06-26** — [`gpu-ec3-result`](_archive/2026-Q2/jax-throughput/13-gpu-ec3-result/) (standard, **M4-EC3 GPU 실측 달성 기록 + bench 배치 상한 정직 수정**): #12 Colab bench를 **사용자가 무료 NVIDIA T4서 실행 → M4-EC3(≥10M steps/s GPU) 실측 확보**. overworld fused-`lax.scan` rollout vmap: **75.9M(b1024) → 271M(b4096) → 952.8M(b16384) steps/s** = **≥10M EC의 95×**, batch와 단조 스케일(numpy 49k·jit-single 64k 참고) → **M4-EC3 달성**. `docs/reference/milestones.md` M4 `⬜→🟢`(EC1 hotpath 4/4 family 포트·EC2 parity 0·EC3 GPU 실측 전부 ✅; doc lag 정직화, 증거 task 포인터 동반) + `jax-throughput.md` §5 item3(GPU "미측정"→"✅ measured", T4 수치) + `competitive-analysis.md` matrix "Speed" 행(JAX vmap ~950M GPU)·tradeoff "Speed" bullet(채택-게이트 갭 "largely closed") 갱신. `scripts/gpu_bench.py` 기본 batch 상한 **65536→16384**(b65536이 65536 procgen 빌드 + 메모리 한계로 무료 T4 멈춤·full-episode OOM → 기본 제외; 16384가 이미 ~950M로 throughput 포화; 로직·시그니처 무변경·`--quick` 무영향). **정직 경계(과대 금지)**: overworld slice 한정·**single run**·**free T4**; full-episode env GPU 수치는 분기 많은 step의 free-T4 컴파일 한계로 미확보(단 **CPU 22M/s로 EC 이미 초과** → GPU도 초과 자명, 정밀치는 better-HW minor 후속); peer 압도 주장 안 함(동일 GPU-벡터화 class 진입). **다층 검증**: 사용자 실측 + CPU sanity + L3 2/2 APPROVE(정직성 축). src 무변경 → **442 무회귀**, mypy(28)/ruff/build clean. **M4 사실상 완료** — 남은 프로젝트 게이트=공개(M3-EC4/EC5·사람)·M5 eval 제품(전략·사람). Acceptance AC1–AC5. _(commit pending)_
- **2026-06-25** — [`gpu-bench-colab`](_archive/2026-Q2/jax-throughput/12-gpu-bench-colab/) (standard, **M4-EC3(≥10M steps/s GPU) enabler + 로컬 Apple GPU 비가용 박제**): GPU 측정은 하드웨어 게이트. 이 세션에서 **사용자 맥(M5 Pro·macOS 26.5)의 로컬 GPU를 jax-metal로 실측**: **0.1.0**(jax/jaxlib 0.4.26)·**0.1.1**(jax/jaxlib 0.4.34) 둘 다 `METAL(id=0)` 인식·단순 op(vmap+scan+scatter+gather+where) 정확 실행 OK지만, **fused `lax.scan(vmap(env_step))`(공정 GPU 측정·RL 학습에 필요한 패턴)에서 둘 다 NSException 크래시**(Metal PJRT 플러그인 op-coverage 한계, **우리 코드 문제 아님**); per-step 루프만 ~23k steps/s(dispatch-bound, CPU보다 느림) → **로컬 Apple GPU는 JAX로 EC 측정 불가**. 신규 **`scripts/gpu_bench.py`** — 기존 bench(`bench_throughput.py`)의 per-step python 루프(dispatch-bound, GPU 과소평가)를 **fused `lax.scan` rollout**(overworld + full-episode, batch 스윕, numpy/jax-single/jax-vmap 3행)로 교체한 GPU-공정 측정. 기존 `jax_overworld`/`jax_env`/`jax_train` 심볼만 import(**src 무변경**). CPU sanity: overworld vmap **~480M/s**·full-episode **~22M/s** @b1024(vmap≫numpy·전부 유한·크래시0). 신규 **`scripts/colab_gpu_bench.ipynb`** — 무료 Colab/Kaggle T4용 **15-cell 노트북**(GPU 확인→토큰[getpass]→clone[public+PAT]→`pip install "jax[cuda12]"`+repo 설치→backend 확인→`gpu_bench.py` 실행→`train_ppo` train throughput→복붙용 요약; **nbformat valid**·ruff clean). 측정 실행=사용자 Google 로그인(게이트). **정직 경계**: notebook은 GPU 부재로 **로컬 CPU-sanity까지만 검증**(Colab 실제 실행 미검증)·repo 공개전(clone 경로 안내)·**EC 달성은 사용자 Colab 숫자 회수 후 별도 기록**(도구+음성결과 박제이지 EC 달성 주장 0). **다음 세션 로컬 Metal 재시도 방지**를 위해 jax-throughput.md §5에 박제("do not re-attempt"). src 무변경 → **442 무회귀**, mypy(28)/ruff(ipynb 포함)/build clean. L3 2/2 APPROVE. Acceptance AC1–AC5. _(commit pending)_
- **2026-06-25** — [`v1-results-packaging`](_archive/2026-Q2/jax-throughput/11-v1-results-packaging/) (standard, **KR3 결과 패키징 / v1.0.0-rc 준비**): 흩어진 강한 결과를 front-facing 문서로 통합 + **1-command 재현** + 버전 1.0.0-rc. **제품 코어(src) 무변경 — docs/scripts/meta만**. (1) 신규 `scripts/reproduce_results.py [--quick] [--runs N]` — `bench_throughput.py`+`ppo_baseline.py` 오케스트레이션으로 throughput 표(numpy/jax-single/jax-vmap)+oracle-headroom 표를 **라이브 재생성**(수치 하드코딩 0·honest framing 보존). (2) `README.md` "What it measures"에 **competitively-fast(JAX vmap 27–1047×·4/4 family parity 0)** + **hard-and-learnable(tuned PPO 21–28% of oracle, 5-run robust·hard서 PPO<type_blind capability ladder)** 헤드라인 통합 + 재현 명령 + Release status(1.0.0-rc + 잔여 게이트 GPU[M4-EC3]·arXiv[M3-EC4]·OSS[M3-EC5]=사람). (3) `docs/paper/critter-gym.md`: §2 throughput 갱신 + **신규 §6 "Throughput: a parity-proven JAX port"**(parity 0·multiplier 표·4 family·trains·GPU 미측정 boundary) + §4 PPO oracle-headroom subsection(표·사전약정 R1/R2/R3) + §5 note + §8 limitations + §9 conclusion(JAX port=done) + §7/8/9 renumber. (4) `docs/paper/README.md` figure→source map 4 신규 행. (5) `pyproject.toml` 0.0.1→**1.0.0rc1**(build이 1.0.0rc1 아티팩트 생성; **태그/publish 안 함**). **정직성**: 모든 정량 주장에 caveat(CPU·vmap-only·single jit slower·oracle proxy·5-run·GPU 미측정), 수치 라이브 재생성으로 fabricate 0. **공개(OSS 리스팅·arXiv·태그)는 사람 게이트 — 직전 정지**. **freeze 전 pilot**: reproduce_results --quick 두 표 재생성·수치 라이브 검증. 415 무회귀(src 무변경), mypy(28)/ruff clean, build→1.0.0rc1. L3 2/2 APPROVE(정직성 축 포함). Acceptance AC1–AC6. _(commit pending)_
- **2026-06-25** — [`jax-duel-integration`](_archive/2026-Q2/jax-throughput/10-jax-duel-integration/) (standard, **duel(C) family를 jax_env에 통합 — 4/4 family 벡터화 완성(자율런 KR2 마무리)**): 마지막 family **duel(C)** = type-AGNOSTIC RPS/stamina 배틀(type chart 무용)을 별도 `duel_battle_branch`로 `make_jax_env(JaxEnvConfig(family=duel, commit=False))`에 통합, numpy `DuelEnv(commit_battles=False)` 미러. **duel 고유 3요소**: (1) **동시 데미지**(속도순 없음·양쪽 `take_damage` 무조건→한 턴 동시 기절=loss), (2) **raw stat 데미지** `floor(atk×(1+charge))`(defense/eff/min-1 clamp 없음·`_damage` 미사용), (3) **duel 전용 charge obs**(`player_charge`/`enemy_charge` 비-0 → `encode_obs` family-aware, 비-duel 0-mask byte-identical). 보스 결정론(echarge≥1→ATTACK else CHARGE·GUARD 안 함). overworld=family A CATCH 재사용. `JaxEnvState`에 `player_charge`/`enemy_charge` 필드 + `battle_turn`을 duel turn 카운터로 재사용(40턴 stalemate cap=loss; duel config서 noncommit branch 미도달이라 안전). **parity 0 mismatch**: 13 obs(charge 2키 포함)+reward+term+trunc, full 에피소드, fixed+vary, 5 정책(random·gym-seeking·charge-exploit·all-GUARD stalemate·**scripted-optimal**[보스 결정론 악용→win·evolve]) + **non-vacuity 가드**(`test_duel_battery_is_non_vacuous`: 3 액션·turn-cap loss·win·evolve 6신호 실자극). **freeze 전 pilot**: 19,200 steps 0 mismatch + **always-attack는 0승 포착**(탱키 보스가 charge-0 공격 out-trade→승리엔 RPS 필요)→scripted-optimal로 win 60·evolve 24. **실측**(CPU·single·vmap-only): numpy 123k/s · **jax vmap 4.96M(b=1024)=40× / 10.15M(b=16384)=83×**. **4/4 family(A/B/C/D) 전부 벡터화 = full breadth**. **정직 범위**: CPU·single·vmap-only·GPU(M4-EC3)만 남은 M4 항목. **다층 검증**: pilot(0 mismatch+RPS·동시기절·교착·win/evolve) + 19 테스트 + non-vacuity 가드 + L3 2/2 APPROVE(라인별 numpy↔JAX 대조). 396→**415**(+19 importorskip parity, 회귀 0; family A/B/D byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(duel Update)+DESIGN §4+competitive-analysis+INITIATIVE 갱신. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-25** — [`jax-family-integration`](_archive/2026-Q2/jax-throughput/09-jax-family-integration/) (standard, **forage(B)+muster(D) family를 jax_env에 통합 — family 폭(자율런 KR2)**): family A(critter)만 JAX화돼 있던 `jax_env`를 `make_jax_env(JaxEnvConfig(family=…))`로 **forage**(B: contact-collect overworld·CATCH inert·collect시 gym-enter 상호배제)·**muster**(D: CATCH+party 공격 부스트)까지 미러. `JaxEnvConfig.family`(0/1/2) + `JaxEnvState.party_atk_boost`. **muster 핵심 상호작용**: 부스트는 attack→battle damage→enemy_hp obs로 흐르고 `evolve()`가 `attack=form.attack`로 **리셋**(creatures.py:97) → 단순 `base+12×caught` 아님 → `party_atk_boost` 누적기(catch +12 전멤버/evolve시 해당 멤버 0, `attack_of`/`reset_boost_on_evolve`)로 정확 미러, 양 battle branch(commit fight + noncommit) 적용. **parity 0 mismatch**: 13 obs+reward+term+trunc, full 에피소드, fixed+vary, random+gym-clearing+catch-then-gym 정책, **24 tests** + **non-vacuity 가드**(`test_muster_buff_actually_exercised`가 catch[부스트]·evolve[리셋] 양 경로 실자극 증명). 3/4 family(A/B/D=type-matchup 배틀) 벡터화. **duel(C)는 type-agnostic RPS/stamina 별도 배틀 엔진=후속 포트**(jax_battle 선례). **정직 범위**: family A/B/D·non-commit·CPU·vmap-only·GPU/duel 후속. **다층 검증**: pilot(parity 0+muster 상호작용) + 24 테스트 + non-vacuity 가드 + L3 2/2 APPROVE. 372→396(+24 importorskip parity, 회귀 0; family A byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(family Update)+DESIGN §4+INITIATIVE 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-25** — [`jax-ppo-tuned`](_archive/2026-Q2/jax-throughput/08-jax-ppo-tuned/) (standard, **tuned PPO 베이스라인 + oracle-headroom 정량화 — 벤치마크 "결과 표" 실체(자율런 KR1)**): `jax_train`의 A2C-lite(truncated returns·단일 grad step)를 **proper PPO**로 격상 — `gae`(순수함수·reverse scan·value bootstrap) + `make_ppo_rollout`(value·logp_old 수집) + `ppo_loss`(clipped surrogate ε + adv-norm + value MSE + entropy) + `train_ppo`(GAE(λ)→K epoch×minibatch jit update + Adam) + `evaluate_gym_clears`(final gyms_defeated=oracle와 동일 지표). A2C `train`/`evaluate`·`jax_env` **무변경**(추가 API). `scripts/ppo_baseline.py`가 default+hard(고-gym) commit-mode서 held-out PPO를 scripted oracle ceiling과 대조. **사전약정 결정규칙**(데이터 전 고정): R1 학습·R2 PPO≥A2C·R3 PPO<0.75×oracle⇒headroom. **freeze 전 pilot**: gae property 3종(numpy ref·γ1λ1↔MC·λ0↔1-step TD) + PPO 학습 + headroom 파이프라인 입증, falsify 0. **실측**(CPU·single·200iter): default PPO held-out **0.59=oracle 1.84의 32%**(A2C 0.78), hard PPO **1.06=oracle 7.28의 15%**(A2C 1.88 거의붕괴)·held-in≈held-out(gap≈0=일반화). **R1 학습·R2 PPO≥A2C·R3 hard-and-learnable 양 config**(reframe 미발동). **striking 정직 발견**: hard서 tuned PPO(1.06) < 비추론 type_blind(2.03) — capability ladder oracle 7.28 ≫ type_blind 2.03 > PPO 1.06 선명, 현 baseline은 oracle 추론 못 깸. **정직 caveat**: single-run·작은 net·CPU·이 예산 한정(더 큰 compute는 PPO↑→headroom은 이 예산 기준)·oracle=scripted ceiling proxy·gym-clear gap=single-run 노이즈→multi-run rigor 후속. **다층 검증**: gae property + PPO smoke + L3 2/2 APPROVE. 360→365(+5 importorskip, 회귀 0; A2C/jax_env 무변경), mypy(27)/ruff/build clean. jax-throughput.md(PPO Update)+DESIGN §3.1.1+competitive-analysis(gap register)+INITIATIVE 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-25** — [`jax-noncommit-env-integration`](_archive/2026-Q2/jax-throughput/07-jax-noncommit-env-integration/) (standard, **non-commit full battle을 jax_env에 통합 — env 기본 경로까지 full-episode 벡터화**): `jax-battle-full`이 standalone(parity0)으로 포트한 non-commit 배틀(party+SWITCH+force-switch+party-wipe)을 **`jax_env`의 battle branch에 연결** — 이제 `make_jax_env(JaxEnvConfig(commit=False))`가 numpy `CritterEnv(commit_battles=False)`(env **기본** 경로 `CritterGym-v0`/`-procgen-v0`)를 미러. `JaxEnvConfig`에 `commit`/`potions`/`battle_max_turns`, `JaxEnvState`에 `items`/`battle_turn` 추가; `overworld_branch`가 진입 시 commit 여부로 commit_window/items/battle_turn 설정; 신규 `noncommit_battle_branch`(action 매핑 `<4`→MOVE·`4`→SWITCH[cyclic next-alive]·`5`→wasted ITEM + Phase1/2/3 + party-wipe/boss-dead/battle-trunc + 승리시 gym_defeated·레벨·evolve); `step`이 `config.commit`(compile-time)으로 정적 분기(commit=True default=byte-identical). **freeze 전 pilot이 두 가정 코드로 확정**(보스 항상-MOVE=`scripted_opponent` 구조적·battle-trunc 비발산) + **핵심 발견**: Phase1 SWITCH는 cyclic(`_next_alive_player`) vs Phase3 force-switch는 first-in-order(`_next_alive`)=서로 다른 순서 → JAX서 cyclic loop / `argmax`로 구분 미러(놓쳤으면 parity 깨질 off-by-one). **parity 0 mismatch**: 13 obs+reward+term+trunc, full 에피소드, fixed(num_types3)+vary(8), **4 정책**(random·gym-clearing·switch-heavy·never-attack[패배=force-switch+party-wipe+무보상 종료]) + **non-vacuity 가드**(`test_force_switch_actually_exercised`가 force-switch·wipe 실자극 증명; coverage probe 9battle·21switch·7win·5evolve). **실측**(CPU·single·vmap-only): numpy 139k/s · **jax vmap 5.08M(b=1024)=36× / 8.35M(b=16384)=60×**(commit full-env 31×와 동급=동일 overworld·다른 배틀 경제). **정직 범위**: family A·CPU·single·vmap-only(단일 jit는 손해)·potion inert(action space 유효 ITEM 미방출=정확 미러)·GPU/다른 family/tuned PPO 후속. **다층 검증**: pilot(가정·순서차) + 32 parity 테스트 + L3 2/2 APPROVE. 328→360(+32 importorskip, 회귀 0; commit byte-identical), mypy(27)/ruff/build clean. jax-throughput.md(§4 Update+§5 #1)+DESIGN §4+INITIATIVE 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-24** — [`jax-battle-full`](_archive/2026-Q2/jax-throughput/06-jax-battle-full/) (standard, **non-commit full battle JAX 포트 — 핫패스 배틀 남은 절반**): `jax_battle.py`(commit 챔피언)에 이어 **non-commit full battle**(`Battle(commit_mode=False)`: party P + SWITCH + ITEM(potion) + 기절 force-switch + party-wipe 종료)을 functional JAX로. 신규 `src/critter_gym/jax_battle_full.py`(`FullBattleState`/`FullBattleParams` + `full_battle_step`[1턴: Phase1 switch/item, Phase2 speed-order moves(빠른쪽 먼저·tie A·기절 attacker skip), Phase3 force-switch, party-wipe terminal — 전부 branch-free `jnp.where`, 동적 party gather + `argmax` next-alive] + `params_from_parties`/`initial_state` bridge). **freeze 전 pilot이 parity 게이트 통과**: numpy `Battle(commit_mode=False)`(starter 3 vs boss 1) 대비 **0 mismatch** — 배터리(attack/switch/item-heal/force-switch/party-wipe/truncation) + random 40 seed(fixed+vary chart), 매 턴 party_a_hp·active_a·boss_hp·winner·turn·done 일치. **실측**(CPU·single run): numpy 96k/s · **jax vmap 43.5M/s(b=1024)=452×**(순수 산술이라 벡터화 효율↑). **한계효용 정직**: gym-boss 실경로는 commit-mode(이미 포트)라 본 포트=env 기본(non-commit) 경로 커버 + M4 배틀 완전성이지 load-bearing 교체 아님; non-commit full-env 통합은 별도 후속(jax_env 무변경, standalone). 310→328(+18 importorskip parity, 회귀 0; `__init__` 미import), mypy(27)/ruff/build clean. jax-throughput.md(§5 #1 ✅)+DESIGN §4 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-24** — [`jax-difficulty-report`](_archive/2026-Q2/jax-throughput/05-jax-difficulty-report/) (standard, **R5 — jax_env config화로 고-gym 동적 범위 재포트 + parity + jax_train 가속**): `difficulty-dynamic-range`가 numpy서 입증한 고-gym(8) 변별 설정을 JAX 벡터화로. `jax_env`가 `_GRID/_MAX_STEPS/_MAX_GYMS=3/_BOSS_*` 하드코딩이라 못 돌리던 것을 **`JaxEnvConfig`+`make_jax_env(cfg)` factory**(JAX static-shape→cfg는 클로저 캡처 compile-time 상수)로 해소. **module-level `jax_env_step`/`jax_reset`/`encode_obs`/`make_env_step`=default-config 인스턴스로 보존**(기존 import·parity·bench byte-identical). `jax_train` config-aware(`EnvSpec`/`default_env_spec`/`difficulty_env_spec`, obs_dim 동적[patch 크기 의존]). **freeze 전 pilot**: 고-gym parity **0 mismatch**(grid6·8gym·patch 11×11>grid·num_types12·super_mult3·boss150/16, obs 13키+reward+term+trunc, random+gym-clearing, fixed+held-out) + 기존 parity 3종 무회귀(R2). **실측**(CPU·single run): 고-gym 학습 jit/vmap OK(곡선 상승) / **~196k env-steps/s vmap vs sb3 ~3.1k = ~63× FASTER**(고-gym 발산으로 default ~170×보다 배율↓, 정직). `jax_rl_demo --difficulty`. **정직 범위**: family A commit·고-gym 재포트지 difficulty 전체 JAX화 아님(scripted resolution arms는 env peek이라 numpy 유지)·GPU/tuned PPO 후속. **다층 검증**: pilot(R1 parity·R2 무회귀·R3 tracer·R4 speed) + 16 parity 테스트 + L3. 294→310(+16 importorskip, 회귀 0; default config byte-identical), mypy(26)/ruff/build clean. jax-throughput.md(R5)+DESIGN §4 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-24** — [`jax-rl-demo`](_archive/2026-Q2/jax-throughput/04-jax-rl-demo/) (standard, **M4 마감 — JAX 벡터화 env로 실제 학습 1회 데모("속도 실재" 가시화)**): #40/#41/#42가 입증한 벡터화 surface를 *데모*로 전환 — 벤치 숫자 너머 "실제로 빠르게 학습됨". 신규 `src/critter_gym/jax_train.py`(JAX-native A2C: `flatten_obs`[OBS_DIM=38]+tiny MLP+manual Adam+region-bank auto-reset+`lax.scan` rollout+jit, 전부 on-device vmap+jit) + `scripts/jax_rl_demo.py`(학습곡선+throughput+sb3 비교+held-out eval) + `tests/test_jax_train.py`(importorskip smoke 4). **설계 핵심**: sb3로 감싸면 step마다 host↔device 넘나들어 vmap 속도 소실 → JAX-native 최소 루프가 정직한 데모. env/jax_env **무변경**(parity 보존). **freeze 전 pilot이 사전약정 결정규칙(R1 `mean_late−mean_early≥std_late`, `learning_verdict` 코드로 동결)으로 분기 (a) 기계적 확정 — 사후편향 차단.** **실측**(CPU·single run): 학습곡선 **상승**(reward/step 0.0068→0.0513, ep_return~1.8→~10.0, rise 0.041≫std_late 0.003=**분기 (a) 학습+빠름**) / 학습 throughput **~0.66M env-steps/s**(1.23M steps/~1.9s, 별 run ~1.1M) vs 기존 numpy/sb3 ~3.8k = **~170× FASTER**(R4 부등식 성립=on-device vmap lock-step) / held-out **1.44** vs held-in **1.00**=**gap≈0**(seed split, (A) no-memorization 일관). **정직 framing**: A2C-lite(tuned PPO 아님)·CPU·single run·reward/step proxy=신호, sb3 baseline=기존 단일-env 경로(다중프로세스면 비율↓ but vmap 미만)·GPU 미측정·env config 하드코딩. **다층 검증**: pilot(R1/R2/R4) + L3 plan-reviewer가 A2C 수식·Adam·returns 부호·auto-reset·OBS_DIM·evaluate 마스킹 코드 정합성 확인. 283→287(+4 importorskip smoke, 회귀 0), mypy(26)/ruff/build clean. jax-throughput.md(§4 Update+§5)+DESIGN §4+competitive-analysis 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-24** — [`jax-env-integration`](_archive/2026-Q2/jax-throughput/03-jax-env-integration/) (standard, **M4 — 벡터화 full-episode env step (overworld+commit-battle 합성)**): `jax_overworld` + `jax_battle`를 **하나의 full-episode env step**으로 합성 — RL 루프가 JAX 엔진 실소비 가능. 신규 `src/critter_gym/jax_env.py`(`JaxEnvState` pytree + `jax_env_step(state,action)→(state,obs,reward,term,trunc)` `lax.cond` mode dispatch + `jax_reset` numpy bridge + `encode_obs` 13키[local_patch egocentric pad+dynamic_slice]) + `tests/test_jax_env_parity.py`(importorskip, 실제 `CritterEnv(commit_battles=True)` 대비 full parity) + bench full env 행. **freeze 전 pilot이 AC7 분기 (a) 목표 확정 + 버그 2건 포착**(jnp 인덱싱·battle중 NOOP/SWITCH champion 미공격). **실측**: jit OK / **full parity 0 mismatch**(obs 13키 전체[local_patch 포함]+reward+term+trunc, random+gym-clearing 정책, fixed+vary) / numpy 130k/s · **jax vmap 34×(b=1024)~73×(b=4096)**(full-episode 제어흐름 발산으로 슬라이스보다 배율↓, 정직 보고). **AC7 분기 (a) 완성**(pilot 미검증 local_patch까지 달성). **다층 검증이 버그 3건 포착**: pilot 2 + 구현중 가변gym(`gym_active` 마스크) + **L3 reviewer가 truncated 독립성 갭**(numpy term/trunc 독립계산=둘다 True 가능, 마지막 gym을 max_steps째 격파) → numpy 정확 미러링 수정 + edge 테스트. family A commit-mode·full obs(B/C/D·non-commit full battle은 후속). 263→281(+18, 회귀 0), mypy(25)/ruff/build clean. jax-throughput.md(§4·§5) + competitive-analysis 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-24** — [`jax-battle-port`](_archive/2026-Q2/jax-throughput/02-jax-battle-port/) (standard, **M4 — commit-mode 챔피언 battle JAX 포트(핫패스 어려운 절반)**): `jax-hotpath-foundation`(overworld)에 이어 battle sub-MDP 포트. **commit-mode 챔피언 경로**(gym-boss 실제 경로, `CritterGym-commit-v0`)를 functional JAX로: 신규 `src/critter_gym/jax_battle.py`(`ChampionBattleState`/`ChampionBattleParams` pytree + `champion_battle_step`(lax.cond 속도순 + where faint/terminal) + `eff_matrix`/`params_from_creatures` numpy bridge) + `tests/test_jax_battle_parity.py`(importorskip, 실제 `Battle(commit_mode=True)` 대비 parity) + bench battle 행. **freeze 전 pilot이 AC7 분기 (b) 확정 + hp 음수 parity 버그 포착**(numpy `take_damage` `max(0,·)` 클램프 누락→`jnp.maximum` 교정). **실측**(CPU·single run): jit OK / **parity 0 mismatch**(fixed 45 + vary 24 config: champ_hp·boss_hp·winner·turn·done) / numpy 112k/s · **jax vmap 117M/s(b=1024)=1047×**(순수 산술이라 overworld 186×보다 벡터화 효율↑). **정직 framing: 이득 vmap 한정**. **AC7 분기 (b)**: commit-mode 포트 + full(switch/item/multi-creature non-commit)은 후속 `jax-battle-full` 분리(scope balloon 방지). overworld+commit-battle=핫패스 load-bearing 대부분. + `jax_overworld.py` 1줄(직전 task latent UP037 lint 정정; 프로세스 학습: mypy 수정 후 ruff 재실행 누락). 210→263(+53, 회귀 0), mypy(24)/ruff/build clean. DESIGN §4 정신 + jax-throughput.md + competitive-analysis 갱신. Acceptance AC1–AC8. _(commit pending)_
- **2026-06-24** — [`jax-hotpath-foundation`](_archive/2026-Q2/jax-throughput/01-jax-hotpath-foundation/) (standard, **M4(Throughput/JAX) 착수 — overworld 슬라이스 JAX 포트 토대 + de-risk**): (B) 종결 후 피벗(M4 override, (B)의 M5-enabler-before-M3 선례와 동일 functional-readiness-first). env step이 Python dict/mutable라 직접 jit 불가 → **overworld step(move+catch/contact-collect, battle 제외)만 functional JAX 포트**해 핵심 미지수 2개를 de-risk. 신규 `src/critter_gym/jax_overworld.py`(`OverworldState` pytree + branch-free `overworld_step` family A/B + `state_from_region` numpy bridge + `make_step_fn` jit) + `tests/test_jax_parity.py`(importorskip, 실제 `CritterEnv`/`ForageEnv` 전이 대비 parity) + `scripts/bench_throughput.py`(정직 framing) + `pyproject [jax]` extra(코어 numpy-only 유지). **freeze 전 pilot이 AC7 분기 (a) 확정**. **실측**(단일머신·single run·CPU): ① jit 컴파일 OK(family A·B) ② **parity 0 mismatch**(동일 seed→동일 trajectory, 재현성 북극성 #3 유지) ③ throughput — numpy ~410k/s · **jax single 55k/s(0.13×=더 느림)** · **jax vmap 26.5M(b=1024)→76.5M(b=16384)=186× numpy**. **정직 framing: 이득은 전적으로 vmap 벡터화, 단일 env는 손해**(헤드라인 금지). CPU vmap만으로 M4-EC3 ≥10M GPU 목표 7.6× 초과(GPU는 범위 밖). **feasibility verdict=양성**→후속 `jax-battle-port`. caveat: battle 미포트(M4-EC1 *토대*, 부분)·CPU·single run·int32(x64 off, env 범위 무해)·난이도 작업이 메커닉 바꾸면 재포트(R4). 199→210(+11 parity, 회귀 0), mypy(23)/ruff/build clean. DESIGN §4 + 신규 `explanation/jax-throughput.md` + competitive-analysis 갭 register 갱신. Acceptance AC1–AC8. _(commit pending)_

## env-core
- **2026-06-24** — [`duel-fewshot-adapt`](_archive/2026-Q2/env-core/33-duel-fewshot-adapt/) (standard, **(B) duel 제로샷 불가 *메커니즘 입증* + few-shot 적응 SLOW — (B) 스레드 종결**): #32가 국소화한 frontier(duel) 공략. 사전 탐색이 원인 발견 → 입증: duel RPS가 의존하는 `charge` obs가 train 3 family서 **항상 0(degenerate)** → gradient 0 → **제로샷 duel 전이 원리적 불가**. `charge_trace`+`test_charge_degenerate_in_train_families`(numpy-only, 결정론: train charge≡0/duel battle>0)로 박제. **일반 명제**: genre 전이는 새 메커닉이 *train서 degenerate한 obs*에 의존하면 제로샷 불가. + `fewshot_adapt_curve`+`--fewshot`(held-out duel fine-tune, held-out eval seed≠adapt seed). **실측 곡선**(base 150k, 5run): zero-shot **0.65±0.20** / 25k 0.59 / 50k 0.76 / **100k 1.45±0.48** → **사전약정 verdict=SLOW**(≤50k는 z₀+σ₀ 안=노이즈; 100k에야 ~2배 상승). 단일-seed pilot 50k≈1.6은 또 노이즈→**multi-run 4번째 교정**. **결론: duel은 적응으로 닿지만 *느림*(~100k=base 2/3) = RPS는 거의 새로 배우는 진짜 새 skill, 빠른 전이 아님.** **(B) 스레드 종결**: 메커닉 이웃 제로샷 전이 / 구조적 새 배틀시스템(duel)은 제로샷 *원리 불가*(degenerate)+느린 few-shot — sharply characterized partial result(open도 solved도 아님). 제로샷≠few-shot 구분. env 코어 무변경(scripts continue-learning). 197→199(+2: charge 가드 numpy-only + fewshot smoke, 회귀 0), mypy(22)/ruff/build clean. DESIGN §3.1.1 + genre-generalization.md 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-24** — [`transfer-budget-recovery`](_archive/2026-Q2/env-core/32-transfer-budget-recovery/) (standard, **(B) 예산으로 held-in RECOVERY + confound-reduced gap — "전이는 메커닉 이웃 안, duel로는 실패"**): #31이 보류한 probe(더 큰 예산) 실행. `budget_ladder_configs`+`--budgets`(baseline-net, 용량 배제). **실측 사다리**(5run): 250k 2.44→**400k/500k 2.75±0.39**(held-in PLATEAU ≈2.75, 회복임계 2.5 통과=**사전약정 RECOVERY**; 단일-seed pilot 1.4~2.2는 또 노이즈→**multi-run 3번째 교정**). RECOVERY라 AC3 트리거 → 회복예산(400k)서 **full-LOO confound-reduced gap 재측정**(`loo_multirun`): held-out **critter −1.08±0.73·forage −1.48±0.51·muster −0.12**(≤0=전이 OK) vs **duel +1.73±0.61**(robust 양수, std≪gap). **held-in 회복(2.0~2.75=평범 아님) 상태라 generalist-mediocrity 아님 = 진짜 전이 실패**(duel만 구조적 다른 배틀시스템). **정직 결론: (B)는 real but structurally bounded — 학습 정책이 메커닉 이웃(수집+타입상성) 안에선 전이, *진짜 다른 게임시스템(duel)*으론 실패. open frontier를 cross-배틀시스템 전이로 국소화.** caveat: 음수 gap은 held-out 난이도 비대칭도 반영(forage≈4.0)·깨끗한 신호는 duel·held-in 천장 2.75<#26 2.94·단일 config·N16. 196→197(+1 smoke, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 + genre-generalization.md 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-23** — [`transfer-capacity-budget`](_archive/2026-Q2/env-core/31-transfer-capacity-budget/) (standard, **(B) 용량×예산 동시 스케일 — PARTIAL 회복, 예산은 lever·용량은 아님**): #30(net만↑·50k=underfit)·#28(예산만↑·150k) 사이의 안 해본 점=용량+예산 *동시* 스케일. `held_in_sweep`+`SweepRow`+`--sweep`(muster anchor fold multi-run, 천장 기준선 표 + 사전약정 회복임계 2.5). **실측**(5 run): baseline-net **150k 2.07±0.62 → 250k 2.44±0.35**(std 조임, 2.5 근접) / big-net@250k **1.87±0.39**(robust 하락). **사전약정 verdict=PARTIAL**(2.44>2.07 천장,<2.5). 두 robust 발견: ① **예산이 held-in 계속 올림**(2.07→2.44)→**#28 "compute 병목 아님"을 부분 정정**(그건 저예산 조기결론) ② **용량(큰 net)은 robust 해로움**=lever는 예산이지 용량 아님. **가장 긍정적 점**: baseline@250k held-in 2.44(평범 아님)·held-out 2.49·**gap −0.05≈0** — held-in 비평범+gap~0인 첫 점(단일 fold/config·큰 std=주장 아님). AC3 조건부: held-in<2.5(RECOVERY 미달)→full-LOO gap 재측정 **보류**(다음 probe=더 큰 예산, 수확체감 감시). **경계 미종결**(예산 cheap-ish 아직 오름·용량 배제). 195→196(+1 smoke, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 + genre-generalization.md 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-23** — [`transfer-skill-policy`](_archive/2026-Q2/env-core/30-transfer-skill-policy/) (standard, **(B/a') 정책·obs 개선으로 held-in 끌어올리기 — 정직한 음성**): #28이 좁힌 "남은 직접 경로"(정책/obs 표현 개선)를 직접 검증. `train_and_transfer`에 개선 노브 `net_arch`+`scale_obs`(+`--improved`) 추가 + `_ScaleObs`(결정론적 대형키 스케일). **pilot이 whole-obs `VecNormalize`가 범주형 obs(in_battle/local_patch/type) 망쳐 *해롭다*를 밝힘→제외**(AC7 분기), 개선설정=net256+선택스케일로 정련. **실측**(baseline vs improved, 50k×5run, N16): 개선설정이 widened held-in을 **못 올리고 4 fold 전부 오히려 낮춤**(muster 1.73→1.15·duel 1.74→1.10·critter 0.86→0.65·forage 0.92→0.68; drop 대부분 run-std 초과=robust) — net256+스케일이 50k서 **underfit**. **정직 음성 verdict**: compute(#28)도 이 정책/obs 레버도 held-in을 #26(2.9) 쪽으로 못 올림 → **generalist-mediocrity confound stubborn 잔존**, (B) 여전히 신호. 제거엔 더 깊은 작업(아키텍처/커리큘럼/용량+예산) 필요. AC3 조건부대로 **held-in 미상승→gap 재측정 불요** 정직 기록. 194→195(+1 smoke, 결정론 검증 포함, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-23** — [`transfer-rigor`](_archive/2026-Q2/env-core/29-transfer-rigor/) (standard, **(B) widened-train 전이 신호 robust 재측정 — multi-run + 예산↑**): #27 양성 신호의 두 caveat(generalist-mediocrity·단일run) 메우기. `train_and_transfer_loo_multirun`+`--runs N`(per-fold run-간 **mean±std**) + `MultiRunFoldReport`. **사전약정 결정규칙**으로 사후 narrative 편향 차단(muster fold: 신호강화 held-in≥2.5∧gap≤+0.5 / 아티팩트 gap>+1.0 / 불확실 std≥gap). **실측**(50k·150k × 5 run, N16): (1) gaps가 #26 +2.56보다 **robust하게 훨씬 좁음**(유지). (2) 그러나 대체로 **held-in 하락**(widened 0.9~2.1≪#26 2.94); 예산 50k→150k는 held-in을 **약간만** 상승(muster 1.73→2.07, <2.5) → generalist-mediocrity **축소되나 제거 안 됨**(병목=정책/obs, compute 아님). (3) **#27 음수 muster gap은 robust 아님** — 5 run서 +0.22±0.45/+0.44±0.72, std>gap → 사전약정상 **불확실**(부호 불안정); #27 −0.25=run 노이즈. **단일-seed pilot의 "held-in 안 오름"도 노이즈→multi-run이 교정**(예산↑로 mean은 상승). (4) duel이 robust 최난 held-out(+1.15±0.11). **정직 verdict**: wider train이 gap 좁히나 *낮은 절대skill confound*+음수는 노이즈 → (B) 여전히 신호, 깨끗한 주장엔 **절대skill↑(정책/obs 개선, compute·seed 아님)** 필요. **pilot이 (a)예산↑로 held-in 끌어올리기 전제를 일부 falsify→AC7(ii)/R3 사전등록 분기로 정직 reframe**((a)=수정→발견). 193→194(+1 smoke, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-23** — [`genre-transfer-policy`](_archive/2026-Q2/env-core/28-genre-transfer-policy/) (standard, **(B) 전이하는 학습 정책 — widened-train LOO 전이 측정**): obs 조화(#27) 위에서 train 분포를 넓혀(**duel 포함**) 학습 정책의 unseen family 전이 gap을 측정 — #26 baseline(train{A,B}→muster, gap **+2.56**)과 **동일 metric**(전이 gap=held_in−held_out family, 리턴 단위). `scripts/genre_learned_transfer.py`(`[rl]`) `train_and_transfer_loo`+`--loo`: 4 family LOO(각 family held-out, 나머지 3 family 단일 PPO net 학습). **실측**(PPO 50k, N16/16, 단일run): unseen-muster gap이 #26 **+2.56(2-family) → −0.25(3-family, duel 포함)** 로 크게 좁아짐, 타 fold critter −0.92/forage −1.48/duel +1.08 — gap이 0 근처/음수로 붕괴 = **wider train이 unseen 전이를 돕는다는 양성 신호**. ⚠ **정직 caveat**: widened held-in 절대성능도 2.94→1.1~2.0 **하락**(1 net·동예산·3 family = generalist 평범화) → 좁아진/음수 gap은 *families 전반 균일 평범*도 반영(강한 전이의 증명 아님); 음수 gap(held-out>held-in)은 *낮은 절대skill + held-out이 더 쉬움*이 유력. (B)가 "측정상 미해결(#26)" → "wider train이 gap 좁힘(이 task)"으로 이동 = **신호지 증명 아님**(절대skill↑·multi-run 필요). 마일스톤 M5/층2(M3 공개보다 먼저). 192→193(+1 smoke importorskip, 회귀 0), core CI numpy-only 유지, mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-23** — [`obs-harmonization`](_archive/2026-Q2/env-core/27-obs-harmonization/) (standard, **(B) 전이 정책 이니셔티브 선행 — 4-family obs 조화**): "전이하는 학습 정책"(competitive-analysis 갭 register 1순위, moat 층2)의 **하드 블로커 enabler**. #26이 obs 불일치로 **duel(13키)을 제외**(나머지 11키)하던 것을 해소 — 4 family(critter/forage/duel/muster)가 **단일 공유 obs 공간**을 노출하게 조화. `env_family.HARMONIZED_OBS_KEYS`(=`REQUIRED_OBS_KEYS` ∪ {`player_charge`,`enemy_charge`}) SSOT + `MAX_CHARGE_OBS`; base `CritterEnv`가 charge 키를 **0-마스킹**으로 노출(비-duel), `DuelEnv`는 실제값 override(중복 space 확장 제거). **A안(코어 최소 침습+superset/마스킹) freeze 전 pilot로 회귀 표면 실측**(base에 charge 추가→전체 테스트: 회귀 단 1건=AC5가 의도적으로 뒤집는 "duel 거부" 테스트)→A안 확정(B안 reframe 불필요). 패딩은 **행동 불변**(scripted 정책은 이미 `.get(charge,_ZERO)`): 신규 테스트로 *수치 증명*(charge 키 제거 시 액션 동일). `assert_obs_compatible`가 duel 포함 4 family 통과 + `_MultiFamilyEnv` duel 포함 구성 가능(smoke; **4-family 전이 *실험*은 다음 task**, gap 미축소). 마일스톤 override: M5/moat 층2 enabler를 M3 공개 EC보다 먼저(기능 준비 우선 방침). 185→192(+7, 회귀 0), mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7. _(commit pending)_
- **2026-06-23** — [`genre-learned-transfer`](_archive/2026-Q2/env-core/26-genre-learned-transfer/) (standard, **(B) 학습-정책 전이 첫 측정**): (B) 장르 일반화의 핵심 미싱피스(competitive-analysis 갭 register 1순위) — 지금까지 scripted 대조였던 (B)를 **학습 정책의 unseen family 전이**로 첫 측정. `scripts/genre_learned_transfer.py`([rl]): train families {critter,forage}에 PPO 학습(reset마다 family 택1, 공유 obs) → **held-out family {muster}** 전이. obs 제약상 동일 11키 family만(**duel 13키 제외**=obs 조화 future, `assert_obs_compatible` 가드). held-in eval seed는 `split_train_pool`로 학습시드와 disjoint, held-out family는 학습 미노출(family-level split). **실측**(PPO 50k, N16/16): held-in **2.94 ±2.02** vs held-out-family **0.38 ±0.70**, **전이 gap +2.56**(std 훨씬 넘음) — **학습 정책이 unseen family로 전이 안 됨**(muster의 수집-강화 메커닉 미학습) = **정직한 "(B) 학습 genre 전이는 어렵다/미해결" 신호**(실패도 증명도 아님). 한 train-set→한 held-out family=첫 측정이지 증명 아님; 닫는 것=M5/moat 층2. core CI numpy-only 유지(PPO `[rl]` 뒤), 183→185(+2 smoke importorskip, 회귀 0), mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`difficulty-generalization`](_archive/2026-Q2/env-core/25-difficulty-generalization/) (standard, **M3 신뢰성 + (A) hard-and-gap≈0**): DESIGN §3.1.1 로드맵("toy서 gap≈0은 능력 예측력 약함"). **pilot이 "깨끗한 단조 scripted 난이도 사다리"를 falsify**(난이도 다차원: num_types=추론난이도는 blind엔 *쉬움*/보스 stat=cliff/scripted oracle 천장 ~0.6) → **학습정책 gap 실험**으로 정직 reframe(typechart-depth식). `scripts/difficulty_generalization.py`(`[rl]`): 난이도 **점** 3종(calibrated 사다리 아님) × PPO held-in 학습 → held-in vs held-out gap(기존 `measure_generalization` 재사용, held-in eval은 `split_train_pool`로 학습시드와 disjoint=gap 부풀림 없음). **scripted gap≈0은 trivial(암기 불가)이라 학습정책이 옳은 측정**. **실측**(PPO 40k, N=16/16): d0 gap +0.375(±0.86), d1 +0.438(±0.43), d2_hard +0.062(±1.64) — **모든 점의 gap이 per-seed std 안**(가장 어려운 d2 포함), 학습정책에서도 gap≈0과 *consistent*. **정직 한계**(L3 honesty 반영): 큰 std·저예산서 "gap-within-std"는 *약한 증거*(작은 real gap을 zero와 구분 못 함, "입증" 아님)·단일run·N16 = **신호**. DESIGN §3.1.1 "Toward hard-and-gap≈0" 문단 추가. core CI numpy-only 유지(PPO `[rl]` 뒤), 181→183(+2 smoke importorskip, 회귀 0), mypy(22)/ruff/build clean. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`competitive-analysis`](_archive/2026-Q2/env-core/24-competitive-analysis/) (standard, docs-only, **공개 전 갭 탐지기**): OSS RL 벤치마크(Procgen/Craftax/XLand/NetHack) 대비 **정직한 비교 분석** `docs/explanation/competitive-analysis.md` — 사용자 방침(공개 맨 마지막, 기능 준비+분석 먼저) 지원. capability 매트릭스(9축×5열) + 트레이드오프 산문(**열위 먼저**: Craftax/XLand 속도, Procgen/NetHack 성숙·채택, NetHack/Crafter 난이도, XLand meta-RL 폭) + peer verify-list + **갭 register**(우리가 주장 못 하는 것 → 필요 기능 → 마일스톤). **정직성**: peer 사실은 학습지식 기반이라 전부 `[verify]` 라벨(공개 전 1차 출처 검증), 속도는 numpy CPU/core ≠ JAX GPU basis 명시(동일행 수치비교 금지), 우리 수치는 논문/DESIGN 일치(날조 0), DESIGN §9 "moat prospective" 자기평가 준수("전 축 우위" 0). **결론**: 공개 전 leverage 최대는 **난이도 스케일(toy→hard)** + **family 확장+학습정책 held-out(B 토대→주장)**, **JAX 포트**(채택 enabler). 제품 코드 무회귀(181 passed 불변), broken-link 0. L3 APPROVED(2/2). Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`oss-release-prep`](_archive/2026-Q2/env-core/23-oss-release-prep/) (standard, **M3-EC5 전진(준비)**): 오픈소스 릴리스 **로컬 산출물** 준비. (1) MIT `LICENSE` 파일 신규(pyproject 이미 MIT 선언, 누락 파일 보강; copyright "CritterGym contributors" 교체 가능). (2) stale `README.md`("Phase 0 — Nothing built yet") → **실제 빌드 상태**로 재작성: install(`pip install -e .` + `[rl]`/`[viz]`/`[render]`) + quickstart(`register_envs`+`gymnasium.make` **env id 6종** 표, family A–D) + "What it measures"(A 측정/B 토대, 논문 링크) + 정직 positioning(**Pokémon=메타포 비경쟁**, Procgen/Craftax/XLand 대비) + 재현(seed split) + citation + Release status. (3) `CONTRIBUTING.md` 신규(dev 셋업·4 체크·정직성 규율·lifecycle). **수치 verbatim**(held-in 40%/held-out 45%, ~266k, gate ≥0.20/≥0.10, C +3.9 vs +0.2)는 논문/DESIGN과 일치(L3 코드·논문 대조 APPROVE, 날조 0). **정직 범위 경계**: 실제 외부 발행(Prime Intellect Hub 리스팅·repo public)은 **사용자 게이트**(M3-EC6 선례), README "Release status"에 미수행 명시 — EC5 자동충족 주장 0. 제품 코드·테스트 무변경(181 passed 불변), LICENSE↔pyproject 정합, broken-link 0. M3-EC5 **준비**(외부 발행은 사람 결재 후 충족). Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`arxiv-writeup`](_archive/2026-Q2/env-core/22-arxiv-writeup/) (standard, docs-only, **M3-EC4 전진**): 측정 인프라+결과를 **arXiv 논문 초안**으로 패키징 — `docs/paper/critter-gym.md`(abstract/intro+positioning/env design/측정 결과 A·load-bearing·learnability·B/related work/honest limitations/conclusion 8섹션) + `docs/paper/README.md`(수치↔모듈/테스트 재현 맵). **정직성 = 자산**: 모든 정량 주장이 코드/측정 근거(날조 0), **CI-reproducible(test-frozen gate)** vs **run-derived(run 평균)** 구분 라벨. Pokémon=plain-language 메타포(비경쟁), Procgen/Craftax/XLand 대비 정직 벤치. (A) 인스턴스 일반화(held-out 45% vs held-in 40%, gap≈0) 측정 / (B) 장르 4-family 토대(C gap +3.9 vs +0.2, D muster 1.42≫rush 0.00, B forgiving — within-family 대조=skill-structural)는 **토대지 증명 아님** 명시. honest limitations 전 caveat(B=토대/D confound/단일run·N/learnability 천장·oracle==infer/C win 예측가능성/재현 tier). L3 reviewer 코드 대조로 수치 정확성 검증, honesty SUGGEST(duel 예측가능성 caveat) 반영. docs-only 무회귀(181 passed 불변), broken-link 0. M3-EC4 **초안 산출**(LaTeX·제출은 후속). Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`family-d-muster`](_archive/2026-Q2/env-core/21-family-d-muster/) (standard, **M3 신뢰성 + (B) 토대 4-family 확장**): (B) 장르 일반화 토대를 **네 번째 family**로 확장 — 세 번째 구조 축(**진행 의존**). family D `MusterEnv`(`CritterGym-muster-v0`)는 **수집이 배틀력 좌우**(CATCH 성공 시 파티 공격력 +12) + 강한 보스(hp300/def24) → 보스를 이기려면 **먼저 muster**(수집)해야 함 = 수집·배틀 의존 결합. `CritterEnv` 서브클래스로 `_step_overworld`만 override(family A/B/C 무변경). `genre_generalization`에 obs-only 레퍼런스 `rush_policy`(직행·수집안함)/`muster_policy`(수집-우선) 추가, LOO 4-family 자동 확장. **측정**(within-family 대조, gym-clears): family D muster **1.42 ≫ rush 0.00**(스킬 load-bearing), family A muster 1.08 ≤ rush 1.58(스킬 **무용**, catch 버프 없어 수집은 step 낭비) → gap이 **skill-structural**(family C와 평행한 정책-특정 대조; D는 muster로 winnable이라 "그냥 어려움" 아님). **정직성**: family D는 강한 보스(다른 calibration=family 정체성)라 **raw cross-family LOO 평균 gap은 난이도 confound** → 헤드라인은 within-family 대조(같은 config, 정책만 변주)로 통제, DESIGN §3.1.1 명시. 4 family도 **토대지 장르 일반화 증명 아님**(많은 family 필요). family A/B/C 무회귀(174→181, 신규 7), check_env **6종**, mypy(22)/ruff/build clean, honesty 가드 pass. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`learnability-precision`](_archive/2026-Q2/env-core/20-learnability-precision/) (standard, **M3 신뢰성 + (A) learnability 정밀화**): #17의 정직한 caveat 1번(episode return = 격파+진화 **합산** → learned가 oracle을 "넘는" 착시) 해소. **gym-clear-only 메트릭** 도입 — `EpisodeOutcome`(return/gyms_cleared/evolutions)으로 보스 격파와 진화 stream 분리, `run_episode`가 outcome 반환, `LearnabilityReport`에 `*_gyms`(gym-clear-only means) combined와 **병기**, `measure_learnability` 양 메트릭. **측정**(held-out, num_gyms=8): gym-clear-only oracle/infer **4.19** ≫ type_blind 1.81 > probe 1.06 — 진화 인플레(~1.44) 제거 후 **load-bearing 순서 유지**. `scripts/learnability.py`에 gym-clear 중심 보고 + `--runs N`(다중 PPO seed 평균, 단일run caveat 완화). **정직성**(L1 measurement reviewer 2-SUGGEST 반영): gym-clear-only는 (i) **num_gyms 천장**으로 강한 arm 간 gap 압축, (ii) **oracle==infer 구분불가**(이 config는 한 번 보면 추론 자명 → 추론이 *suffices*지 이 메트릭만으로 load-bearing 증명 아님, 증명은 scripted gate 몫), (iii) 단일config·N modest·다중run [rl]비CI — 모두 DESIGN §3.1.1 명시. 공개 API 무회귀(`measure_learnability` 시그니처+combined·`arm_mean` 보존), PPO smoke pass. 171→174(신규 3, 회귀 0), mypy(21)/ruff/build clean, honesty 가드 pass. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-23** — [`battle-system-family`](_archive/2026-Q2/env-core/19-battle-system-family/) (standard, **M3 신뢰성 + (B) 장르 일반화 토대 강화**): DESIGN §3.1.1 (B)의 정직한 약점(family B가 thin=수집 1축, A-튜닝 정책이 gap≈0 전이=관대한 축)을 메우는 **세 번째 env family**. family C `DuelEnv`(`CritterGym-duel-v0`)는 **배틀 시스템 자체를 교체** = family B가 결여한 *더 강한 구조 축*: **타입-무관 stamina/commit RPS 결투**(ATTACK>CHARGE>GUARD>ATTACK, charge가 공격 배율; 타입표·무브선택·스위칭 없음 → family A의 load-bearing 스킬인 타입추론이 무용). `CritterEnv` 서브클래스로 배틀 경로만 override(family A/B 무변경), duel charge를 **extra obs 키**로 노출(`conforms` ⊇ REQUIRED → 계약 무위반, 특권접근 없이 공정 플레이). `genre_generalization` **다-family leave-one-out** 측정 + obs-only 레퍼런스 정책(`type_attacker`/`duel_aware`) 추가(2-family API 무회귀). **측정**(LOO {critter,forage,duel}, held-out N=12): held-out=duel에서 **A-튜닝 0.583/gap +3.917 ≫ C-적합 4.333/gap +0.167** = gap이 **skill-structural**(틀린 스킬, C는 winnable)이지 난이도 confound 아님 — **family B(gap≈0)가 못 만든 정책-특정 신호**(L1 measurement reviewer 요구한 anti-confound 대조). **정직성**(헤드라인 과대 금지): 3 family도 토대 강화지 **장르 일반화 증명 아님**(많은 구조-상이 family 필요); gap=signal; 캐비엣(결정론 보스+charge-in-obs readability, N=12 단일run) DESIGN·report 명시(L3 SUGGEST 반영). freeze 전 pilot로 5조건(conforms/구조적상이/winnable/A-비전이/skill-structural 대조) 검증 후 진입. family A/B 무회귀(160→171, 신규 11)+check_env **5종**, mypy(21)/ruff/build clean, honesty 가드 pass. Acceptance AC1–AC6. _(commit pending)_
- **2026-06-22** — [`genre-generalization-foundation`](_archive/2026-Q2/env-core/18-genre-generalization-foundation/) (heavy, **M3 신뢰성 + (B) 장르 일반화 토대**): DESIGN §3.1.1 (B) environment-level(구조-상이 env 패밀리 간) 일반화의 **측정 머신을 end-to-end로 세움**(이니셔티브급의 토대 첫 슬라이스). (1) **env-family 추상화** `critter_gym.env_family`(공유 obs/action 계약 `conforms` + family registry + `trajectory_signature`); CritterEnv(family A) **무변경** 만족. (2) **family B** `ForageEnv`(`CritterGym-forage-v0`) = 구조-상이 **contact-collect**(creature 타일 진입 시 자동 수집, CATCH inert) vs family A action-collect; **same-seed·same-actions→A≠B trajectory** 테스트로 시드변형 환원불가 입증. (3) **env-level 측정** `critter_gym.genre_generalization`(numpy-only, `generalization.evaluate` 재사용): train-family→unseen-family 갭. **측정**: random gap −0.35, greedy(A-튜닝) gap +0.00(A-정책이 B로 전이 — 머신 작동 입증). **정직성**(헤드라인 과대 금지): 2 패밀리=토대지 장르 주장 아님 / gap=신호(threshold 아님) / family B 최소성(수집 1축, gap≈0은 축이 관대해서) — 신뢰할 장르 주장은 다수 구조-상이 패밀리 후속. family A 무회귀(151→160, 신규 9만) + check_env **4종**(fixed/procgen/commit/forage). Acceptance AC1–AC6, mypy(20)/ruff/build clean. _(commit pending)_
- **2026-06-22** — [`learnability-measurement`](_archive/2026-Q2/env-core/17-learnability-measurement/) (standard, **M3 신뢰성 + (A) learnability 완성**): `reasoning-load-bearing`의 정직한 follow-up — **학습 정책(PPO)이 commit-v0에서 추론을 *학습*하는가** 측정. (1) commit 모드 **챔피언-선택 액션 UX**(`critter_env._commit_window`: 보스전 turn-0 commit window — action 4 cycle=무턴/무피해, 첫 move lock; "유리하면 attack 아니면 switch" 전략이 동역학에 암묵 내장돼 **M1 obs 무변경**). (2) `critter_gym.learnability`(numpy-only): env-level reference arm 4종(oracle/infer/type_blind/probe) + `measure_learnability`(held-in/out split 가드) — 액션 UX 경유로도 `oracle≥infer>probe` 재현. (3) `scripts/learnability.py`(`[rl]`): PPO commit-v0 학습→reference 대조. **PPO 100k 측정: learned held-out 4.00 ≫ type_blind 1.88·probe 1.56, infer-ref 3.25 수준 이상 = 양성 learnability 신호** — (A) 차별점이 구조뿐 아니라 *학습 에이전트 수준*에서도 작동. **정직 caveat**(헤드라인 과대 금지): return=격파+진화 합산(learned>oracle는 진화 착시)·N16·단일run → 정밀 벤치 아닌 *신호*로 보고. acceptance=*측정 산출+정직 보고*(성능 freeze 아님, §4 교훈). DESIGN §3.1.1 follow-up 갱신. M1·procgen·commit-v0 무회귀(48 passed+check_env ×3). Acceptance AC1–AC6, 151 passed/2 skipped(140→151), mypy(17)/ruff/build clean. _(commit pending)_
- **2026-06-22** — [`reasoning-load-bearing`](_archive/2026-Q2/env-core/16-reasoning-load-bearing/) (standard, **M3 신뢰성 + DESIGN §3.1.1 해소**): `typechart-depth` 가 불가 입증→descope 했던 **"infer-the-meta provably load-bearing"** 을 **team-commit 보스 경제**로 *scripted-arm 실증*. freeze 전 achievability pilot 통과 후 진입(§4 함정 회피). `Battle(commit_mode=True)`: 보스전 **크리처 1마리 commit** — switch no-op + faint 시 force-switch 순회 차단 + active-faint=즉시패배 → (a) 다중파티 brute-force 제거 + (b) 배틀내 probing 구조적 불가. 난이도 knob `TypeChart.super_mult`·`gym_boss(hp/atk/df)` env/registration config 노출(상수 X), `CritterGym-commit-v0` 등록(num_types12/super_mult3/boss140·18/commit). **게이트(`test_reasoning_gate.py`, 42 고정 held-out 시드, product API): Gate0 oracle 1.000≫type_blind 0.521=0.479≥0.2, Gate1 infer 0.836>probe 0.473=0.363≥0.1** — 추론이 load-bearing. **정직성**: scripted-arm 으로 *task 구조* 가 추론을 강제함을 입증(M1 경제가 결여했던 선결조건); *학습* 정책이 추론을 실제 *학습* 하는지는 미측정 **follow-up**(DESIGN §3.1.1 + honesty 가드 재정의로 learnability 과대표현 차단). **M1 완전 무회귀**(commit_battles/super_mult 기본 off; 42 passed + check_env ×3). Acceptance AC1–AC6, 140 passed/2 skipped(128→140, 회귀 0), mypy/ruff/build canonical clean. _(commit pending)_ `typechart-depth` 가 불가 입증→descope 했던 **"infer-the-meta provably load-bearing"** 을 **team-commit 보스 경제**로 *scripted-arm 실증*. freeze 전 achievability pilot 통과 후 진입(§4 함정 회피). `Battle(commit_mode=True)`: 보스전 **크리처 1마리 commit** — switch no-op + faint 시 force-switch 순회 차단 + active-faint=즉시패배 → (a) 다중파티 brute-force 제거 + (b) 배틀내 probing 구조적 불가. 난이도 knob `TypeChart.super_mult`·`gym_boss(hp/atk/df)` env/registration config 노출(상수 X), `CritterGym-commit-v0` 등록(num_types12/super_mult3/boss140·18/commit). **게이트(`test_reasoning_gate.py`, 42 고정 held-out 시드, product API): Gate0 oracle 1.000≫type_blind 0.521=0.479≥0.2, Gate1 infer 0.836>probe 0.473=0.363≥0.1** — 추론이 load-bearing. **정직성**: scripted-arm 으로 *task 구조* 가 추론을 강제함을 입증(M1 경제가 결여했던 선결조건); *학습* 정책이 추론을 실제 *학습* 하는지는 미측정 **follow-up**(DESIGN §3.1.1 + honesty 가드 재정의로 learnability 과대표현 차단). **M1 완전 무회귀**(commit_battles/super_mult 기본 off; 42 passed + check_env ×3). Acceptance AC1–AC6, 140 passed/2 skipped(128→140, 회귀 0), mypy/ruff/build canonical clean. _(commit pending)_
- **2026-06-22** — [`typechart-depth`](_archive/2026-Q2/env-core/15-typechart-depth/) (standard, **DESCOPED**, M3 신뢰성): procgen 타입 시스템 심화 — `ElementType` 3→15(F/W/G=인덱스 0/1/2 유지), procgen-v0 `num_types=12`(시드별 숨은 차트 66 정향관계, 3-cycle보다 외우기 훨씬 어려움) + 보스 타입을 시드별 소수 풀서 **반복 추출**(에피소드 내 재출현, was 0/40→34/40) + winnability 구조 보장. **M1 고정월드 완전 무회귀**(num_types=3 기본, 풀리힐 유지). **정직성 디스코프**: 원래 목표("infer-the-meta provably load-bearing")는 G1 후 pilot이 *달성 불가* 입증(no-heal에서 switch 비용이 매치업 이득 압도 → type-blind 1위) → 사용자 승인 (가)로 no-heal keystone·infer>probe 게이트 드롭, "추론 load-bearing=future work"를 DESIGN §3.1.1에 명시(코드 honesty 가드 테스트 포함). 포기 아닌 falsification(가설을 G2 전에 반증, transparent 축소). Acceptance D1-D10, 128 passed/2 skipped, mypy/ruff/build canonical clean. _(commit pending)_
- **2026-06-22** — **M3-EC6 충족 (결재)**: `scripts/killer_demo.py` 실제 실행(PPO 100k 학습) → held-out seed 1000000(새 맵+새 타입표) **보스격파 GIF** `docs/assets/killer_demo.gif`. 단일 일화 아님 — **held-in 8/20(40%) vs held-out 9/20(45%) 보스격파율, 일반화 갭 ≈ 0**(암기 아닌 진짜 일반화 = 우리 moat 실증). 절대성능 45%는 향상 여지(데모 주장=일반화는 입증). milestones M3-EC6 `[ ]`→`[x]`.
- **2026-06-22** — [`killer-demo`](_archive/2026-Q2/env-core/14-killer-demo/) (standard, **M3-EC6 수단**): 킬러데모의 녹화 파이프라인 — `critter_gym.demo.record_episode`(정책 rollout→`env.render()` 프레임 수집 + 보스격파 감지 `remaining_gyms==0`, **numpy-only**, render_mode 가드) + `EpisodeRecording` + `save_demo`(render.save_gif 위임). `scripts/killer_demo.py`(`[rl]`+`[render]`): train 64시드 학습 → **held-out 시드** 녹화(deterministic=True)→GIF + 격파 리포트. **정직성**: CI 가 검증한 건 파이프라인·감지(type-aware scripted 가 seed=3 **train 영역**에서 gym 격파 녹화)이지 *일반화 아님* — "학습 에이전트가 held-out 보스격파"는 학습품질 의존 비CI. **milestones M3-EC6 `[ ]` 유지**(world-render 선례; `[x]`=실제 held-out GIF 확인+결재). 측정 eval rollout 과 직교(프레임 vs 점수). Acceptance 8/8, 118 passed/3 skipped(113→118, 회귀 0). _(commit pending)_
- **2026-06-22** — [`world-render`](_archive/2026-Q2/env-core/13-world-render/) (standard, **M3-EC6 토대**): 킬러데모의 render 레이어 — 월드 상태→픽셀 프레임 `critter_gym.render.draw_frame`(**numpy-only**, 색칠 셀: agent·creature·active/defeated gym·배틀 틴트; 배경→creature→gym→agent 그리기 순서로 set-순서 무관 byte-identical 결정론). `CritterEnv` Gymnasium 표준 `render_mode="rgb_array"` 통합(read-only `render()`, obs/step/reset 무변; metadata render_modes/render_fps). GIF 인코딩(`save_gif`)만 `[render]` extra(imageio) 지연 import 격리. **rgb_array=새 의존성 0**. `test_compliance` 가 `skip_render_check` 제거 → check_env 가 rgb_array render 실제 검증. **EC6 를 전진(토대)만, 충족은 후속 `killer-demo`**(보스격파 에이전트+GIF, CI비검증). render 도메인 첫 vertical. Acceptance 7/7, 113 passed/2 skipped(102→113, 회귀 0). _(commit pending)_
- **2026-06-22** — [`metrics-viz`](_archive/2026-Q2/env-core/12-metrics-viz/) (standard, **M3-EC3**): 측정 viz 4종 차트(학습곡선·일반화 갭·baseline spread·시드 분포) — **연구자용 메트릭 플롯**(게임 렌더링 아님). numpy-only `critter_gym.viz`: plot-ready 데이터 정형(`spread_data`/`gap_data`/`seed_distribution_data`/`LearningCurve`)은 core CI 검증, matplotlib 드로잉은 **함수 내부 지연 import + `[viz]` optional extra** 격리(측정 모듈은 viz/matplotlib 미import → 단방향 의존, import 순수성 테스트). `Agg` headless. `Leaderboard.from_score_table` 분리로 `benchmark.py --plot` 가 ScoreTable 1회로 리더보드+4차트(per-seed 분포) 생성, `train_ppo.py` 가 실제 학습 루프에서 `LearningCurve` 누적+저장. 측정 인프라(EC1·EC2·EC3) 완성 — 남은 M3 는 데모·공개. Acceptance 8/8, 102 passed/1 skipped(92→102, 회귀 0). _(commit pending)_
- **2026-06-21** — [`leaderboard`](_archive/2026-Q2/env-core/11-leaderboard/) (standard, **M3-EC2**): 리더보드 포맷 + 재현 configs — numpy-only `critter_gym.leaderboard`(`BenchmarkSpec` 핀 고정 eval 프로토콜 + `Leaderboard.to_json`(sort_keys, spec embed)/`to_markdown` + `run_benchmark`). held-out 평균 내림차순 랭크(결정적 tie-break gap/name) = unseen 일반화 철학. **공개 점수 스키마 개명** 완료(generalization-harness L3 forward-note 이행): `train_mean`/`test_mean`→`heldin_mean`/`heldout_mean`(+`n_heldin`/`n_heldout`) — held-in/held-out *eval* 평균임을 정직히 표현, 단일 위임(GapReport.to_dict)이라 부채 0, 리터럴 grep=0. `benchmark.py` 리더보드 소비자화(재현 spec 헤더). viz·OSS·킬러데모 공개 포맷 토대. Acceptance 8/8, 92 passed/1 skipped(86→92, 회귀 0). _(commit pending)_
- **2026-06-21** — [`baseline-suite`](_archive/2026-Q2/env-core/10-baseline-suite/) (standard, **M3-EC1**): 베이스라인 4종(random/scripted/PPO/recurrent) train+test 점수표 — M2 의 갭 하네스를 N-베이스라인으로 확장하는 **numpy-only** `critter_gym.scoreboard`(`score_baselines`→`ScoreTable.to_markdown`/`to_dict`). 베이스라인마다 `measure_generalization` 1회, seeds tuple materialize 로 동일 split 공정 비교, 누수 가드는 위임 상속. 빌더는 torch/sb3 무의존(core CI 가 random+scripted 로 spread 가드 보존), PPO/recurrent(`sb3_contrib.RecurrentPPO`)는 `[rl]` extra 뒤 `benchmark.py` 소비자 + `importorskip` smoke 로 격리. `benchmark.py` dead-bug 3종 정정(`target_catches` AttributeError·`range(50_000)` train영역 오칭·`vary=False`) + sb3 미설치 graceful. 리더보드·viz·킬러데모 데이터 토대. Acceptance 9/9, 86 passed/1 skipped(81→86, 회귀 0). _(commit pending)_
- **2026-06-21** — [`generalization-harness`](_archive/2026-Q2/env-core/09-generalization-harness/) (standard, **M2-EC4 → M2 완성**): train-vs-test 일반화 갭 측정 하네스 — 정책-비의존 **numpy-only** `critter_gym.generalization`(`rollout`/`evaluate`/`split_train_pool`/`measure_generalization`/`GapReport`/`format_report`). `gap = train_mean − test_mean`(Procgen 관례), held-in 은 `split_train_pool` 로 학습 시드와 **구조적 disjoint**(낙관편향 방지), 양방향 split 누수 가드(ValueError). 측정 로직은 torch/sb3 무의존 → core CI 상시 검증, PPO 학습은 `[rl]` extra 뒤 `scripts/train_ppo.py` 소비자로 격리(+`vary=True` procgen, 기존 `target_catches` dead-kwarg TypeError·하드코딩 50_000 split 버그 정정). **마일스톤 M2(procgen + train/test moat) 완성** — 4 EC 충족, 킬러 데모·리더보드 토대. Acceptance 9/9, 81 tests green(71→81, 회귀 0), check_env(fixed+procgen). _(commit pending)_
- **2026-06-21** — `procgen-typechart` (standard, **M2-EC2/EC3**): moat 핵심 novelty — 시드별 내부정합 타입 상성표 `generate_typechart`(antisymmetric per-pair, 결정론) + `TypeChart` 데이터주도화(기본=FIXED, M1 무회귀). Region 이 시드별 차트 보관, env 가 Battle+scripted_opponent 에 전달(scripted 차트 누락 버그 수정). 차트는 obs 미노출 → **infer-the-meta**(고정표 암기 불가). held-out 시드가 새 맵+새 타입표 생성(EC3). Acceptance 8/8, 71 tests green, check_env(fixed+procgen). _(commit pending)_
- **2026-06-21** — `procgen-region` (standard, **M2-EC1**): 우리 moat 의 첫 벽돌 — 시드→절차 생성 region(creature/gym 수·위치·보스 타입) 순수 생성기 `generate_region` + train/held-out 시드 구조적 분리(`TEST_SEED_OFFSET` offset + overrun ValueError 가드 + split-safe 무시드 reset). `CritterGym-procgen-v0`(vary=True) 등록. obs 형태 불변(차원 고정·내용만 변주·경계=max). vary=False 기본으로 M1 무회귀. Acceptance 8/8, 64 tests green, check_env(fixed+procgen) 통과. 일반화 측정 토대 마련. _(commit pending)_
- **2026-06-21** — `creature-evolution` (standard, **M1-EC2 → M1 완성**): 진화 = long-horizon 투자 — 배틀 승리 시 active creature 레벨업, 임계(evolve_level=2)서 자동 진화(강화 stats), 진화=boolean subgoal 리워드(+1, RLVR). obs `evolved`/`player_level` 추가, num_gyms 기본 3 + 보스 [GRASS,GRASS,WATER]로 진화 payoff non-vestigial. **마일스톤 M1(고정월드 full subgoal chain) 완성** — 탐험→잡기→gym 배틀→보스 격파→진화가 한 에피소드에서 verifiable subgoal로 동작. Acceptance 9/9, 54 tests green, check_env 통과, 회귀 0. _(commit pending)_
- **2026-06-21** — `gym-boss-progression` (standard, **M1-EC3/EC4**): 배틀 엔진을 `CritterEnv.step`에 연결 — 월드 gym=gated checkpoint, OVERWORLD↔BATTLE 모드 머신, 보스 격파=boolean subgoal 리워드(+1, RLVR). obs 확장(in_battle+양측 active hp·type+gyms_defeated), action_space Discrete(6) 모드별 재해석. 종료 진화(catch 비종료, 전 gym 격파→terminated). 처음으로 "탐험→배틀→보스 격파"가 한 에피소드에서 동작(scripted seed3 2 gym 격파 14스텝). Acceptance 9/9, 45 tests green, check_env 통과, 회귀 0. _(commit pending)_
- **2026-06-21** — `battle-system` (standard, **M1-EC1**): 턴제 배틀 sub-MDP 엔진 — 고정 타입표(rock-paper-scissors) + 타입 상성 데미지 + 스위치 + 아이템(potion) + scripted 상대. 완전 결정론(랜덤 없음, RLVR/재현성). `types.py`/`creatures.py`/`battle.py` 독립 모듈(env 통합은 M1-EC3). Acceptance 9/9, 35 tests green. 활성 마일스톤 M1 첫 EC 충족. _(commit pending)_
- **2026-06-21** — `product-roadmap` (standard, docs-only): 제품 마일스톤 SSOT 신설 — `docs/reference/milestones.md`(M0–M5 goal·exit criteria·구성 task·DESIGN 매핑) + `docs/explanation/roadmap.md`(순서 근거·킬러 데모·master-plan↔roadmap 구분). "매 task 는 활성 마일스톤의 미충족 EC 에서 내려온다" 규율을 CLAUDE.md(auto-load)에 hook-up. 즉흥적 task 생성 차단. Acceptance 7/7, broken-link 0. _(commit pending)_
- **2026-06-21** — `env-validation` (standard): 벤치마크 성립성 검증 레이어 — Gymnasium `check_env` 통과 + 베이스라인 spread(random 0.38 ≪ greedy 2.44) + train/test held-out 일반화 + throughput 회귀 가드(실측 ~266k steps/s/core, 목표 50k의 5배). `random_policy`/`greedy_policy` 패키지 동봉, PPO 학습 데모(`[rl]` extra, trained 2.90 vs random 1.49). 무거운 deps는 core에서 분리. Acceptance 7/7, 18 tests green. _(commit pending)_
- **2026-06-21** — `scaffolding` (standard): CritterGym 제품 코드 첫 줄 — `src/critter_gym/` src-layout 패키지 + hatchling 빌드 + ruff/mypy/pytest 툴체인 + 10×10 catch-only `CritterEnv` (Gymnasium API, seeded 결정론, RLVR boolean subgoal 리워드, `gymnasium.make("CritterGym-v0")` 등록). Acceptance 8/8, 12 tests green, HARNESS-PORT-MANIFEST §(c) 커플링 #1–#4 정합 확정. _(commit pending)_

