# Changelog

All notable task-level changes are appended here by `/task-end` (one line per task).

## research (post-launch R&D)
- **2026-07-09** — [`llm-diversity-curve`](_archive/2026-Q2/llm-diversity-curve/) (standard, **LLM 다양성 곡선 — 난이도 다이얼이 불완전 추론자에게 보이는가 (돈 게이트, 사용자 승인 quota)**): scripted 밴드=calibration 불가(infer arm saturated)를 확인한 두 scout의 미증명 조각을 실제 LLM으로 구매 — `boss_pool_size`(1→8) sweep을 claude-cli(fable-5, 구독)로 측정. **게이트형 사전약정 프로토콜**(G-0 smoke→G-1 pool=1 floor<0.10 조기중단→G-2 pool=8 Δ판정→G-3 중간점은 |Δ|≥0.15시만) + 예산 하드캡(≤1,820콜) + 게이트별 JSON flush. **src 무변경**(기존 부품 조립: SealedEvalSet boss_pool_size·BattleMemoryLLMAgent·score_inference_telemetry·무료 scripted 앵커 정규화). **실측(1,200콜, 파싱실패 0/1,200)=DIAL-VISIBLE**: pool=1 **0.68**(SE 81%)→pool=8 **0.47**(SE 50%), **Δ=+0.21≥0.15**(frozen endpoint rule). 같은 sweep에서 scripted infer=0.96→0.92 평평 → **"난이도 곡선은 불완전 추론자에게만 보인다" meta-가설 실측 확증** = 판매 티어 레버(boss_pool_size) 첫 실증. **정직 경고**: G-3(pool=4)=1.00 **비단조**(전투 22 moves뿐=노이즈 가능성 大) → 클레임="양끝점 gap 입증"이지 매끈한 곡선 아님; 단일 run·4 worlds/point(scripted는 8)·1 모델·1 provider·오차막대 없음·헤드라인 금지. 부수: blind floor 붕괴(0.39→0.05)=#9 고정리드 희석 메커니즘 정합; G-1 0.68은 아레나 inconclusive와 대조(overworld+gym 설정에선 추론 신호 뚜렷). **투명 공개**: G-0이 1-world 밴드퇴화(oracle==blind) 발견→G-1 데이터 전 EPS_BAND 가드 추가(거짓 FLOOR 방지, stub 고정, 본측정 미발동); 첫 실행 인자 버그(팩토리 오용)는 지출 0에서 잡힘. 15 stub 테스트(quota 0·CI-safe), 770→**785**(회귀 0), ruff clean. L3 **2/2 APPROVE**(리뷰어가 점수 공식 재계산 검증). results.json 박제. 후속(사람 게이트): 다중 run 곡선 확정·Fable5 리더보드 정식 측정.
- **2026-07-08** — [`attrition-closure`](_archive/2026-Q2/hard-benchmark/09-attrition-closure/) (standard, **commit-mode가 순환-attrition 우회로를 닫는가**, hard-benchmark): #7(SE-only)이 attrition을 부분만 축소한 원인=non-commit party-cycling(faint→force-switch 순환→무추론 정책이 super 멤버 조우). #7 NOTE가 지목한 commit-mode 축을 침 — **엔진 변경 0**, 기존 두 knob(`commit_battles`+`super_effective_only`)+arm 조합만. 무추론 `type_blind` arm을 {commit,non-commit}×{default,strict,SE-only} 6칸으로 hard config(grid16·5gym)서 측정, `probe`=blind-luck floor. **실측(16 seed·사전약정 3조건 freeze)=정직 (b) NOT CLOSED, 미묘**: type_blind non-commit SE-only **3.75 → commit 2.19**(순환 이득 **−1.56**, default에선 5.00→2.44=**−2.56**) → **조건1 cycling-removed=True**(commit이 순환 grind 확실히 죽임). BUT committed 2.19 **>** probe floor 1.25(+0.5=1.75) → **조건2 at-floor=False**. **메커니즘**: `type_blind`가 고정 리드(creature0)로 싸우는데 그 타입이 무작위 commit보다 많은 boss(~44% vs 25%) 커버 → **confound가 2 성분: 순환 성분=닫힘·고정-리드 커버리지 성분=잔존**. oracle winnable(5.00) 유지·(c) 미발동, 추론 gap(oracle−blind) **2.81=6칸 중 최대**(commit+SE-only=eval-validity 최대 config). 완전 폐쇄엔 리드 커버리지 제거(무작위 시작 파티)=별개 축. 5 결정론 메커니즘 테스트(Battle-level: non-super=0·못이김·정답 commit 승리·non-commit 순환 승리 vs commit 순환 차단), 758→**763**(회귀 0), ruff clean·신규 src 없음. L3 **2/2 APPROVE**. 순환 폐쇄 부분성과를 "closed"로 승격 안 함·(b) 그대로(정직>헤드라인). floor≠0 규율·scripted·1-seed-set 라벨. §5(i) confound 상환 계보 #6 falsify→#7 partial→#9 순환-닫힘/커버리지-잔존.
- **2026-07-08** — [`sota-headroom`](_archive/2026-Q2/hard-benchmark/08-sota-headroom/) (standard, **더 강한 memory agent에도 hard한가 — capacity confound 재측정**, hard-benchmark): #3(recurrent PPO h128=oracle 43%, "hard ROBUST")이 recurrent net을 의도적 좁게(h128<ff h256) 만들어 남긴 capacity confound("hard인가 agent를 덜 키웠나")를 침 — 같은 `hard_env_spec`(grid16·5gym·420step)에서 recurrent PPO를 capacity+budget sweep(h128/h256/h384 × 300/600 iters)으로 키워 재측정. 판정 로직을 `headroom.classify_scaled_headroom` 순수함수(best-non-tiny 선택+`classify_headroom` 위임+non_vacuous(best>tiny)/exceeds guard, 기존 `classify_headroom` 무변경, numpy-only=CI 단위테스트)로 추출. **실측(full 3-run, 사전약정 frac=0.75/k=1.0 freeze)=정직 VACUOUS**: tiny h128 **2.12(42%, #3 43% 재현)** > wide h256 1.56(31%) > wider h384 0.83(17%) — **넓힐수록 나빠짐**(best non-tiny 1.56 < tiny 2.12 → non_vacuous=False → **verdict 보류**). **메커니즘**: 순수 width 확대가 CPU·small batch(48)·중간 budget에서 최적화 난이도(GRU hidden-replay=width²)에 눌려 underfit → **값싼 capacity 레버로 더 강한 agent 만들기 실패**. 사전약정 **non-vacuity guard가 거짓 robust 차단**(guard 작동=핵심 산출). "SOTA에도 hard"는 **답 못 함=OPEN 유지**(더 강한 agent=더 큰 batch·budget·LR·GPU 필요, 파라미터 수 아님). 부수 소득: #3 h128 독립 재현. opt-bound 2.18≪3.75=비borderline→5run 확충 불요. p-hacking 부재(grid·best선택·임계 데이터전 freeze, VACUOUS 사후 재해석 없이 그대로). 12 신규 테스트, 746→**758**(회귀 0), ruff clean·mypy 신규 clean. L3 **2/2 APPROVE**. 원하던 robust 대신 반증 그대로 보고(정직>헤드라인).
- **2026-07-08** — [`super-effective-economy`](_archive/2026-Q2/hard-benchmark/07-super-effective-economy/) (standard, **super-effective-only 배틀경제 knob + 변별밴드 scout**, hard-benchmark): 06-strict-battle scout 가 NOTE 로 지목한 후속 — opt-in `super_effective_only` knob(strict 의 **strict superset**: 중립 eff==NEUTRAL 타까지 0, super-effective 만 데미지)을 엔진 3-지점(battle.py `damage()` 클램프 `eff<=NEUTRAL`·critter_env passthrough·jax_env `_gym_damage` compile-time const `eff<=1.0`, 전부 strict_battle 선례 대칭·default off=byte-identical)에 배선. scout 가 default/strict/SE-only 3경제 scripted 변별밴드 측정. **실측(16 seed·정직 partial SIGNAL)**: (Q1) SE-only 가 spread(oracle-blind)를 **완만히** 넓힘(grid16 2.56→**2.81** +0.25, grid10 1.62→1.69 +0.06). (Q2) oracle **winnable 유지 전 config**(5/5·3/3=공정 레버 생존). attrition confound: strict **불변**(5→5·3→3) vs SE-only **부분** 축소(5→3.75·3→2.31) but **~0 미달=완전 폐쇄 아님**(non-commit party-cycling 이 운으로 SE 멤버 조우; 완전 폐쇄=commit-mode 별개 축). **정직 경계**: 단일타입 월드는 guarantee #15 로 winnable 보장, but SE-only+boss_secondary 는 super×resisted=NEUTRAL 천장→구조적 unwinnable 가능(버그 아님·Q2 falsifiable 경계, 테스트로 구체 seed 증명). scripted·1-seed·no-robust-threshold=**헤드라인 금지**, 학습/LLM arm=money-gated 후속. 28 신규 테스트(knob 계약 12+parity 16), 718→**746**(회귀 0), ruff clean·mypy 수정파일 clean(render.py:82=baseline). L3 **2/2 APPROVE**(plan-reviewer verdict-first 재호출·scope_paths parity-test 누락 SUGGEST 반영). knob 은 판매 티어 난이도 레버 후보로 `boss_pool_size`·`strict_battle` 과 병렬 잔존.
- **2026-07-08** — [`diversity-dial`](_archive/2026-Q2/diversity-dial/) (standard, **per-episode 타입 다양성이 진짜 추론 다이얼인가 — scout**): inference-difficulty-curve 후속. opt-in `boss_pool_size` knob(region.py, 기본 None=byte-identical, boss_secondary 선례) + CritterEnv/SealedEvalSet 플러밍 + `diversity_curve`(실측 distinct-types/world x축). **실측=정직 FALSIFY+메타 통찰**: 다양성 knob 작동(distinct 1.0→5.0/world) but scripted infer_score 평평(0.96→0.92). **메커니즘**: scripted infer arm=즉석-완벽 학습이라 saturated → **scripted 밴드는 eval *검증*용이지 난이도 *calibration*용 아님; 난이도 다이얼은 불완전 추론자(학습/LLM)에게만 보인다** → 돈 게이트 LLM 곡선 동기 규정. 두 scout(num_types·diversity) 모두 flat 로 정직 종결. parity 무회귀(byte-id). 5 신규 테스트, 713→**718**, ruff clean·mypy 신규 0. L3 2/2 APPROVE. knob 은 판매 티어 레버 후보로 잔존.
- **2026-07-08** — [`inference-difficulty-curve`](_archive/2026-Q2/inference-difficulty-curve/) (standard, **추론 난이도 곡선 — de-risked scout**): 추론 eval 을 '점 하나'→'눈금 계측기'로 만들 수 있나 — num_types(숨은 차트 크기) sweep 하며 scripted 4-arm 밴드의 infer-arm(첫-만남 추론 프록시) 낙하 곡선. 신규 `inference_curve.py`(additive, eval_harness 재사용). 사전선언 규칙(단조↓=다이얼/평평=falsify) 데이터 전 선언. **실측=정직 FALSIFY**: 곡선 평평(infer_score 0.91→0.92, num_types 3→12 거의 불변). **메커니즘 검증**: 재발생 pool 이 월드당 distinct 보스타입을 ~2개로 캡 → 차트 *크기*가 추론자에게 안 닿음. 정직 결론: num_types 는 다이얼 아님, 진짜 다이얼=**per-episode 타입 다양성(pool)** — 후속 scout. 명백 knob 반증+진짜 knob 지목(patch_radius·strict_battle falsify 선례 재현). 5 신규 테스트, 708→**713**, ruff clean·mypy 신규 0. L3 2/2 APPROVE. 헤드라인 금지·scripted proxy·학습/LLM 앵커=돈 게이트.

## site (public launch polish)
- **2026-07-13** — [`board-discoverability`](_archive/2026-Q2/board-discoverability/) (standard-lite, **보드 발견성 수리 — "Leaderboard" 제목 충돌 제거 + #community 점프 링크**, 소유자 실사용 발견): 소유자가 모델 행을 **두 번** 못 찾음(1번 섹션 제목이 "Leaderboard"인데 내장 baseline만 있고, 진짜 모델 표는 9/9 맨끝) = 방문자 이탈 UX 구멍. 수리 3항목(사용자 명시 승인): ① 제목→"Built-in baselines — generalization check"(ko "내장 baseline — 일반화 검증") ② baseline 표 아래 "Looking for submitted models (LLMs)? → Community leaderboard ↓" 점프 링크(en/ko) ③ Community 섹션 `id="community"`. 기존 헤딩·수치 무변경. 1 신규 테스트, 793→**794**, ruff clean. L3 2/2 APPROVE.

- **2026-07-13** — [`board-haiku-seed`](_archive/2026-Q2/board-haiku-seed/) (standard, **CLI 모델 선택 배선 + Haiku 4.5 커뮤니티 보드 첫 LLM 씨딩(구독 quota, API 0원)**, 사용자 승인): `claude_cli_complete`에 `model` 인자(None=기존 argv **byte-identical**, 계약 테스트 고정) + `community_submit --cli-model/--cli-bin` 스레딩. **fake-binary 카나리아(quota 0)**로 argparse→스레딩→채점→VALID 전 경로 검증 후 본측정. **실측(사전약정 freeze: season1·8w·BattleMemory w8·단일 run·점수 그대로)**: claude-haiku-4.5 클리어 2/1/0/0/1/1/2/1 → **mean 1.0** — 보드에 scripted 0.750 위 첫 LLM 행(en/ko). **L3가 결함 실제로 잡음**: plan-reviewer BLOCK(reproduce에 --cli-bin 누락=재현 정직성 훼손)→조건부 append 수리+카나리아 재검증+기측정 JSON 출처 정정(점수 무변경)→재승인. 2 계약 테스트, 791→**793**(회귀 0), ruff clean. L3 2/2 APPROVE(BLOCK→fix 사이클 포함). fable-5 행은 이제 개발 0으로 가능.

- **2026-07-13** — [`readme-pip-upgrade`](_archive/2026-Q2/readme-pip-upgrade/) (standard-lite, **fresh-clone smoke가 잡은 방문자 차단 요인 수리**): 마케팅 전 체크리스트의 퀵스타트 smoke 실측 — stock macOS python3.9(동봉 pip 21.2.4)에서 README 첫 명령 `pip install -e .`가 **하드 에러**(PEP 660 editable=pip≥21.3 필요, repo=pyproject-only). 수리=en/ko 설치 블록 첫 줄 `python -m pip install -U pip`+이유 주석(+4줄, 그 외 0). **유효성 실증**: 업그레이드 후 fresh clone에서 설치→퀵스타트→커뮤니티 데모(0.75)→`--validate` VALID까지 전 경로 green. 791 sanity. L3 2/2 APPROVE.

- **2026-07-13** — [`readme-ko-parity`](_archive/2026-Q2/readme-ko-parity/) (standard, **한국어 README 완전 패리티 + 정직성 drift 제거**, 사용자 요청): ko README가 영문의 절반(78줄/6섹션 vs 161줄/12섹션)이라는 지적에서 출발 — 조사 중 **정직성 drift 발견**: 낡은 ko 요약이 "GPU(T4) 수억 steps/s"를 주장하나 영문은 "GPU 목표(M4-EC3) **미측정**" 명시. 수리: ① README.ko.md **전면 재작성**(영문 12섹션 완전 번역 패리티 — What-it-measures 7불릿의 정직 캐비앗("CPU·single-run·GPU 미측정"/"baseline not SOTA sweep"/"토대이지 증명 아님"/"포켓몬은 비유") 전부 약화 없이 번역, 과대주장 제거[grep 수억/T4=0], **수치 토큰 16개 자동 대조 → 영문 존재 missing 0**) ② README.md stale 수정(Release status "공개 미수행"→"Public since July 2026", 남은 게이트 GPU·arXiv·태그/허브 보존) ③ 양쪽에 라이브 사이트+how-it-works 링크(언어 대응, 4 URL HTTP 200 확인). 한국어 웹사이트=범위 외(en/ko 키 패리티 테스트로 구조 강제 확인). 상대 링크 11개 유효, 791 green sanity(docs-only). L3 **2/2 APPROVE**(리뷰어가 캐비앗 행 단위 대응 확인).

- **2026-07-10** — [`site-how-it-works`](_archive/2026-Q2/site-how-it-works/) (standard, **"시험지 작동 원리" 딥다이브 페이지 — 방문자의 진짜 질문에 답하기**, 사용자 요청·구조 선택): 사용자가 GIF를 보며 던진 질문들(승리 조건·카운터 복잡도·왜 잡기가 전투력에 안 붙나·"룰이 제각각 아니냐")=방문자의 질문이라는 관찰 → face-validity 구멍 수리. **랜딩+딥다이브 1페이지 구조**(전면 멀티페이지 대신 — en/ko 유지비·빌더 리팩터 최소): 신규 `how-it-works(.ko).html`에 ① 승리 조건(파티 전멸 전 보스 HP 0·결정론·상성 **×2/×1/×0.5=4× 스윙**이 사실상 승패) ② 숨은 상성표(세계마다 재추첨=암기 불가, 매치업 보장은 "기본 시험지 기준" 한정어로 정밀 서술) ③ 카운터 복잡도(탐색=스타터 3·실험 1=정답 1칸으로 작게 설계, 난이도는 실험 비용구조 표: 피격·걸음 예산·장부 크기·commit·숨은 2차·strict) ④ **안티-grinding 룰 표**(야생파밍 없음/잡기≠전투력/레벨=승리만/재도전 결정론 → 각각 막는 우회로, "수학 시험장 계산기 금지" 비유) ⑤ 재는것/안재는것. 랜딩 3추가(scope 박스+링크·범례 잡기 명확화 1줄·캡션 링크) — **기존 줄 변경 0(순수 추가)**. **측정 주장 0**(DIAL-VISIBLE/0.68 등 부재를 테스트가 assert — 측정 게재=사람 게이트) + **상수 code-sync**(배수를 `critter_gym.types`에서 빌드 시점 유도, drift 시 테스트가 잡음). en/ko 키 패리티(기존 테스트 자동 커버)+상호 토글. L3: qa APPROVE + plan-reviewer 예산소진 SUGGEST(코드 대조 권고)→메인이 직접 대조 수행(상수·파티 3/1·승리조건·보장·CATCH 무추가·레벨업 전부 일치)+보장 문장 한정어 정밀화. 6 신규 테스트, 785→**791**(회귀 0), ruff clean, src 무변경.
- **2026-07-09** — [`demo-gif-purposeful`](_archive/2026-Q2/demo-gif-purposeful/) (standard, **홈페이지 데모 GIF 목적성 수리 — gym-seeking demo_policy**, 사용자 직접 발견): gameplay.gif 에이전트가 체육관이 시야에 있는데도 최단경로로 안 가고 잔디깎이 훑기만 하던 문제 — 원인=GIF 생성 정책 `greedy_policy`가 patch==1(생물)만 추적·체육관(2) 무시. 수리=`baselines.demo_policy` 신규(additive, 우선순위 battle 공격>내칸 CATCH>**살아있는 체육관 최단 step**>생물>sweep; 깬 체육관은 env가 패치에서 숨겨 갇힘 없음) + `build_site.py` GIF 생성 경로만 교체. **baseline 숫자 보호**: greedy/random **byte-identical**(diff hunk 1개=순수 append 직접 증거)·`_free_policies`(랭킹·차트 수치 경로) 무변경·재빌드 html **숫자줄 변경 0**(캡션 4줄만). **정직 캡션**: en/ko에 "데모 전용 정책, 랭킹 scripted 행과 다름" 명시(오인 방지)+패리티. **궤적 개선**(동일 seed 6): old 51/120/120/120/67/78 step·3/6 클리어 → **new 29/19/26/14/5/60 step·6/6 클리어**(체육관 직행). 7 신규 테스트(계약 고정), 763→**770**(회귀 0), ruff clean. L3 **2/2 APPROVE**(plan-reviewer stall→verdict-first 재호출). 새 GIF 사용자 확인 완료.
- **2026-07-08** — [`site-submit-howto`](_archive/2026-Q2/site-submit-howto/) (standard, **커뮤니티 섹션에 '리더보드 올리는 법' 단계별 + 가이드 링크**): 방문자가 사이트만 보고 제출 흐름을 이해하도록 — 커뮤니티 섹션에 **4스텝 how-to 카드**(시험지 받기 `season_seeds`→모델 실행 `--demo/--llm`→JSON+`--validate`→PR, 번호 카운터+코드 스니펫) + **가이드 링크**(en→submit-your-model.md/ko→.ko.md GitHub blob + community/submissions tree, 라이브 200 확인). en/ko 패리티 유지, 수치·정직 라벨 무변경. 2 신규 테스트, 706→**708**, ruff clean. 브라우저 en·ko 실측+링크 클릭. L3 2/2 APPROVE.
- **2026-07-08** — [`site-redesign`](_archive/2026-Q2/site-redesign/) (standard, **공개 사이트 디자인 시스템 개편 + en/ko 싱크 + GIF 무한반복**): 공개된 런치 사이트 3개선 — (1) **디자인 시스템**: CSS 토큰(--surface/--ink/--series)+라이트/다크 테마(prefers-color-scheme+수동 토글 vanilla JS)+컴포넌트(카드·uppercase 테이블·moat 카드·pill). (2) **인라인 SVG 차트**: matplotlib PNG(gap/band) 삭제→인라인 SVG 2종(gap 2색 그룹막대·band 단일 accent), 값=leaderboard.entries·inference_baseline **실측**(하드코딩 0), 테마 반응·hover, dataviz 팔레트 validator 통과. (3) **en/ko 싱크**: 키 집합 동일(테스트 강제)+title 언어별화(ko 정보량=en). (4) **GIF 무한반복**: save_gif(loop=0). **브라우저 4조합(en·ko×라이트·다크) 실측**(라벨 클리핑 1건 수정). 결정론·수치 SSOT 유지. 7 신규 테스트, 699→**706**, ruff clean. L3 2/2 APPROVE.

## monetization-surface (M5 — 수익화 표면 prototype)
- **2026-07-02** — [`community-leaderboard`](_archive/2026-Q2/monetization-surface/10-community-leaderboard/) (standard, **커뮤니티 리더보드 — 시즌제 공개 시험지 + 자가-신고 경쟁 #10**, M5-EC3 기술 artifact·사용자 지시 기능): 2-트랙의 공개 트랙 — 누구나 **시즌제 공개 seed 블록**(`community.season_seeds`=1.0M+s×1000, 공개 유도·결정론·비밀 0; sealed(1.1M+)·default 블록·시즌 간 분리를 테스트로 고정, `heldout_seeds(1000)` 경계 포함)에서 자기 모델을 로컬 실행(비용=제출자)하고 JSON 제출(장차 PR)→사이트 시즌별 랭킹. **정직성 구조화**: 스키마가 `self_reported: true` **강제**(공개 트랙은 검증된 척 불가) + 사이트 상시 라벨(self-reported·honor-system·**sealed 퍼널**·"submissions open when announced") + spec-일치 강제(다른 config 점수 거부) + **지표 비혼동 명시**(커뮤니티=순수 gym-clears — 메인 보드 return 지표와 직접 비교 불가). `validate_submission`(bool→int/float 오염 차단 포함, 장차 CI 게이트) + `load_submissions`(랭킹·불합격 사유 보고) + `community_submit.py`(--validate exit 0/1 / --demo **실측**) + **organizer 예시 1건 실측 커밋**(scripted baseline, season 1, 0.750) + 가이드 en/ko(5분 흐름·honor-system 규칙·시즌 개념). **시즌=moat 가 재미가 되는 지점**(절차생성=시험지 무한 재발급→순위 리셋 재미+암기 가치 제한). 20 신규 테스트, 630→**650**(회귀 0), ruff/mypy clean. 사이트 섹션 렌더 확인(브라우저 탭 고착→ko HTML 구조검사 대체, 정직 기록). L3 **2/2 APPROVE**. **사람 게이트**: 접수 공지·Pages 공개·시즌 개시·Hub 등록=미포함(park 결정). Acceptance AC1–AC6.
- **2026-07-02** — [`hard-note-precision`](_archive/2026-Q2/monetization-surface/09-hard-note-precision/) (standard, **hard 티어 difficulty_note 정밀화 #9**): #8 [SSOT 관찰] 후속 — note 의 "SOTA/recurrent not yet established(OPEN)"가 **과소 서술**이 됨(이후 hard-benchmark #3/#5 가 관련 grid16 config[5gym·420step]에서 recurrent PPO 를 robust 측정: oracle 의 ~32–43%, 천장 미달). note 를 3단 구분으로 정밀화: **ff ~11–16% 유지 + "related, deeper config 에서 rec ~32–43% 측정" + "이 정확한 티어 config 의 recurrent·SOTA-class = OPEN, SOTA-hard 주장 금지"** — 과대(여기서 측정된 척)·과소(아무것도 모르는 척) 양쪽 차단. 텍스트-only(knob 무변경), SSOT 파급 전부 갱신(env-tiers.md 2곳·list_env_tiers.py·site 재빌드[note 문자열만 치환]). **추가 stale 수리(scope 내)**: 데모 script 의 "sealed 가 patch_radius/num_gyms 드롭" — #7 에서 이미 고쳐진 낡은 서술 → "carries ... drops only num_creatures" 정정(+build_sealed 출력에 두 레버). 정직성 테스트에 "recurrent"/"related" 토큰 추가. 630 passed(회귀 0), ruff/mypy clean. L3 **2/2 APPROVE**(수치·stale 정정을 코드 대조 확인). 교훈: SSOT 원문 렌더는 코드의 낡은 문장이 곧 판매 페이지의 낡은 문장 — 측정 갱신 시 note 동반 갱신 규율. Acceptance AC1–AC4.
- **2026-07-02** — [`site-tier-section`](_archive/2026-Q2/monetization-surface/08-site-tier-section/) (standard, **사이트 난이도-티어 섹션 — env_tier SSOT 연결 #8**, M5-EC2/EC3 연결): 이번 세션에 만든 판매-표면(티어 API #5·마켓플레이스 #6)이 런치 사이트에 안 보임 → "Difficulty tiers" 섹션 추가. **SSOT 규율**(#3 팔레트 패턴): 티어 knob·`harder_knobs`·`difficulty_note`(측정 사실+"SOTA/recurrent OPEN" caveat 포함)를 `critter_gym.env_tier` 에서 **원문 그대로 렌더**(전 값 html.escape) — **사이트가 코드보다 세게 주장 불가, 수치 하드코딩 0**. **L1 BLOCK 반영(리뷰 가치 실증)**: 초안의 레지스트리 전역 순회는 같은 pytest 프로세스에서 테스트가 등록한 custom 티어가 판매 페이지에 새어드는 실제 누출 경로 → `_SITE_TIERS=("standard","hard")` **고정 리스트**만 렌더 + `register_tier` 실제 등록 후 미포함 **실증 테스트**. **L3 SUGGEST 반영**: buyer-flow 문장에 "**프로토타입**(시연 artifact, 운영 서비스 아님)" 한정어 인라인(양 언어). 정직 캡션(custom=검증만·실판매/가격/hosting=사람) 양 언어. `--no-assets` 재빌드 — site 2 html **+36줄 순수 추가**(기존 byte-identical=결정론, 자산 무변경). **브라우저 시각 확인**(en text+ko 스크린샷). 4 신규 테스트, 626→**630**(회귀 0, 기존 site 12 불변), ruff clean. L3 **2/2 APPROVE**. 공개 배포=사람 게이트 유지. Acceptance AC1–AC6.
- **2026-07-01** — [`sealed-difficulty-levers`](_archive/2026-Q2/monetization-surface/07-sealed-difficulty-levers/) (standard, **정직성 갭 수리 — sealed 이 난이도 레버를 담게 #7**, M5-EC1/EC2 정직성 경화): #5 리뷰가 우리 스스로 문서화한 부채 상환 — `SealedEvalSet` 이 `patch_radius`/`num_gyms` 를 못 담아 `build_sealed` 가 드롭 → 이 레버를 튜닝한 티어의 sealed 변형이 덜 어려울 수 있었음. 3 코어 moat 파일 **수정**(additive 아님): (1) `eval_harness.py` — `SealedEvalSet` 에 `patch_radius`(기본2)/`num_gyms`(기본3) param + `env_factory` 전달(기본값=CritterEnv 기본값 → **byte-identical 하위호환**). (2) `eval_package.py` — `seed_commitment` material 에 두 레버(**swap/rug-pull 가드 확장**) + `EvalManifest`/`build_manifest` 공개 필드로 노출(**구매자가 난이도 레버를 투명 확인**, 비밀 seed 미노출 유지; L1 SUGGEST). (3) `env_tier.py` — `_SEALED_KNOBS` 에 두 레버, `_SEALED_DROPPED`=`(num_creatures,)` 만, docstring 갱신. **정직**: 내장 `hard`(patch=2/gyms=3=기본값)는 **이미 충실**했음 — 이 fix 의 실질 효과는 **custom 티어**가 레버를 튜닝할 때이며 "이제 hard 가 더 어렵다"는 **과대표현 금지**. 3 reference 문서 갱신("faithful difficulty", "덜 어려움" 경고 제거). golden 커밋먼트 하드코딩 없음 → 커밋먼트 값 변화 무영향. 6 신규/갱신 테스트, 592→**598**(+6; 0 실패), ruff clean·mypy Success, 3 데모 무오류. L3 **2/2 APPROVE**(qa 초기 false BLOCK[문서 완료 미인지]→정정 APPROVE; plan-reviewer test docstring stale SUGGEST 반영). evergreen `docs/reference/{env-tiers,sealed-eval-packaging,tier-eval-bundle}.md`. Acceptance AC1–AC6.
- **2026-07-01** — [`tier-eval-bundle`](_archive/2026-Q2/monetization-surface/06-tier-eval-bundle/) (standard, **판매-표면 통합 캡스톤 — 티어 eval 번들 #6**, M5-EC1+EC2 결합): #4(서명 오염-불가 인증서)와 #5(난이도 티어)가 main 에 조각으로만 존재 → 구매자가 겪는 end-to-end 부재. `src/critter_gym/eval_marketplace.py`(신규·**additive·조합-전용**, 두 모듈 공개 API만 소비·재구현 0)로 buyer 흐름 완성: **티어 선택 → sealed 매니페스트 → 제출 → 서명 티어-인증서 → 검증**. (1) `TierOffer`(티어 difficulty 메타+서명·비밀 미노출 매니페스트) + `build_tier_offer`/`publish_catalog`(티어별 distinct 커밋먼트). (2) `SellerListing`(단일 진실원 — `(tier,seed,key,key_id,overrides)`를 한 번 묶어 `offer()`↔`issue_certificate()` 재입력 불일치 구조적 방지; L1 SUGGEST 반영). (3) `issue_tier_certificate`(오염→`ok=False` 미채점, `eval_package` 위임) + `verify_offer_certificate`(커밋먼트 바인딩). (4) `bundle_honesty`(두 층 note 노출). **핵심 계약**(구현 중 테스트가 드러냄): 바인딩=sealed 커밋먼트=**resolved knob+seed**, 티어 label 아님 → same-tier+diff-seed 실패·diff-tier 실패로 **seed confound 격리 검증**(L1 SUGGEST). **정직**: 패키징 HONEST_SCOPE(HMAC prototype)+티어 difficulty_note(실측 한정·SOTA/recurrent OPEN·sealed 드롭 레버) 두 층 유실 0. 데모 `scripts/tier_eval_bundle_demo.py`(catalog→선택→제출→인증서→검증+바인딩실패·오염). stdlib-only 의존성 0. 14 신규 테스트, 578→**592**(+14; 0 실패), ruff clean·mypy Success. L3 **2/2 APPROVE**(plan-reviewer `verify_offer_certificate` docstring "same tier"→"same resolved knobs" 정밀화 SUGGEST 반영; qa APPROVE). evergreen `docs/reference/tier-eval-bundle.md`. Acceptance AC1–AC8.
- **2026-07-01** — [`custom-env-api`](_archive/2026-Q2/monetization-surface/05-custom-env-api/) (standard, **커스텀/고난도 env 티어 API #5**, M5-EC2): difficulty-scaling/hard-benchmark 이 *실측한* 난이도 레버(grid·호라이즌·boss·타입)가 `CritterEnv.__init__` 에 산재 + 지식은 이니셔티브 문서에만 → 구매자가 재현 가능하게 고를 curated tier API 부재. `src/critter_gym/env_tier.py`(신규, **additive**·기존 무수정)로 채움: `TierSpec`(직렬화+difficulty descriptor) + `validate_tier_spec`(가드: 비정상·`num_types<2`·`num_gyms>cells`·`max_steps<2*grid` 거부) + curated 프리셋 **`standard`**(기본치) / **`hard`**(실측 hard: grid16+긴 호라이즌) 레지스트리(env_family 관례 idempotent) + `make_tier_env`/`tier_env_factory`(override 재검증·우회 없음) + sealed tie-in `sealed_config`/`build_sealed`(SealedEvalSet 지원 서브셋만; `num_gyms`/`patch_radius`/`num_creatures` **드롭**·문서화). **정직**: `hard` `difficulty_note`=실측(feedforward PPO ~11–16% of oracle, oracle winnable) + **"SOTA/recurrent=OPEN(미측정)"** 명시(측정 없는 난이도 주장 금지); sealed 변형이 드롭 레버로 덜 어려울 수 있음 명시; 가드=정적 sanity≠증명 명시. eval_package **미import**(cross-branch 회피). 데모 `scripts/list_env_tiers.py`(목록·메타·커스텀 등록 통과+거부·sealed tie-in, exit 0). stdlib-only 의존성 0. 22 신규 테스트, 539→**561**(+22; 0 실패), ruff clean·mypy Success. L3 **2/2 APPROVE**(plan-reviewer 가 `build_sealed` override 가드-우회 **BLOCK**→분리·재검증 수정→재리뷰 APPROVE; qa-verifier APPROVE). evergreen 흡수 `docs/reference/env-tiers.md`. Acceptance AC1–AC9.
- **2026-07-01** — [`private-evalset-package`](_archive/2026-Q2/monetization-surface/04-private-evalset-package/) (standard, **비공개 held-out eval 세트 판매 패키지 #4**, M5-EC1): eval-product 가 지은 sealed-eval *기능 토대*(`SealedEvalSet`/`verify_sealed`/`score_agent`)는 in-process 뿐 — 구매자에게 건네거나 결과 위조를 막을 방법이 0 이었다. 그 빈틈을 `src/critter_gym/eval_package.py`(신규, **additive**·기존 파일 무수정)로 채움: (1) **서명 코어** — `sign_payload`/`verify_signature` 가 canonical(sorted-key) 직렬화 SSOT 위 HMAC-SHA256 + `hmac.compare_digest`(상수시간). (2) **seed 커밋먼트** — `seed_commitment` 가 정렬 secret seeds+world config 를 sha256 one-way 로 묶어 사후 eval 교체(rug-pull) 탐지, seed 미노출. (3) **서명된 buyer 매니페스트** — `EvalManifest`(비밀 seed/offset 미포함, 공개 config+커밋먼트+`HONEST_SCOPE`) + `build_manifest`/`verify_manifest`/`manifest_self_check`(구매자 무-비밀 사전 오염 자가점검). *L1 SUGGEST 반영: 매니페스트 자체도 서명 → 변조 직접 검출.* (4) **서명된 오염-불가 인증서** — `SignedCertificate`+`issue_certificate`(오염 제출은 `ok=False` **미채점**, 부정 인증서도 서명 유효)/`verify_certificate`(서명+매니페스트 커밋먼트 바인딩). **정직**: HMAC=shared-secret **prototype**(실제=비대칭 서명+서버측 비밀 커스터디=사람 게이트) 를 docstring·`HONEST_SCOPE`·데모 3중 명시. E2E 데모 `scripts/package_sealed_eval.py`(seller→buyer→verify, 정상+오염 2케이스, exit 0). stdlib-only 신규 의존성 0. 17 신규 테스트, 539→**556**(+17; 0 실패), ruff clean·mypy Success. L3 **2/2 APPROVE**(verdict-first 로 plan-reviewer stall 우회; retro 큐 `plan-reviewer-verdict-first` 적재). evergreen 흡수 `docs/reference/sealed-eval-packaging.md`. Acceptance AC1–AC7.
- **2026-07-01** — [`site-research-visuals`](_archive/2026-Q2/monetization-surface/03-site-research-visuals/) (standard, **사이트 연구-설명 시각화 — 격자 범례·SE-rate band·held-out 썸네일 #3**): 사이트가 게임플레이 GIF 한 장뿐이라 정작 *연구하는 것*이 안 보이고 격자 색 설명도 0 → 3가지 추가(양 언어). (1) **격자 색 범례** — `render.py` 팔레트 상수(`_AGENT`/`_CREATURE`/`_GYM_ACTIVE`/`_GYM_DEFEATED`/`_BG`)를 직접 import(**SSOT**, 하드코딩 0) → `rgb()` 스와치 + en/ko 라벨 5종. (2) **SE-rate 추론 band 차트**(moat KPI) — `_build_band_png()` 가 `inference_baseline`(demonstrator sealed)로 각 scripted arm 의 super-effective-move 율을 matplotlib 막대(oracle 100%/infer 90%/type_blind 27%/probe 0%)→`site/band.png`. **프런티어 LLM 수치는 차트에 하드코딩 0** — 캡션 텍스트로만("별도 유료 probe·near chart-blind floor·inconclusive·차트에 미표시"). (3) **held-out 세계 썸네일 3장** — `_build_world_thumbnails()` 가 서로 다른 `heldout_seeds(3)` reset 프레임을 `env.render`→imageio 로 `world_1..3.png`("매번 다른 세계=못 외움" 시각화). 모든 자산 생성 lazy+guard(부재 시 skip·커밋 자산 유지). render_site 순수·전 값 `html.escape`·결정론(타임스탬프 없음). **브라우저 시각 확인**(en: 범례 색 정합·band 차트·썸네일·정직 캡션). 4 신규 테스트(palette-match·korean·band+썸네일 참조·"14%" 미포함) + 기존 8 불변. 535→**539**(+4; 0 실패), ruff clean, 기존 스크립트·채점 무변경. L3 **2/2 APPROVE**. **#2와 동일 브랜치/PR**(사용자 요청). Acceptance AC1–AC4.
- **2026-07-01** — [`leaderboard-site-polish`](_archive/2026-Q2/monetization-surface/02-leaderboard-site-polish/) (standard, **리더보드 사이트 폴리시 — 게임플레이·학습 시각화·CSS·한국어 #2**): #1 정적 사이트를 매력적으로 확장. (1) **게임플레이 애니메이션** — `build_assets()` 가 held-out seed 를 순회해 scripted greedy 에이전트가 **실제로 보스를 격파하는** 세계를 골라 `record_episode`/`save_demo` 로 `gameplay.gif`(128×128·42프레임) 녹화(그래야 "defeats the boss" 캡션이 참; 미격파면 정직 fallback). (2) **일반화 격차 플롯** — `viz.plot_generalization_gap` → `gap.png`. `score_baselines` **1회** 로 리더보드+플롯 공용. (3) **순수 CSS 애니메이션** — `@keyframes`(그라데이션 히어로·페이드인·행 hover), 프레임워크 0. (4) **한국어 버전** — `render_site(lang)` + `_COPY` en/ko dict → `index.html`+`index.ko.html` + 언어 토글. imageio/matplotlib **lazy+guard**(부재 시 skip, 커밋 자산 재사용). **정직**(양 언어): GIF=scripted baseline(학습/LLM 아님)·prototype·in-process·수치 출처·**공개 배포=사람 게이트**. 전 값 `html.escape`, 결정론(타임스탬프 없음). **브라우저 시각 확인**(en+ko). 4→8 테스트, 529→**535**(+4 신규, +2 imageio un-skip; 0 실패), ruff clean, 기존 스크립트·채점 무변경. 옛 `killer_demo.gif`(미참조) 제거. L3 **2/2 APPROVE**(비차단 관찰: `--no-assets` 경로 demo_cleared 가정). main 단독 PR. Acceptance AC1–AC4.
- **2026-07-01** — [`leaderboard-site`](_archive/2026-Q2/monetization-surface/01-leaderboard-site/) (standard, **정적 리더보드 웹사이트 — M5 런치 자산 prototype #1**): 새 initiative(monetization-surface) 의 첫 판매-표면 제품. `leaderboard.py` 결과를 **프레임워크 없는 단일 정적 HTML** 로 찍어내는 `scripts/build_site.py` 신설 — `render_site(leaderboard, *, generated_note) -> str`(순수·testable, `Leaderboard.to_dict`/`spec.to_dict` 재사용, 전 값 `html.escape`) 가 랭크 표 + 킬러데모 GIF + moat 설명(held-out seed split·오염 가드·RLVR·un-gameable 추론) + repo 링크 + 정직 캡션을 조립. main() 이 무료 baseline(random/scripted, `run_benchmark`) 실측 랭크(또는 `--from-json`) → `site/index.html`(3.8KB, 결정론: seed=0·note 타임스탬프 없음) + `killer_demo.gif` 복사 + 로컬 프리뷰 안내. **npm·프레임워크·네트워크·빌드스텝 0**(stdlib html/json). PPO/recurrent 는 `[rl]` 없으면 제외(문구 명시). **브라우저 로컬 프리뷰 시각 확인**(표·GIF·moat·"Honest scope" 렌더). **정직 게이트**: 빌드+로컬 프리뷰=자율, **공개 배포(GitHub Pages 공개 토글)=사람 게이트**(페이지·docstring 명시: prototype·in-process sealing·수치 출처). 4 신규 테스트(entry·결정론·moat/정직 문구·escape). 525→**529**(+4, 회귀 0), ruff clean, 기존 스크립트·채점 무변경. L3 plan-reviewer(라인 단위)+qa-verifier **2/2 APPROVE**. main 단독 PR. Acceptance AC1–AC4.

## release-prep (M3-EC5 OSS launch)
- **2026-07-06** — [`paper-arena-update`](_archive/2026-Q2/release-prep/03-paper-arena-update/) (standard, **tech report 확정 + 인용 + 신고 정책**, M3-EC4 완료·Phase A-3): 논문을 **in-repo technical report 로 확정**(arXiv=사용자 결정 무기한 보류, milestones EC4 재정의[x]). §5 에 arena 실측 반영 — Fable 5 5-run 0.132±0.037 종결적 INCONCLUSIVE·engagement 가설 기각·**모델 세대 caveat**(opus-4-8↔fable-5 직접 비교 금지) + 한계(i) strict falsify. §7/§9 stale "GPU 미측정"→M4-EC3 실측(950M/T4/95×, 경계 라벨). Acknowledgments+AI 공개 문구. `CITATION.cff`(GitHub 표준 인용 — arXiv 대체). README en/ko "Reporting problems"(제보 환영·정정=CHANGELOG·AI 투명성 — soft-launch 방침과 세트). 제품 코드 0, 699/0. L3 2/2 APPROVE(수치를 battle-arena.md 와 대조 확인). **Phase A 완료 — 남은 것=사람 게이트(Public+Pages)**.
- **2026-07-06** — [`peer-facts-verify`](_archive/2026-Q2/release-prep/02-peer-facts-verify/) (standard, **공개 전 준비 ② — 피어 사실 검증 + 약관 확인 + Pages 준비**, M3-EC5 Phase A-2): competitive-analysis 가 스스로 명령한 "[verify] 는 1차 자료 확인 전 공개 금지" 이행 — 4피어(Procgen/Craftax/XLand-MiniGrid/NLE)를 공식 repo·논문으로 4병렬 검증, [verify] **전수 종결(0건)**, §4=검증 기록. **정직 하향 3건**(XLand "one notch above" 폐기 — 그쪽도 규칙 은닉·발견 요구, "축이 다름"으로 재서술 / Craftax→long-horizon / NLE→fast-CPU) + 완화 2건(양 대회의 일회성 private test 인정). **핵심 확정**: 4피어 모두 regenerable sealed-eval 제품 없음 = 우리 최정직 차별화 축(출처 명시). **약관**: Claude 수치 공개=제한 없음(4문서 전수, 2026-07-06) / 구독 배치 측정=GRAY(운영 방침: 소규모+헤드라인은 API 선호). `pages.yml`(공식 deploy-pages, 사람 토글 전 no-op). molt.church 정찰=홍보 부적합 판정(종교 사이트+Moltbook 보안사고·astroturf·공급망 위험). paper §8 XLand 불릿 동일 정정. 제품 코드 0 파일, 699/0. L3 2/2 APPROVE. 다음: A-3 → (사람) Public+Pages.
- **2026-07-06** — `harness-privatize` (repo-meta, 사용자 지시): 내부 프로세스 툴링(.claude/, CLAUDE.md, HARNESS-PORT-MANIFEST.md, docs/harness/)을 git 이력 전체에서 제거(git-filter-repo, 220→218 커밋, 잔여 diff=하네스 72파일 삭제뿐임을 검증) + .gitignore 편입. 공개 repo 는 제품(코드·테스트·문서·논문·감사추적)만 담는다 — task 서사(archive/CHANGELOG)는 유지.
- **2026-07-06** — [`release-readiness`](_archive/2026-Q2/release-prep/01-release-readiness/) (standard, **공개 전 준비 ① — CI 신설 + 이력 정리**, M3-EC5 Phase A-1): 사용자가 OSS 공개 게이트 개방. **비밀 스캔 통과**(이력 216커밋 전수: 키 패턴 0·비밀 파일 0·데모 prototype 상수만; 저자 이메일 2건 공개=사용자 인지). `.github/workflows/ci.yml` 신설 — core job(py3.9+3.12 matrix, numpy-only 설계 그대로: ruff·mypy·pytest[jax importorskip auto-skip]·**커뮤니티 제출 --validate 게이트**[Phase D '접수 CI 연결' 선행물]) + jax-parity job(py3.9 floor 에서 parity 0 증명). CHANGELOG stale `(commit pending)` 마커 88건 제거(`git show|sed|diff`=0 로 **marker-only 증명**). README CI 뱃지+quickstart 실측. 제품 코드 **0 파일**, 699/0, 로컬 CI-동일 시퀀스 green(mypy 는 CI 에서 imageio 미설치→Any 처리로 green 논증, 첫 실런 로그로 재확인 예정). L1 qa BLOCK(baseline 정의)→보완, L3 **2/2 APPROVE**. 다음: A-2 peer-facts-verify → A-3 paper-arena-update → (사람) Public+Pages. Acceptance AC1–AC4.
- **2026-06-30** — `paper-eval-product-section` (quick-fix, **arXiv 초안 §5 신설 — M3-EC4**): 논문 초안(`docs/paper/critter-gym.md`)에 **moat(오염방지 sealed eval + 프런티어-LLM inference 측정)** 가 통째로 빠져 있던 것을 보강 — 신규 **§5 "The eval as a product: a contamination-proof sealed held-out, and a frontier-LLM probe"**: `SealedEvalSet`(secret master_seed→비공개 held-out 블록·재생성)·`verify_sealed`(오염 가드=검증가능 non-contamination)·`inference_score` KPI·`classify_inference`(사전약정 분류기) + **이번 세션의 정직한 사례연구**(render 버그→engagement floor 풀림 / battle-memory→데미지 신호 / 둘 다 제거 후에도 inference floor robust=하네스 artifact 아닌 능력 신호) + 정직 경계(신호이지 verdict 아님·oracle 100%=hard-not-impossible·gym-clear 변별 band가 attrition으로 좁음→SE-무브율이 attrition-proof 판별자·in-process sealing). Abstract에 기여 (iv) 추가, §5~9→§6~10 renumber + 교차참조 3곳 수정, paper/README status 갱신. **검증**: qa-verifier가 SealedEvalSet·verify_sealed·inference_score·classify_inference·render버그·정직경계 전부 코드 대비 APPROVE(과대주장 0). docs-only, 0 code change. Advances M3-EC4(arXiv 초안).
- **2026-06-29** — `onboarding-guides` (quick-fix): OSS-readiness audit (researcher persona, evidence-based) found substance ready but onboarding docs thin → added bilingual (EN + `.ko.md`) guides so a first-time RL researcher can act without reading the 41KB DESIGN.md. New: README "What can you do (guides)" nav + `README.ko.md`; `docs/how-to/{train-a-policy, evaluate-an-llm-agent, measure-genre-transfer}.md` (+ko); `docs/reference/observation-action-space.md` (+ko, obs/action/reward + env-variant tables). All commands/flags/kwargs verified against code; L3 qa-verifier caught+fixed a wrong `local_patch` tile-code claim (env emits `0`=empty/`1`=creature/`2`=gym, `Box(0,2)` — not the 4-code/wall claim). Honest scope preserved (LLM eval = in-process prototype, cost/scope labeled; no overclaim). docs-only, 0 code change. Advances M3-EC5.

## eval-product
- **2026-07-06** — [`cli-complete-retry`](_archive/2026-Q2/eval-product/25-cli-complete-retry/) (standard, **LLM 런 강건화 — timeout 재시도 + 월드별 진행 로그 #25**): 실사고 수리 — Fable 5 커뮤니티 실측(8월드 ~1600호출)이 **CLI 1건의 120s stall 로 전체 사망**(TimeoutExpired 전파, 부분 진행·쿼터 소실). `claude_cli_complete(retries=2)` — timeout 시에만 동일 prompt 를 fresh subprocess 로 재시도(총 최대 3회), 소진 시 raise; **침묵 폴백 0**(실패를 기본 액션으로 대체 금지 — CLI print 모드=무상태 독립 프로세스라 재시도=동일 질문 재요청, 측정 비편향). + `score_submission_on_season(on_world=None)` additive 콜백과 `--llm` 월드별 `[k/n] seed clears` flush 출력(장기 런 진행·사망 지점 가시화). 4 신규 테스트(재시도 성공/소진 호출수 정확 3·콜백 인자·형식 regex), 695→**699**(회귀 0), ruff clean·mypy 신규 0. L1 qa BLOCK(AC 구체성)→보완, L3 **2/2 APPROVE**. 후속: 재측정=쿼터 사용자 승인.
- **2026-07-03** — [`community-llm-entry`](_archive/2026-Q2/eval-product/24-community-llm-entry/) (standard, **LLM 커뮤니티 리더보드 엔트리 wiring #24**, M5-EC3): 커뮤니티 트랙에 첫 LLM 엔트리 경로 — `community.score_submission_on_season`(additive)로 채점을 **단일 SSOT 루프**로 승격(--demo 도 같은 함수 호출, 0.75 재현 테스트로 수치 고정; BenchmarkSpec env·season_seeds·순수 mean gym-clears; 월드별 `reset()` 기억 격리=sealed 규율) + `build_submission`(validate_submission 자체검증 — out-of-schema 산출 구조적 불가) + `community_submit.py --llm`(claude-cli/anthropic, memory 3종, **quota 경고**[최대 n_worlds×200 호출] 출력, --demo/--validate 무변경). fake complete end-to-end(실호출 0)로 검증 — **실측·제출 JSON 커밋(=보드 등재)=사람 게이트** 3중 명시(출력·docstring·how-to en/ko). battle-arena.md "arena≠leaderboard" 라벨의 정규 경로 완성. 5 신규 테스트, 690→**695**(회귀 0), ruff clean·mypy 신규 0. L3 **2/2 APPROVE**. Acceptance AC1–AC5.
- **2026-07-02** — [`arena-fable5-measured`](_archive/2026-Q2/eval-product/23-arena-fable5-measured/) (quick-fix): #22 아레나 도구로 **Claude Fable 5 실측**(claude-cli, modelUsage 로 id 확인; 사용자 승인 quota, 3+2 runs 임계불변 확충) — arena SE-rate 48% vs 앵커 40%/oracle 100%, 5-run **0.132±0.037 → INCONCLUSIVE(종결적**: mean>floor_eps 라 추가 run 무의미). 사전선언 해석: **engagement 가설 기각**(전투 보장에도 +8pp — 오버월드 바닥=추론 결핍), 단 "완전 floor" 단정도 과장(약한 일관 above-chance 신호). `battle-arena.md` Measured 섹션+모순 제거, docs-only 1 file. L1·L3 qa-verifier APPROVE.
- **2026-07-02** — [`battle-arena-probe`](_archive/2026-Q2/eval-product/22-battle-arena-probe/) (standard, **LLM engagement confound 분리 — 전투-전용 arena 프로브 #22**, M3-EC4 논문 §5 한계): LLM probe 의 SE-rate 바닥(≈14%)이 "추론 불가"인지 "전투 지속 실패(오버월드 탐색/생존)"인지 미분리 → **`ArenaEnv`**: reset 즉시 전투·bout 종료 즉시 다음·K회 후 종료 — 탐색/생존 confound 를 **구조적으로 제거**. additive 서브클래스: 전투 진입=부모 `_maybe_enter_battle` 재사용(풀힐·commit-window·미래 knob 자동 상속), 전투 턴=부모 `_step_battle` 그대로(전투 경제 무변경), 보스=region gyms 순환(타입 RECUR→cross-battle 추론 여지·procgen/seed split/매치업 보장 그대로). `arena.py`: 4-arm band+submission telemetry — SE-rate=`_super_effective_move`·정규화=`se_inference_score`·다중-run 판정=`classify_inference` **새 임계 0**. **프로브 실측(12 held-out seed, K=10)**: oracle SE 100%(10/10 winnable)/infer 96%/probe 69%/type_blind 46% — **spread +54pp 변별, 도구 유효**. **정직 발견 — 앵커 이동**: arena 는 floor 앵커가 크게 상승(오버월드 type_blind 2%→46%, probe 1%→69%, **probe>type_blind 역전**) → "arena 점수는 arena band 양 앵커로만 읽기, 오버월드 표 접합 금지" 를 문서·러너 출력에 명문화. `llm_eval_run.py --arena --k-battles`(조기분기, 기존 경로 코드 무변경, fake complete 로만 검증). **LLM 실측=미실행** — quota=사용자 승인 게이트("계측 가능해졌을 뿐 아직 미측정" 명시). 13 신규 테스트, 650→**663**(회귀 0; strict-battle PR #103 과 파일 겹침 없음, 둘 다 머지 시 690), ruff clean·mypy 신규 0. L1 BLOCK(AC7 구체성)→보완→APPROVE / L3 **2/2 APPROVE**(plan-reviewer MALFORMED 1회→재호출; additive 진위·재무장 안전성 코드 확인). evergreen `docs/reference/battle-arena.md`(4섹션). Acceptance AC1–AC7.
- **2026-07-01** — `repro-stale-se-ref` (quick-fix): #20 이 §5 를 "inconclusive, near-floor"로 하향한 뒤 `scripts/reproduce_results.py` 정직 노트(docstring + `_print_inference_band` 출력)가 여전히 "§5: ~50%"를 인용하던 불일치 정정 — 두 문자열을 "§5: a robust multi-run probe is inconclusive, near the chart-blind floor"로 교체. comment/string-only(로직·출력·테스트 무변경), pytest 525 유지, ruff clean. mode=quick-fix(manual override: scripts/** critical 이나 comment-only=회귀 0, §F.4). L3 qa-verifier APPROVE. 자기 재현 스크립트가 논문이 철회한 수치 인용하던 credibility 불일치 제거.
- **2026-07-01** — [`inference-robust-correction`](_archive/2026-Q2/eval-product/20-inference-robust-correction/) (standard, docs-only, **§5 정정 — 단일-run "부분 추론(50%)" 을 robust 결과로 정직 하향 #20**): #17 이 §5·Abstract·competitive-analysis 를 "보정 분포 위 partial, real in-context inference(SE ≈50%)"로 reframe 했으나 그 근거는 **단일 run(n=8)**. #18(se-rate-rigor) 도구로 **3-run(n=4) robust 측정** 실행(claude-cli, 구독): 정규화 SE-rate inference score **0.10 ± 0.08 → INCONCLUSIVE**(gym 0.04 ± 0.06 도 inconclusive), LLM SE ≈14%, near chart-blind floor. **단일 50% 는 robust 하게 재현되지 않음** → "partial, real inference" 단언을 "**inconclusive, near-floor**"로 정직 하향. **동시에 설계 건강 명시(과소 reframe 방지)**: scripted infer 앵커가 robust 하게 **≈89%** → eval 은 추론을 registered → LLM near-floor 는 **eval 아티팩트 아닌 진짜 신호**(단 engagement/survival confound 잔존 → 강한 신호지 verdict 아님). **caveat**: 단일 n=8 vs robust n=4(floors 27% vs 6%) → 직접 반박 아님, apples-to-apples n=8 3-run 은 후속. **교훈(moat 실증)**: robustness 도구(#18)가 *우리 자신의 non-robust reframe(#17)* 을 잡음 — 앵커로 검증 후에도 예측이 깨지는 것 = eval 이 정직·un-gameable 하게 작동하는 증거. #17 의 render/memory/matchup confound-peeling·infer 앵커 서사는 유효(correction not negation), **결론(50% partial)만** 하향. 변경: `docs/paper/critter-gym.md`(Abstract + §5 single-run bullet + 신규 design-validated bullet + Honest scope), `docs/explanation/competitive-analysis.md`. **무회귀**: docs-only(코드 0), broken-link 0, 잔여 stale overclaim 0. L3 plan-reviewer(gym std 0.08→0.06 정정 지적)+qa-verifier **2/2 APPROVE**. arXiv 공개는 사람 게이트. Acceptance AC1–AC4.
- **2026-06-30** — [`repro-inference-band`](_archive/2026-Q2/eval-product/19-repro-inference-band/) (standard, **논문 §5 inference band 를 one-command 재현에 추가 — M3-EC4 #19**): 논문 headline 재현 진입점 `scripts/reproduce_results.py` 가 (1) throughput + (2) oracle headroom 만 재생성하고 **§5 의 새 중심 수치 = eval-product inference band(#16: oracle/infer/type_blind/probe super-effective-move rate)는 빠져** 있었다 — #17 reframe 으로 §5 가 논문 주요 섹션이 됐는데 그 band 를 한 명령으로 검증 불가. band 는 scripted·무료·결정론 → 완벽 재현 가능. 신규 testable `inference_band(quick) -> InferenceBaseline`(demonstrator config `_DEMO_CONFIG` master_seed=20260627·types3·grid5·boss140/6/18·max_steps=40, quick n_worlds=4/full=8, `inference_baseline` 재사용) + `_print_inference_band` + main() **"(3) eval-product inference band" 섹션**(4-arm SE-rate, ceiling→floor) + 헤더 (1)(2)(3)·docstring 갱신. **정직**: 출력에 "scripted band=무료·결정론·재현 / 논문 §5 frontier-LLM read(~50%)=유료·평가자 로컬·**본 repro 미포함**" 명시(과대 0). **무회귀**: 기존 (1)(2) shell-out(bench_throughput/ppo_baseline) byte-identical, 추가·display-only. 2 신규 테스트(band 단조·결정론·quick<full). 523→**525**(+2, 회귀 0), mypy/ruff clean. L3 plan-reviewer(라인 단위)+qa-verifier **2/2 APPROVE**(SUGGEST: SealedEvalSet 이중 생성 → sealed 1회로 정리). **동반**: #18 audit trail 복구(`722f9e4` — #88 커밋이 코드만 포함해 누락된 CHANGELOG/INITIATIVE/archive 복원). main 단독 PR. M3-EC4(arXiv 초안 신뢰성) 전진. Acceptance AC1–AC4.
- **2026-06-30** — [`se-rate-rigor`](_archive/2026-Q2/eval-product/18-se-rate-rigor/) (standard, **SE-rate robustness — 다회 super-effective-move-rate 의 사전약정 robust verdict #18**): #17 헤드라인(보정 분포 위 LLM SE-rate ≈50% = 부분 추론)이 **단일 telemetry 패스**라 robust 하지 못했다. #10 `inference-score-rigor` 가 gym `inference_score` 에 한 사전약정 분류기를 **SE-rate 에도** 적용. 신규 **`eval_harness.se_inference_score(submission_se, oracle_se, blind_se)`**: SE-rate 를 [0,1](blind=0/oracle=1)로 정규화(`Scorecard.inference_score` span 공식 **정확 mirror**, clamp, span≤0→0.0) → inference_score 와 *같은 frame*. 러너 `--telemetry` 가 submission telemetry 를 `n_runs=max(1,--runs)`회 실행(기존 1회 → N회) → 각 SE 정규화 → **runs>1 이면 기존 `classify_inference`(임계 0.50/0.10/1.0 frozen)로 robust SE verdict**(mean±std + infers/at-chart-blind-floor/inconclusive), runs=1 이면 단일 se_inference_score. **rigor**: 정규화 SE 가 inference_score 와 동일 [0,1] frame(0=blind/1=expert)이라 frozen 임계 transfer by construction — **새 임계 0**(p-hacking 회피). **무회귀**: score_agent/score_inference_telemetry 본문 무변경(추가 함수·display/loop), runs=1 telemetry 1회=byte-identical. **정직 경계**: scripted-proxy band·1 모델 = 신호이지 verdict 아님; 유료 N-run LLM 실행은 평가자 로컬; 정규화 SE 가 max_steps 의존(blind floor step-cap 민감 → 같은 LLM 50% 가 ms40 0.32/ms200 0.46) → band+submission 같은 sealed(같은 max_steps)서 계산(러너 자동, #16 계승). **검증**: 3 신규 테스트(앵커·band·분류기 재사용[oracle→infers·type_blind→at-floor]) + L3 plan-reviewer(라인 단위: 공식 mirror·same-sealed·정규화 입력)+qa-verifier **2/2 APPROVE**(plan-reviewer 1차 MALFORMED→재호출 APPROVE). 520→**523**(+3, 회귀 0), mypy(31)/ruff/build clean. INITIATIVE(#18) 갱신. main 위 단독(#84/#85/#86 머지 후, #87 로 baseline·reframe main 안착). **후속(사용자)**: `--telemetry --runs 5` 로 SE-rate robust verdict 실측(유료·로컬). Acceptance AC1–AC5.
- **2026-06-30** — [`inference-floor-reframe`](_archive/2026-Q2/eval-product/17-inference-floor-reframe/) (standard, docs-only, **inference floor narrative reframe — 보정 분포 위 부분-추론 실측 정직 반영 #17**): 매치업 fix(#15)+baseline(#16) 위에서 돌린 **단일-run LLM 실측**(claude-opus-4-8, claude-cli, 보정 분포 n=8·max_steps=40)이 SE-rate **≈50%**(68 battle moves) — chart-blind floor(type_blind ≈27%)보다 위, 추론 proxy(infer ≈90%)·oracle(100%) 아래; inference_score ≈0.14, gym-clears 65% of oracle. 이로써 논문 §5·Abstract·competitive-analysis 의 "frontier LLM 이 render·memory 두 confound 제거 후에도 **robust 0% chart-blind floor**" 단언을 정직하게 reframe: **세 번째 validity 아티팩트**(매치업-broken 분포 — boss-placement 필터가 super-effective 매치업을 보장 못 해 oracle SE 자체가 100%→5-23% 붕괴 → "0%"가 *추론 불가*와 *급소 무브 부재*를 뒤섞음)를 추가하고, 보정 분포 위 **부분적이지만 실재하는 in-context 추론**(blind 위·expert 아래)으로 정정. **correction not negation**(render·memory floor 는 실재했고 여전히 제거됨, 매치업은 *세 번째* 층) + 과대("LLM 추론 가능") · 과소("여전히 0%") 양쪽 회피. **정직 캡션**: 단일 run·n=8·1 모델·1 band·scripted-proxy·step-cap 의존(type_blind floor ms=40 27% / ms=200 7%)·신호이지 verdict 아님·robust multi-run verdict 는 후속(--runs 3 run 이 세션서 killed → 단일-run 으로 진행, 사용자 결정). 변경: `docs/paper/critter-gym.md`(Abstract line 29 + §5 frontier-LLM probe 단락 + Honest scope 3 limitations, +44/−22), `docs/explanation/competitive-analysis.md`("monetizable eval" 행 reframe). **무회귀**: docs-only(코드 0), §9 모순 0, 수치 전반 정합(#16 band). **검증**: L3 plan-reviewer(라인 단위 수치 정합)+qa-verifier **2/2 APPROVE**(plan-reviewer 1차 MALFORMED→재호출 후 APPROVE). #15(PR #84)←#16(PR #85)←본 task stacked(파일 disjoint=충돌 0). arXiv 공개는 사람 게이트. Acceptance AC1–AC4.
- **2026-06-30** — [`inference-baseline`](_archive/2026-Q2/eval-product/16-inference-baseline/) (standard, **보정된 분포 위 inference baseline 확정 + 재측정 프로토콜 — #16**): 매치업 fix(#15)가 held-out 세계 분포를 바꿔(SE-exploitable boss 만 배치) **이전 LLM inference 수치(#11/#13/#14)와 직접 비교 불가**가 됨 → 타당한 baseline 재확립. 신규 **`eval_harness.inference_baseline(sealed)`**: 4 scripted arm(oracle/infer/type_blind/probe)을 같은 봉인 세계에서 **세계별 격리**(arm 을 seed 마다 새로 생성=세계 간 메모리 누수 0, `learnability._arm_means`·score_agent 내부 동작과 일치)로 돌려 arm 별 `gym_clears`·`se_rate`·`inference_score`(span 공식은 score_agent 재사용) 반환. 러너 `--telemetry` 가 oracle/type_blind 2-arm → **full 4-arm band**(infer·probe 추가) 출력. **Green 측정 정직 보정 2건**: (1) plan 탐색표의 infer SE 39%는 `score_inference_telemetry`(reference arm reset 안 함)의 **세계 간 stale 메모리 아티팩트** — per-world 격리로 재측정 시 infer SE **90%**(이전 world 틀린 차트 기억 제거). (2) **gym-clears saturate** — per-world 격리 시 infer gym-clears == oracle(2.12), `damage=max(1)` attrition 이 *에피소드 내 학습+소모전*으로 winnable gym 다 깸 → gym 기반 inference_score 둘 다 1.0 → **gym-clears 는 추론/전문가 변별 불가**(#12 confound 재확인). 결론: **SE-rate 가 변별자**(oracle 100% > infer ~85-90% ≫ type_blind 3-7% > probe 0%). 테스트 앵커가 박제(oracle iscore==1.0·type_blind==0.0·infer.se_rate>type_blind.se_rate; infer iscore strictly-between 단정 안 함=saturate 인정). 신규 evergreen `docs/reference/inference-baseline.md`(보정 band 2 config×2 n + 고정 재측정 명령 + 정직 경고). **무회귀**: score_agent/score_inference_telemetry 본문 무변경(추가 함수·display-only) → 채점 byte-identical. **정직 경계**: band=scripted proxy(infer arm=추론 proxy, LLM 아님)·single config·작은 N=신호이지 verdict 아님; 옛 수치 비교 금지; 유료 실측=평가자 로컬; attrition 재설계=사람 게이트. **검증**: 3 신규 테스트(단조·결정론·앵커) + L3 plan-reviewer(라인 단위 정확성)+qa-verifier **2/2 APPROVE**. 517→**520**(+3, 회귀 0), mypy(31)/ruff/build clean. INITIATIVE(#16) 갱신. #15(PR #84) 위 stacked(다른 파일=충돌 0). Acceptance AC1–AC4.
- **2026-06-30** — [`matchup-validity`](_archive/2026-Q2/eval-product/15-matchup-validity/) (standard, **eval-config 매치업 validity — 모든 procgen 세계가 추론-exploitable super-effective 무브를 보장**): #14 직후 발견한 측정-타당성 구멍 정정 — demonstrator config 가 **world 마다 "추론으로 exploit 가능한 결정적 한 수"를 보장하지 못함**. 증거(재현): `SealedEvalSet(master_seed=20260627, grid5, types3, boss140/6/18)` 에서 oracle super-effective-rate 가 n_worlds 1~3 에선 100% 지만 **4·6·8 에선 0.055·0.227·0.115 로 붕괴**(+ n_moves 32→391 폭증=attrition grind) → "oracle 100%"는 유리한 2-world 아티팩트, 추론 잘하는/못하는 LLM 변별 불가. **근본 버그(코드로 확정)**: `region.generate_region(vary=True)` 의 boss-placement 필터 `any(chart.effectiveness(s, t) >= NEUTRAL for s in _STARTER_TYPES)` 가 **no-op** — boss 타입 `t` 자신이 `_STARTER_TYPES`(=party 무브 타입 F/W/G)에 포함돼 `effectiveness(t, t) == NEUTRAL ≥ NEUTRAL` 이 항상 참 → 아무 타입도 배제 못 함. 그래서 per-seed chart 가 transitive(a>b>c, a>c)일 때 최상위 타입 `a`(어떤 타입에도 안 짐)가 SE 카운터 없이 boss 로 배치 → oracle(`_favorable_type`→None)이 neutral 로 grind. **수정**: 필터를 **`> NEUTRAL`**(strictly super-effective)로 좁혀(`winnable`→`exploitable`) 각 placed boss 에 party SE 무브 존재를 보장 + 빈-집합 `ValueError` 가드(num_types≥3 토너먼트 in-degree 합 C(n,2)≥n 으로 이론상 도달 불가, 1200 region 검증서 미발동) + 주석/결합 불변식(`_STARTER_TYPES`==party 무브 타입) 박제. fixed mode(vary=False)는 `if vary:` 밖 → byte-identical. **결과**: oracle SE-rate **1.000 at every n∈{1,2,3,4,6,8}**(붕괴 해소), oracle n_moves(n=8) 391→62(결정적 SE 승리), 변별 band(oracle−type_blind) **+0.93(n=8)/+0.97(n=16)**. **정직 보정(p-hacking 아님)**: band 는 집계 속성(type_blind 고정 챔피언 FIRE 가 n=1/3 소표본서 우연히 정답과 일치) → 비붕괴 floor 는 *모든 n* 에서, 변별 band 는 *정의 성립 규모(n=8,16)*에서 검증; floor(0.5)는 측정 후 약화 0(실측 1.000). **정직 경계**: hard 보장은 SE 무브 *존재* 뿐, oracle 이 *매번* 쓴다는 보장 아님(commit-economy=별개); 전투 모델 재설계(`damage=max(1)` attrition)는 벤치마크 정의 변경=사람 게이트로 남김; 단일 config·scripted proxy 신호. 성격: 생성 correctness 버그(render_obs #14 동성격), 벤치마크 정의 변경 아님. **검증**: 3 신규 테스트(불변식 80seed×3types / 비붕괴 / 변별) + L3 plan-reviewer+qa-verifier **2/2 APPROVE**(5축 무조건). 514→**517**(+3, 회귀 0), mypy(31)/ruff/build clean. INITIATIVE(#15) 갱신. Acceptance AC1–AC4.
- **2026-06-30** — [`render-obs-tile-codes-2`](_archive/2026-Q2/eval-product/14-render-obs-tile-codes-2/) (standard, **render_obs 타일-코드 버그 수정 — #14 재적용(머지충돌 회피) + LLM floor root-cause 확정**): #81(`render-obs-tile-codes`, L1/L3 APPROVED)이 #79·#80 머지 후 main과 충돌(같은 파일 편집·#81은 #79 전에 분기→`BattleMemoryLLMAgent` 미보유로 웹 충돌해소가 #79 코드 손실 위험) → **#79 포함 새 main 위에 동일 수정 재적용**으로 충돌 0 PR. **버그**: `render_obs`(LLM 텍스트 맵)의 `_TILE_GLYPHS={0:".",1:"#",2:"C",3:"G"}`·살라언스가 gym=3·creature=2 가정했으나 env(`critter_env.py:53`)는 `_PATCH_EMPTY=0,_PATCH_CREATURE=1,_PATCH_GYM=2` emit → LLM이 생물(1)을 "#"벽으로, 체육관(2)을 "C"생물로 보고 "G" 안 뜸. scripted는 raw obs라 정상(체육관 클리어) — **LLM만** 뒤섞인 지도(=#6·#7 증상). 미검출: 합성 테스트가 env 실제 코드가 아닌 render_obs의 *틀린 가정*을 그대로 씀. 수정: env `_PATCH_*` 상수 import해 글리프·범례·살라언스·center 분기에 사용(SSOT 드리프트 방지) + 신규 2테스트(상수 대조 + 실 env, `"#" not in grid`) + 기존 2 갱신. **무회귀**: render_obs는 scripted 채점 경로 미참조 → byte-identical, #79 `BattleMemoryLLMAgent` 무영향. **실측 재측정(claude-cli, demonstrator)**: render fix로 **engagement floor 풀림**(LLM이 체육관 진입·battle moves 4~13→~60·비-gated world 100% 클리어). 그러나 **inference floor는 진짜**: render fix + battle-memory(#13) **둘 다** 적용한 gated worlds서도 **SE-무브율 0%**(~30무브, vs oracle 100%)·inference_score 0.00 → floor는 *하네스 artifact 아님*(두 의심 artifact 제거 후에도 유지)=깨끗한 비포화 능력 신호. competitive-analysis 정직 반영. 512→**514**(+2, 회귀 0), mypy(31)/ruff clean. **정직 경계**: 단일 run·2 gated world·proxy·1 모델 제로샷=신호이지 verdict 아님; oracle 100%(풀 수 있음); "쉽게 만들기"가 아니라 *floor가 진짜인지 검증*. #14는 #81에서 L1/L3 APPROVED(본 PR=재적용). Acceptance AC1–AC6.
- **2026-06-29** — [`agentic-battle-memory`](_archive/2026-Q2/eval-product/13-agentic-battle-memory/) (standard, **전투-결과를 기억하는 agentic 메모리 — "얇은 어댑터" confound 제거 #13**): #11 측정 floor(inference_score 0.00 = `at-chart-blind-floor`)가 "프런티어 LLM이 숨은 차트를 추론 못 한다"는 *능력 verdict*인지, 아니면 **어댑터가 추론 신호를 버려서** 생긴 *측정 아티팩트*인지를 가르는 토대. **confound 정체(코드로 확정)**: `StatefulLLMAgent`(#5)는 `_history`에 `(_obs_summary, action)`만 누적하고 `_obs_summary`는 **위치+gyms만** 담아 — 전투에서 *어떤 무브가 적 hp를 얼마 깎았는지*(=효과성 신호)를 **구조적으로 버린다**. 그런데 `DEFAULT_SYSTEM`(#7)은 "무브 써보고 적 hp 낙폭 *관찰·기억*하라"고 지시 → **지시↔메커니즘 불일치**가 "얇은 claude-cli 어댑터" confound. 신규 **`llm_eval.BattleMemoryLLMAgent(StatefulLLMAgent)`**: 연속 in-battle obs를 diff해 직전 공격무브(0-3)의 `enemy_hp` 낙폭을 적 타입별 **`{enemy_type:{move:최신 관찰 데미지}}`**로 귀속((타입,무브)당 **단일값 덮어쓰기**=리스트 누적 아님, 행당 ≤4 무브 → bounded ≤num_types×4) → 프롬프트에 **원시 관찰 사실**로 surface(`vs enemy type 3: move 1 dealt 40, move 0 dealt 8`, move-index 순). **측정 무결성(핵심)**: surface에 hidden type名·차트·**정답-무브 추천 없음** — "어떤 무브가 정답인가"는 LLM이 스스로 추론(이게 env가 재려는 능력; 추천을 넣으면 어댑터가 추론을 대신 = 측정 무효). `reset()`이 표 clear(월드 간 누수 0). 부모에 `_remember()` 추출(act() 윈도우 로직 공유). 러너 `--battle-memory`(stateful 함의) 노출. **귀속 견고성**: 직전 스텝을 obs dict 아닌 **스칼라 tuple `_prev_battle`**로 스냅샷(numpy 배열 별칭 시 delta=0 잠재버그 차단); 같은-type 적 교체 오귀속은 env 불변식(단일 보스·faint가 전투 종료→in_battle 0)으로 **도달 불가**(docstring 명시). **무회귀**: 기존 `LLMAgent`/`StatefulLLMAgent` 본문 무변경, scripted reference arm byte-identical(`oracle_gyms`/`type_blind_gyms` 대조 테스트). 런타임 sanity: oracle로 실제 전투 진입 후 agent에게 전투 위임 → 실 env obs서 귀속 발화 확인(`{2:{1:10}}`). **정직 경계(계승·reframe 금지)**: 메커니즘이지 측정 결과 아님(CI=stub); 실측 재측정은 사용자 로컬(구독 CLI/API); 어댑터 두껍게 한 것은 *후속* 측정을 공정하게 할 뿐 그 자체로 floor가 아티팩트였음을 증명 안 함; 전투 attrition(`damage=max(1)`)·작은 표본 confound 잔존 → floor로 남아도 "LLM 능력 verdict"로 reframe 금지. **다층 검증**: 10 테스트(귀속·덮어쓰기·bounded 상한·surface·무결성[정답-무브 부재]·docstring 정직경계·교체/non-attack 비귀속·reset 격리·byte-identical 대조·protocol) + 실 env sanity + L3 plan-reviewer+qa-verifier 2/2 APPROVE(SUGGEST 5건 전부 반영). 502→**512**(+10, 회귀 0), mypy(31)/ruff/build clean. INITIATIVE(#13) 갱신. **De-thinned 재측정 실행(claude-cli, demonstrator config grid5·types3·boss140/6/18, 3 runs)**: 40·120스텝 모두 여전히 **inference_score 0.00 = `at-chart-blind-floor`** + **SE-무브율 0%**(120스텝서 메커니즘 발동 — battle moves 4→13, vs oracle 100%/18). 결론(reframe 없음): floor는 *메모리 아티팩트 아님*(de-thinning이 그건 제거) 그러나 *능력 verdict도 아님* — binding confound가 *메모리*→**전투 engagement/생존**으로 이동(에피소드당 ~2 battle moves vs oracle ~3) + 표본·`damage=max(1)` attrition 잔존. 다음(전투모델/config)=벤치마크 정의=사람 게이트. `competitive-analysis.md` "monetizable eval" 행에 정직 반영. Acceptance AC1–AC8.
- **2026-06-29** — [`inference-telemetry`](_archive/2026-Q2/eval-product/12-inference-telemetry/) (standard, **직접 추론 메트릭 — super-effective 무브 사용률(attrition confound 우회) #12**): probe 여정으로 확인 — 전투 `damage=max(1,...)`라 **살아남으면 중립 무브 attrition으로 이김** → gym-clear 기반 inference_score는 "추론 필수 + 학습 가능" sweet spot이 노브로 안 잡힘(전투 *모델* 변경은 벤치마크 정의 변경=사람 게이트). **사용자 결정=자율안전 대안**: env를 **read-only**로 두고, 전투에서 agent가 숨은 타입표를 추론해 **super-effective 무브(eff>1.0)를 골라 쓰는 비율**을 직접 측정 — 이기든 지든, attrition과 무관하게 *추론 행위 자체*를 잼. 신규 `InferenceTelemetry(super_effective_rate, n_battle_moves)` + `_super_effective_move(env, action)`(read-only: `env._mode=='battle'` ∧ action 0-3 → chosen move `min(action,len-1)`의 `chart.multi_effectiveness(move.type, enemyB.types) > 1.0`; switch(4)/pass(5)/overworld는 None→제외) + `score_inference_telemetry(submission, sealed)`(seeds 루프 + per-episode `reset` 훅, se_hits/n_moves, 0-move→0.0 가드). 러너 `llm_eval_run.py`에 **`--telemetry`** — submission + `oracle`/`type_blind` 앵커의 SE-rate 출력(미지정 시 기존 경로 불변). **변별 입증(무료 scripted)**: oracle **SE-rate 61%** vs type_blind **7%**(33/56 battle moves) — chart-exploiting과 chart-blind를 깨끗이 가름, 승리/attrition과 분리. **무회귀(핵심)**: env를 *읽기만* 함(`_battle`/`state`/`chart` write 0) → `score_agent`/`_play_once` 무변경, scripted byte-identical(default oracle mean 1.125/frac 1.0 불변), JAX parity·기존 baseline·obs/reward 전부 불변 — `test_telemetry_is_read_only_score_agent_unchanged`로 고정. **정직 경계**: SE-rate는 *exploit 신호*이지 "추론했다"의 완전 증명 아님(oracle/blind 앵커 대비로 읽기·단일 config·scripted proxy); 점수 보장 아님; 실측 LLM SE-rate는 후속 probe(acceptance 아님) — docstring + 러너 출력에 "exploit signal, not proof of inference / read-only" 명시. **다층 검증**: 4 테스트(oracle>blind·unit-interval+결정론·0-move 가드·read-only score_agent 불변) + scripted 변별(61% vs 7%) + L3 2/2 APPROVE. 498→**502**(+4, 회귀 0), mypy(31)/ruff/build clean. INITIATIVE(#12) 갱신. 후속(사용자/자율): `--telemetry`로 LLM SE-rate 실측 → gym-clear floor와 분리된 추론 신호 기록. Acceptance AC1–AC5.
- **2026-06-29** — `inference-measurement-sync` (quick-fix): #10 도구로 측정한 첫 robust 결과를 competitive-analysis "monetizable eval" 행에 반영(#9 "first probe 0.00 weak" 격상) — claude-opus-4-8, inference-gated demonstrator(grid5·types3·boss140/6/18): **40스텝 3 runs → inference_score 0.00 ± 0.00 = `at-chart-blind-floor`**(사전약정 분류기, 노이즈 아님) + **호라이즌 sweep 40/60/120 모두 0.00** → floor는 *inference-bound, not budget-bound*(생각 시간 3배도 무의미). 정직 경계: non-saturated·discriminating 신호(강점)이지 "LLM 못 함" 아님; 단일 band·2월드·scripted-oracle proxy; difficulty curve=다음 측정; hosted/고객/공개 human-gate. docs-only, L3 qa-verifier APPROVE.
- **2026-06-28** — [`inference-score-rigor`](_archive/2026-Q2/eval-product/10-inference-score-rigor/) (standard, **inference_score 측정 robust화 — 사전약정 multi-run 분류기 #10**): #8의 첫 KPI는 **단일 run·2월드·60스텝 = 약한 0.00**. 과거 (B) 스레드가 single-run을 노이즈로 반복 교정한 학습대로, **multi-run + 데이터 보기 전 고정한 결정규칙**으로 "프런티어 LLM이 chart-blind floor를 robust하게 못 넘는가/넘는가"를 판정 가능하게. 신규 **`src/critter_gym/inference_rigor.py`**(numpy-only, CI-cheap, `headroom.py`/`classify_headroom` rigor 패턴 mirror): `classify_inference(inference_runs, *, infer_thresh=0.50, floor_eps=0.10, k=1.0)` + `InferenceVerdict(verdict, mean, std, n_runs)` — per-run inference_score들의 mean `m`·std `s`(across runs)로 **3-branch**: `m−k·s ≥ infer_thresh`→**infers**(비관적 하한도 임계 위=robust 추론/floor 초과) / `m+k·s ≤ floor_eps`→**at-chart-blind-floor**(낙관적 상한도 floor=robust 추론 실패) / else→**inconclusive**(run band가 임계 가로지름→run 더 필요); 빈 run ValueError. 러너 `llm_eval_run.py`에 **`--runs N`**(기본 1) — N회 `score_agent`(scripted oracle/type_blind 결정론, LLM submission만 변동) → per-run inference_score 모아 `classify_inference` verdict 출력(`mean ± std (n runs) → VERDICT`); **N=1이면 기존 단일-run 출력 경로 불변**(무회귀). **임계(0.5/0.1/1.0)는 데이터 보기 전 freeze**(코드 기본값 + qa-checklist + `test_frozen_thresholds_are_defaults`) → p-hacking 차단. **정직 경계**: 분류기는 *도구*(verdict 그대로 기록, 결과 단정 안 함 — docstring 명시); 실측 N-run LLM 수치는 **후속**(acceptance 아님); inference_score 자체의 한계(특정 band·scripted-oracle proxy)는 #8에서 명시. **다층 검증**: 9 property 테스트(infers/floor/inconclusive/high-mean-wide-std/floor 경계/mean·std·n/빈 가드/frozen 기본값/single-run, **LLM 불요·순수 numpy**) + 집계 로직 scripted arm 검증(oracle×3→infers mean1.0 / type_blind×3→at-floor mean0.0) + L3 2/2 APPROVE. 489→**498**(+9, 회귀 0), mypy(31)/ruff/build clean. INITIATIVE(#10) 갱신. 후속(사용자/자율): `--runs 5` 등으로 demonstrator config 실측 → 첫 robust inference verdict 기록. Acceptance AC1–AC5.
- **2026-06-28** — `eval-product-narrative-sync` (quick-fix): competitive-analysis "monetizable eval" gap 행을 갱신 — #5~#8(sealed LLM 어댑터+기억+obs/전투 가독성+inference_score KPI)로 **측정 토대 완성** + 첫 probe **inference_score 0.00**(claude-opus-4-8, inference-gated demonstrator: oracle 3.00/type_blind 0.00)을 정직하게(non-saturated·discriminating 신호이자 약한 증거, "LLM 못 함" reframe 금지; hosted/고객/공개 human-gate) 반영. docs-only, L3 qa-verifier APPROVE.
- **2026-06-28** — [`inference-score-metric`](_archive/2026-Q2/eval-product/08-inference-score-metric/) (standard, **고객용 moat 지표 — 봉인 eval의 in-context 추론 정량화 #8**): 고객(프런티어 랩/RL 연구자)에게 파는 희소재("외울 수도 오염될 수도 없는, 숨은 규칙을 in-context 추론해야 풀리는 eval")를 **단일 KPI**로. 신규 `Scorecard.inference_score = (submission − type_blind) / (oracle − type_blind)`, [0,1] 클램프(`0`=규칙 모르는 chart-BLIND baseline 수준 / `1`=규칙 아는 expert; 분모≤0(oracle≤type_blind)이면 0.0). probe 여정(#5~#7)+scout(무료 scripted)로 확인한 **oracle 100% vs type_blind 50%** 추론 gap 위에 submission이 어디 떨어지는지를 정규화 — 처음 보는 봉인 세계서 숨은 타입표를 *그 자리에서* 추론해야 점수가 오르므로 **암기·오염·게이밍 불가**(고정 벤치마크가 언젠가 유출돼 못 주는 신뢰=파는 희소재). + `SealedEvalSet`에 **`grid_size`·`boss_hp`·`boss_atk`·`boss_def` 노브**(기본=CritterEnv 기본값 grid10·120/12/12 → **byte-identical**; `env_factory`가 CritterEnv에 전달)로 *navigable*(작은 grid=LLM 항법 가능)+*inference-gated*(chart-blind 실패·expert 성공)+*학습 여유*(낮은 boss_atk) demonstrator config를 봉인 set으로 타깃. 러너 `llm_eval_run.py`에 노브 CLI(`--grid-size/--boss-hp/--boss-atk/--boss-def`) + 고객용 3-arm(oracle=expert ceiling / type_blind=blind floor / LLM) + `INFERENCE SCORE: X.XX (0=no-knowledge baseline / 1=expert)` headline + demonstrator preset docstring. **정직 경계(과대 금지)**: inference_score는 *특정 난이도 band 신호*이지 절대 능력치 아님(config·oracle proxy·seed-set 동반); type_blind=floor 정당성=규칙 모르고 침; **점수 보장 아님**(낮으면 낮은 대로 기록); demonstrator=시연용(제품 난이도 표준 아님, 사람/전략 게이트). **무회귀**: 기본 SealedEvalSet byte-identical(oracle mean 1.125 불변), Scorecard NamedTuple 끝에 필드 추가(생성처 1곳 keyword → positional 무영향). **다층 검증**: 5 테스트(노브 env 전달·기본 무회귀·oracle→1.0·type_blind→0.0·random∈[0,1]·분모 가드) + L3 2/2 APPROVE. 484→**489**(+5, 회귀 0), mypy(30)/ruff/build clean. INITIATIVE(#8) 갱신. 후속: demonstrator config 실측으로 첫 inference_score 기록. Acceptance AC1–AC5.
- **2026-06-28** — [`battle-legibility`](_archive/2026-Q2/eval-product/07-battle-legibility/) (standard, **전투 obs 가독성 — LLM이 숨은 타입표 추론 루프에 진입하게 #7**): #6(render 수정) 후에도 stateful probe 0% floor → **existence probe**(작은 5×5 grid·체육관1·`num_types`3·긴 호라이즌, 탐색 벽 제거)로 진단: 에이전트가 **체육관엔 잘 도달(battle-entries 19)하나 보스전을 전부 패배**. 같은 config에 scripted 대조 — **oracle 클리어(3턴) / type_blind 59턴 전패(0 gym) / claude-opus-4-8 = type_blind처럼 행동**(transcript: move 0만 반복·다른 무브 0~3[다른 타입] 미탐색·파티 교체(action4)·커밋 윈도우 미사용·2턴 사망·체육관을 생물로 착각해 Catch 반복). 즉 **승리는 숨은 타입표 추론을 요구**하는데(type_blind=0이 증거) LLM이 그 *추론 루프에 진입조차 못 함* — 전투 메커닉(무브가 여러 타입·교체·커밋·재시도)을 obs/시스템이 안 알려줘서. 수정: **`DEFAULT_SYSTEM`** 에 전투 전략(공격 무브 0~3은 서로 다른 **HIDDEN type** → 시도하며 적 hp 낙폭으로 super-effective 무브 발견·기억 / `spamming move 0 usually loses` / 나쁜 매치업이면 action4 Switch / 패배 시 파티 회복되니 같은 체육관 재진입해 배운 걸로 재시도) + **catch 명확화**(wild creature C 타일에 **정확히** 서야 동작·gym/빈 타일선 무동작 → catch-루프 낭비 차단). **`render_obs`** 전투 분기에 한 줄 Tip(무브 0~3 다양화·적 hp 변화 관찰·Switch). **벤치마크 정직성(핵심)**: 어떤 무브가 super-effective인지 **정답을 떠먹이지 않음** — "시도→관찰→기억"의 *방법*만 안내, 정답은 LLM이 추론(이게 env가 측정하려는 in-context hidden-rule 추론). **obs 스키마 무변경**(commit_window 상태·개별 무브 타입 노출은 env 변경=별도 task로 분리; 시스템 프롬프트가 메커닉을 *설명*하되 obs에 없는 실시간 상태 날조 안 함). **점수 보장 아님**(2턴 사망·num_types 등 잔존 → 재측정해도 floor 가능). **무회귀**: scripted agent는 `render_obs`/`DEFAULT_SYSTEM` 미사용 → `score_agent` 수치 불변(oracle frac_of_oracle=1.0). **다층 검증**: 4 테스트(전투 전략 가이드[정답 미노출 포함]·전투 render Tip+스탯 유지·오버월드 누출 없음·catch 명확화) + L3 2/2 APPROVE. 480→**484**(+4, 회귀 0), mypy(30)/ruff/build clean. INITIATIVE(#7) 갱신. 후속: 수정된 전투 가독성으로 existence probe 재측정 → 클리어 여부 확인. Acceptance AC1–AC5.
- **2026-06-27** — [`render-obs-legibility`](_archive/2026-Q2/eval-product/06-render-obs-legibility/) (standard, **render_obs 가독성 수정 — LLM 오도 제거 #6**): #5 stateful probe(claude-opus-4-8, claude-cli 구독, 봉인 3월드×40스텝)가 무상태·stateful **둘 다 0% of oracle**로 floor → 1 에피소드 **transcript 진단**(seed 1506920)으로 원인이 **`render_obs`의 오도**임을 확정(난이도 verdict도 파싱 버그도 아님): (1) **0-마스킹 오도(핵심)** — env(`critter_env.py:354`)는 `player_hp/type/level`을 *전투 중에만* 채우고 오버월드선 0-마스킹하는데 `render_obs`가 이를 그대로 `"Your creature: hp 0, type 0, level 0"`로 찍어 LLM이 step 1부터 *"나는 생물이 없다"* 오판(실제론 `reset()`이 `starter_party()` 부여) (2) **gym/creature 혼동** — LLM이 체육관(G) 타일을 생물(C)로 착각해 진입·보스전 패배 후 `(3,7)↔(3,8)` 루프로 40스텝 소진(gym0/caught0). 수정: **`render_obs`** 가 `in_battle` 분기 — 전투 중엔 player/enemy 스탯 표시(정확), 오버월드엔 오도 줄 제거 + `"Your party: 스타터 파티 보유(스탯은 전투 중 표시)"` 정직 표현 + 신규 `_nearest_in_view`(center 제외 Manhattan 최근접)·`_dir_phrase`(N/S/E/W 방향구)로 **시야 내 gym(G)/creature(C) 살라언스**("A gym (G) is visible 2 north", "wild creature (C) is visible 2 east") + 중앙 타일 gym/creature 플래그("You are ON a gym — 진입=보스전" / "on your tile — Catch"). **`DEFAULT_SYSTEM`** 에 스타터 파티 보유·목표(G 보스 격파)·catch 흐름(C 타일서 Catch) 1~2문장 추가. **정직 경계(과대 금지)**: obs에 파티 마릿수 필드가 없으므로 **거짓 수치 날조 금지** — 진실 한도("파티 보유, 스탯은 전투 중")만; **렌더 수정 ≠ 점수 보장**(호라이즌 40스텝·전투 난이도 잔존 → 재측정해도 floor일 수 있음, docstring/plan/report 명시); 재측정 probe는 **사용자 로컬**(구독 CLI/API), 나오는 frac_of_oracle은 그대로 기록·reframe 금지. **무회귀**: scripted agent(oracle/type_blind/random)는 `render_obs`를 안 쓰므로 `score_agent` 수치 **불변**(렌더↔채점 분리; oracle frac_of_oracle=1.0 확인) — 렌더는 LLM 텍스트일 뿐. **다층 검증**: 6 테스트(오버월드 오도 부재·전투 스탯·gym 살라언스+플래그·creature 살라언스·결정론+코어필드·DEFAULT_SYSTEM) + 합성 obs 헬퍼 + L3 2/2 APPROVE. 474→**480**(+6, 회귀 0), mypy(30)/ruff/build clean. INITIATIVE(#6) 갱신. 후속(사용자 실행): 수정된 렌더로 stateful probe 재측정 → 0% floor가 풀리는지 확인. Acceptance AC1–AC6.
- **2026-06-27** — [`stateful-llm-agent`](_archive/2026-Q2/eval-product/05-stateful-llm-agent/) (standard, **기억 가진 LLM agent(스텝 간 history+윈도잉) — 공정한 재측정 도구 #5**): 무기억 `LLMAgent`는 부분관측(국소 5×5 view)서 본 지도를 즉시 잊어 floor한다 — 우리가 이미 입증한 **memory load-bearing**(`recurrent-baseline`)과 일관된 현상이지 "프런티어 LLM도 못 푼다"는 난이도 verdict가 **아니다**. 그래서 *공정하게* 재측정하려면 agent에게 기억을 줘야 한다. 신규 **`llm_eval.StatefulLLMAgent`**: 한 에피소드 안에서 `(obs 한 줄 digest, 취한 action)` 쌍을 **슬라이딩 윈도우**(`window` 기본 8)로 누적해 매 프롬프트에 prepend(과거는 full render 아닌 한 줄 digest로 토큰 절감; window 초과분 oldest-first drop; `window=0`=무기억 등가) → 같은 `Agent` Protocol(`act(obs)->int`) 만족 → #1 `score_agent`로 봉인 set 채점. **가장 중요한 정합성 게이트=월드 간 기억 격리**: `eval_harness.Agent` Protocol에 **선택적 `reset()` 훅** 도입 — `score_agent`가 `getattr(submission,"reset",None)`로 duck-typing 추출, `_play_once`가 `env.reset()` **직후 1회** 호출(에피소드당 1회, per-step 아님) → 월드 A의 transcript가 월드 B로 누수 불가. 러너 `scripts/llm_eval_run.py`에 `--stateful --window K`(미지정 시 무상태 경로 그대로) + stateful 시 "프롬프트 길어져 호출당 토큰↑" 경고. **무회귀 비협상**: 기존 `LLMAgent` 본문·`render_obs`·`parse_action` 무변경, reset 없는 submission은 훅 분기 skip → **byte-identical**(`runtime_checkable` Agent는 `act`만 검사하므로 기존 `isinstance` 무영향; `test_score_agent_stateless_submission_unaffected`로 고정). **정직 경계(과대 금지)**: 본 task=**기억 메커니즘**이지 측정 결과 아님(CI/테스트는 결정론 **stub** `complete`, 무-API); 실측 probe는 **사용자 로컬 실행**(구독 claude CLI 또는 API, 키=사용자·채팅 금지) — 나오는 frac_of_oracle은 그대로 기록, "프런티어 LLM이 푼다/못 푼다"로 reframe 금지; "구독으로 과금 0" 주장 금지("API 키 없이 claude CLI 파이프라인이 돈다"까지만). **다층 검증**: 9 테스트(history 누적·window 상한·window=0 무기억·reset 격리·invalid window·end-to-end / reset 에피소드당 1회·무상태 무회귀) + L3 2/2 APPROVE. 465→**474**(+9, 회귀 0; 무상태 byte-identical), mypy(30)/ruff/build clean. INITIATIVE(#5) 갱신. 후속(사용자 실행): `python scripts/llm_eval_run.py --provider claude-cli --stateful --window 8 --worlds N --max-steps M` → "기억 줬을 때 프런티어 LLM이 봉인 환경서 N% of oracle" 실측 회수. Acceptance AC1–AC6.
- **2026-06-27** — [`claude-cli-provider`](_archive/2026-Q2/eval-product/04-claude-cli-provider/) (standard, **구독(claude CLI) provider + score_agent 콜 절반(이중-실행 제거) #4**): (1) **구독으로 실측**(API 과금 회피): 신규 `llm_eval.claude_cli_complete` — 로컬 **Claude Code CLI**(`claude -p`, 중립 tempdir cwd로 repo CLAUDE.md 미로드)를 LLM 백엔드로 → **Claude Code 구독 인증 사용, API 키/과금 불요**; binary 미존재 시 명확 `FileNotFoundError`. 러너에 `--provider {anthropic, claude-cli}`(기본 anthropic; claude-cli=구독·키 불요·`--model`은 anthropic만 적용). (2) **콜 절반(버그 픽스)**: `score_agent`가 submission을 *두 번* 돌리던 결함(`run_episode` 메인 채점 1회 + `_caught_rate` 재실행 1회 = per-step LLM 콜 2배; probe서 30예상→60실제 확인) 제거 → 신규 `_play_once(factory, policy, seed)->(gyms, caught, evolved)`로 한 에피소드서 세 subgoal을 **한 번에** 읽어 seed당 1 패스. submission·reference arm 모두 `_play_once` 1회. **메트릭 byte-equivalent**(동일 env 상태원 `info["subgoals"]`=`sum(_gym_defeated)`/`_caught`/`_evolved`) → 기존 #1/#2 테스트(oracle>random·rate∈[0,1]·end-to-end) 그대로 PASS. **정직 경계(과대 금지)**: 구독은 *대화형 용도*·rate limit 있음 → 자동 다수 호출은 **소규모 probe 권장**·`claude -p`는 **느림(~s/call, full agent 기동)** → 큰 run은 API 권장(docstring 명시). **다층 검증**: 단일-패스 콜-카운팅 테스트 + claude_cli 미설치 에러 테스트 + 기존 metric 테스트 무회귀 + L3 2/2 APPROVE. 463→**465**(+2, 회귀 0; 메트릭 byte-equivalent), mypy(30)/ruff/build clean. 후속: 구독 provider로 적정 호라이즌 실측 → "프런티어 LLM이 봉인 환경서 N% of oracle" 기록. Acceptance AC1–AC5.
- **2026-06-27** — [`llm-eval-run`](_archive/2026-Q2/eval-product/03-llm-eval-run/) (standard, **실제 LLM 비용-제한 sealed-eval 러너 — 프런티어 LLM이 우리 환경서 몇 %? #3**): "우리 시뮬 난이도가 *프런티어 LLM* 기준 어느 정도?"를 실측하는 도구. #2 `LLMAgent`+`anthropic_complete`로 실제 모델(`claude-opus-4-8` 기본)을 #1 봉인 set서 `score_agent` 채점 → **frac_of_oracle**(전문가 대비 %). 매 스텝 LLM 호출이라 비용/시간 폭증 → `SealedEvalSet`에 **`max_steps` 노브 추가**(기본 200=**byte-identical**·env_factory가 CritterEnv에 전달; 작은 값으로 per-step 호출 비용 상한) + 신규 **`scripts/llm_eval_run.py`**(argparse model/worlds/max-steps/num-types/master-seed + 시작 시 **예상 콜 수·⚠️ 비용 경고** + frac_of_oracle/oracle/blind/cleared/caught 출력; 기본 3월드×40스텝≈120콜 작은 첫 probe). **실행=사용자 로컬**(🔑 키=사용자·**채팅/커밋 금지**·SDK가 env `ANTHROPIC_API_KEY`서 읽음; 러너는 키를 인자로 안 받음). 이 환경엔 SDK·키 없음 → 키/SDK 부재 시 명확 `ImportError`(확인). **정직 경계(과대 금지)**: 러너=*도구*(상한+경고)이지 측정 결과 아님; 결과 해석은 **작은 probe·step 상한·proxy oracle·단일 run·signal not definitive** 경계 동반(출력/docstring/report). **다층 검증**: max_steps 캡 테스트(무-API) + 기본 200 무변경 테스트 + 키 없을 때 명확 에러 + L3 2/2 APPROVE. SealedEvalSet 기본 byte-identical → 461→**463**(+2, 회귀 0), mypy(30)/ruff/build clean. 후속(사용자 실행): `python scripts/llm_eval_run.py --model claude-opus-4-8 --worlds 3 --max-steps 40` → "프런티어 LLM이 봉인 환경서 N% of oracle" 실측 회수. Acceptance AC1–AC4.
- **2026-06-26** — [`llm-eval-adapter`](_archive/2026-Q2/eval-product/02-llm-eval-adapter/) (standard, **오염 방지 agentic-LLM 평가 어댑터 — M5 eval 제품 #2**): eval-product의 가장 유망한 갈래(시장 통증 #1 = 오염 안 되는 *agentic-LLM* 능력 평가)를 #1 봉인 하니스 위에 어댑터로 실현 — "처음 보는 세계서 숨은 규칙 추론해 체육관 깨기, RLVR 채점, 봉인 held-out이라 못 외움"을 동작 코드로. 신규 **`critter_gym.llm_eval`**(순수 numpy, core·CI 테스트, eval_harness/env import만): **`render_obs`**(env obs→LLM이 읽는 텍스트: position·in_battle·gyms·party/enemy·5×5 ascii 로컬뷰·문맥별 action 범례; 결정론) + **`parse_action`**(LLM free-text→action: 숫자/키워드[north~wait]/쓰레기·빈→fallback(5=Wait)/범위밖→클램프, 항상 [0,n)) + **`LLMAgent`**(provider-agnostic `complete(prompt)->reply` 주입 → #1 `Agent` Protocol `act(obs)->int` 만족 → #1 `score_agent`[봉인 `SealedEvalSet`]로 그대로 채점) + **`anthropic_complete`**(**옵션** Anthropic hookup, lazy `import anthropic`, `claude-opus-4-8`·thinking 생략·claude-api 레퍼런스 준수, 미설치/키 없음 시 명확 ImportError). **demo**(16 sealed worlds): render_obs 예시 출력 + stub-LLM 에이전트를 봉인 set서 `score_agent` end-to-end 채점. **정직 경계(과대 금지)**: 어댑터 = *메커니즘*(렌더→LLM→파싱→봉인 RLVR 채점 연결)이지 **실측 LLM 능력 결과 아님**(CI/테스트/demo는 결정론 **stub**[무-API]; 실측은 API key+비용 별도 run·사람)·프로토타입(docstring/demo/report 일관 명시). **다층 검증**: 11 테스트(render 결정론·필드·numpy scalar / parse 5종 강건성 / LLMAgent Protocol·end-to-end score / anthropic lazy-import) + demo + L3 2/2 APPROVE. 기존 src 무변경 → 450→**461**(+11, 회귀 0), mypy **30**/ruff/build clean. 후속(사람/비용 게이트): 실측 LLM run(`anthropic_complete`)·서버측 봉인 인프라·hosted eval-as-a-service. Acceptance AC1–AC5.
- **2026-06-26** — [`sealed-eval-harness`](_archive/2026-Q2/eval-product/01-sealed-eval-harness/) (standard, **봉인 held-out + agent 제출 → RLVR 검증 채점 — M5 비공개 eval 제품 기능 토대 프로토타입**): 사용자와 moat 논의 끝에 "기능적 moat의 유일 후보=비공개 재생성 eval 제품(M5)"으로 결론 → 그 *메커니즘*("외울 수도 오염될 수도 없는 eval")을 **동작하는 코드로 입증**. 신규 **`critter_gym.eval_harness`**(순수 numpy, **core·CI 테스트 가능**, 기존 env/learnability/region 심볼만 import·무변경): **`SealedEvalSet`**(secret `master_seed`로 held-out 구역 상단[base=1.1M, 공개 `heldout_seeds` 1M~ 비충돌] 비공개 블록 선택 — 같은 seed→동일 블록[감사], 다른 seed→신선·disjoint 블록[재생성]) + **`verify_sealed`**(오염 가드=moat 핵심: 제출 declared-train이 sealed eval과 **disjoint** ∧ 전부 train 구역[<1M]이어야 ok; leak/구역밖 검출) + **`score_agent`**(verifiable **subgoal로만** 채점: mean_gyms_cleared·cleared/caught/evolved rate·frac_of_oracle; hand-tuned 0) + **`Agent` Protocol**(`act(obs)->int` — 학습/scripted/LLM-agent 공통; EnvPolicy도 수용). **실측 demo**(16 sealed worlds): oracle **1.88(100% of-oracle·94% cleared)** > type_blind **0.94(50%)** > random **0.38(20%·56% caught)** 능력 사다리(RLVR) + **오염 가드**: honest(train 0..50k)→ok·overlap0 / **leak 시도(sealed seed 학습)→ok=False·overlap16 검출·거부**. **moat 메커니즘 end-to-end 입증** = "테스트로 학습 못 함"이 *검증 가능*(고정 벤치마크가 못 주는 신뢰=파는 희소재). **정직 경계(과대 금지)**: **프로토타입**·"봉인"=**in-process 컨벤션**(실제 제품은 서버측 secret seed+제출 샌드박스 필요)·단일 머신·numpy·단일 config·**hosted 서비스/고객/매출/공개 아님**(docstring·INITIATIVE·demo·report 일관 명시). moat "확립" 주장 0 — *메커니즘 데모*. **다층 검증**: 8 테스트(봉인 disjoint·결정론·재생성·오염 가드 3종·RLVR 채점·obs-only 인터페이스) + demo end-to-end + L3 2/2 APPROVE. 기존 src 무변경 → 442→**450**(+8, 회귀 0), mypy **29**/ruff/build clean. **신규 이니셔티브 `eval-product`**(M5, 기능적 moat 유일 후보). 후속(일부 사람/전략 게이트): 서버측 봉인 인프라·agentic-LLM 어댑터·hosted eval-as-a-service. Acceptance AC1–AC5.

## hard-benchmark
- **2026-07-02** — [`strict-battle`](_archive/2026-Q2/hard-benchmark/06-strict-battle/) (standard, **전투 경제 confound 상환 — opt-in strict_battle #6**, M3-EC4 논문 §5 한계(i)): 기본 전투의 `max(1,...)` 최소데미지가 무추론 attrition 승리를 허용 → **opt-in `strict_battle`**: 비효과(<NEUTRAL) 공격 데미지 **0**(양방향 대칭·유효타는 기존 그대로·**default off=byte-identical** — 기존 수치/논문 전부 유효). numpy `Battle.damage` 단일 choke point + `CritterEnv` kwarg + JAX `JaxEnvConfig.strict_battle`(정적 bool — off 시 jaxpr 문자 그대로) `_gym_damage` 4지점. **매치업 보장(#15)이 strict 를 커버함을 실검증**: 200 seed×{single,multitype} winnability sweep unwinnable 0건(곱 최악 super×0.5≥NEUTRAL). **parity 0**: {commit,noncommit}×{single,multitype} 16 케이스. **scout 실측 = 정직 FALSIFY**: (1) commit 두 구성(grid16/grid10) oracle−type_blind spread delta **+0.00**(resisted 커밋은 legacy 에서도 이미 패배 — strict 가 뒤집을 scripted 결과 없음), (2) non-commit attrition probe(always-attack 무추론)가 **전 gym 클리어, strict on/off 무차이** — attrition 은 중립타 chip+파티 순환+재입장 풀힐로 굴러가고 strict 는 설계상 비효과타만 건드림 → **§5 한계(i) confound 는 strict 로 안 닫힘**(문서·scout 출력에 "confound closed 주장 금지" 라벨 명시). 더 강한 변형(super-effective-only 유효타·재입장 풀힐 제거 등)=기본 규칙 변경이라 **사람 결정 후속**. strict_battle 은 그 후속의 기반 엔진 규칙 + learned/LLM 효과는 미측정 open. 27 신규 테스트(단위 11+parity 16), 650→**677**(회귀 0), ruff clean·mypy 신규 0. L3 **2/2 APPROVE**(4지점 치환 완전성 코드 재확인). evergreen `docs/reference/strict-battle.md`. Acceptance AC1–AC6.
- **2026-07-02** — [`multitype-boss-headroom`](_archive/2026-Q2/hard-benchmark/05-multitype-boss-headroom/) (standard, **다중-타입 보스 헤드룸 — 다중-seed 사전약정 측정 #5**): #4 scout(1-seed Δ+3.4pp)의 본측정. **사전약정(G1 freeze, 데이터 무관)**: (A) `classify_headroom(frac=0.75, k=1.0)`(#3 동일 임계) on 다중-config recurrent PPO, (B) 신규 `headroom.classify_depth` — oracle-fraction 정규화(두 config 천장 상이), `gap>max std ∧ 양쪽 winnable→deeper-robust / gap≤0→not-deeper / else inconclusive`; runs 3(borderline 시 5, **임계 불변** — #3 선례). script(`multitype_boss_headroom.py`)가 규칙을 **데이터 계산 전에 출력**(discipline). **실측**(CPU·recurrent PPO GRU h128·250 iter): 3-run (B) inconclusive(gap +9.0pp, std 15% 안) → 사전약정 확충 경로로 **5-run**(임계 불변) → **최종**: oracle single **5.00**/multi **3.00**(둘 다 winnable), rec 1.60±0.42 vs 0.89±0.36, fraction **32%±8% vs 30%±12%, gap +2.4pp**. **(A) hard-for-memory-agent ROBUST**(mean+std 1.25≪2.25 — 다중-타입도 최강 agent 에게 hard·winnable 유지). **(B) inconclusive — scout 의 1-seed Δ+3.4pp 미생존**(3-run +9.0→5-run +2.4pp 축소, 노이즈 내). **정직 결론**: 숨은 2번째 타입은 **천장-낮춤 레버**(oracle 5.00→3.00, 절대 난이도↑)이지 **상대-깊이 레버로 미입증** — "multi-type 이 학습 agent 에게 더 깊다" **주장 금지**. #17→#18 패턴 재현(1-seed 신호 다중-seed 미생존 — scout→사전약정 2단 구조가 헤드라인 사전 차단). 깊이 질문 open(더 무는 config 는 새 scout 선행). 6 신규 테스트(classify_depth 3-branch+경계), 620→**626**(회귀 0), ruff/mypy clean. L3 **2/2 APPROVE**. evergreen `docs/reference/multitype-boss.md` Measured 섹션. Acceptance AC1–AC6.
- **2026-07-01** — [`multitype-boss-scout`](_archive/2026-Q2/hard-benchmark/04-multitype-boss-scout/) (heavy, **다중-타입 보스 — 더 깊은 추론 레버 (env + JAX parity + scout)**): #3 이 공간/호라이즌 절대 난이도를 settled 하고 grid>16=학습불가로 확인 → 남은 "더 깊은" 레버는 **추론 깊이**: gym 보스가 **숨은 두 번째 방어 타입**(effectiveness=두 타입 곱, obs 는 primary 만 노출 → secondary 를 배틀 결과로 *추론*). 사용자 선택 **de-risked 슬라이스**(env 변경+parity 0+1-seed scout; 무거운 다중-seed 측정은 후속). **opt-in default off → byte-identical**(secondary 를 coords draw *뒤*에 그려 rng 시퀀스 불변; `Region.boss_secondary_types` default `()`). numpy 전투는 이미 다중타입 지원(`multi_effectiveness` 곱) → `battle.py`/`types.py`/`creatures.py` 무변경, 보스에 2 타입만 채움. **JAX**: `jax_env` 가 자체 인라인 전투(jax_battle.py/full 미호출)라 **훈련 경로는 jax_env 만** 다중타입화 — `gym_type2`(MAX_GYMS, sentinel -1) + `_boss_def_eff`=eff[m,d1]·(d2<0?1:eff[m,d2]), player→boss **2 지점만**(boss→player 는 보스 move=primary 라 불변, obs=primary 불변). 독립 포트 jax_battle.py/full 단일타입 유지(범위 밖). oracle 은 `_boss_types` 로 보스 전체 타입 읽어 chart-knowing expert 유지, infer/probe 는 primary 만(hidden 격리). **correctness/parity-first**: `test_jax_multitype_boss_parity.py` numpy↔jax_env **parity 0**(14 케이스 random+gym-clearing+held-out, obs+reward+term+trunc). **scout**(`multitype_boss_scout.py --quick`, 1 seed): parity 0 + recurrent PPO 학습 + 단일/다중 oracle-frac — **정직한 발견**: 숨은 2번째 타입이 **oracle 천장도 낮춤**(single 5.00→multi 3.62, 곱이 super-effective 이점 축소; 둘 다 winnable), 학습 agent 28%→24%, **Δ≈+3.4pp**. **강한 정직 라벨**: **1-seed RAW 신호·robust 임계 없음·multi-seed(≥3) 사전약정 측정=후속 task**·PPO(非SOTA)·CPU·grid16 단일·oracle=proxy(헤드라인 금지). 22 신규 테스트, 592→**614**(+22; 회귀 0), ruff/mypy clean. L3 **2/2 APPROVE**. evergreen `docs/reference/multitype-boss.md`. 후속 시드 `multitype-boss-headroom`(다중-seed 헤드룸 측정). Acceptance AC1–AC6.
- **2026-06-25** — [`memory-headroom`](_archive/2026-Q2/hard-benchmark/03-memory-headroom/) (standard, **env가 강한 *메모리* agent에게도 hard한가 — 절대 난이도**): #1·#2가 "memoryless에 hard"(부분관측 메모리 load-bearing)를 보였고 grid10서 가장 강한 agent(recurrent PPO)가 oracle의 53%(절반 회복)였으므로, env를 **더 깊게**(grid16·5gym·420step·**동일 5×5 view**) 만들어 "*메모리 agent에게도* oracle headroom robust한가"(competitive-analysis "a hard benchmark ❌toy"의 미해결 조각) 측정. JAX env가 config-driven이라 **re-port 불요**(`hard_env_spec()` 헬퍼 추가, 기존 spec/train/eval/PPO/recurrent **byte-identical**), 단 새 shape는 **parity 게이트 먼저**(oracle=numpy·agent=JAX 같은 env 보장): `test_jax_hard_config_parity.py` **parity 0 mismatch**(13 obs key+reward+term+trunc, random+gym-clearing+held-out, num_creatures 양쪽 일치). **scout**(1 seed): grid16서 recurrent PPO 학습(recurrent-baseline scout가 "grid16 A2C 학습불가"로 둔 지점 — memory+PPO 조합이 학습 가능케) → grid16 확정. **freeze 전 pilot**(--quick 2 run ~27s): parity0·rec 학습(24%)·oracle winnable·timing. **실측**(공식 script, CPU·**5 seed·300 iter**, 사전약정 `classify_headroom` frac0.75·k1.0 **freeze 고정**): oracle **4.69**(winnable, ≥0.5·5), feedforward PPO **0.53±0.44=11%**, recurrent PPO **2.01±1.05=43%**, opt-bound(mean+std) **3.06 ≪ 0.75·oracle 3.52** → **(a) hard-for-memory-agent ROBUST**(`hard-and-learnable`). **의미**: 더 깊은 config가 *메모리 agent에게도* 더 hard — recurrent PPO가 grid10(53%)보다 *낮은* 43%만 회복하고 **절대 headroom은 훨씬 큼**(~2.7/4.69 gym 미달 vs grid10 ~0.9/1.94), memoryless는 11%로 더 floor, memory 여전히 load-bearing(rec−ff **+1.49 ≈ 4× ff**). **(b)memory-CLOSES 미발동**(헤드라인 reframe 없음·사람보고 stop 불요). **3 seed(52%±0.97, opt-bound 3.41 vs 3.52 경계선)→5 seed(43%±1.05, opt-bound 3.06 여유)**로 robust 굳힘(single-run 노이즈 교정 문화; 임계 고정·seed만 확충=p-hacking 아님). competitive-analysis "Difficulty(absolute)" **❌toy→◐**(현 baseline class엔 메모리 agent 포함 hard) 격상. **경계(과대 금지)**: 강한 agent=recurrent PPO(**SOTA 아님** — 대형/better-algo/HP-튜닝은 닫을 수도)·CPU·5 seed **고분산(std 1.05)**·grid16 **단일 config**·oracle=scripted proxy·recurrent net param-match 아님(의도적 더 좁음). **다층 검증**: parity 게이트 + pilot + 3→5 seed robust + L3 2/2 APPROVE. 427→**442**(+15 parity·smoke, 회귀 0; 기존 byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(memory-headroom Update)+competitive-analysis(gap+matrix)+INITIATIVE(#3) 갱신. recurrence axis(#1·#2)·메모리 agent 절대 난이도(#3) settled. Acceptance AC1–AC6.
- **2026-06-25** — [`recurrent-ppo`](_archive/2026-Q2/hard-benchmark/02-recurrent-ppo/) (standard, **recurrence가 *PPO* headroom을 닫는가 — #1 A2C 메모리 효과의 PPO 연결**): #1 `recurrent-baseline`이 *A2C 내*에서만 메모리 load-bearing을 보였고 Q1 headroom은 *PPO*로 측정됐으므로, **Q1 exact `default` config**(grid10·**5×5 view**·vary num_types8·1–3 gyms)서 recurrent PPO vs feedforward PPO를 깨끗이 대조. **고난도 sequence-preserving minibatch**: PPO가 (T,B)를 평탄화·**시간축 셔플**(recurrence 깸)하므로 **env축(B)만 셔플·T 보존·`h0`서 GRU 재생**. `jax_train`에 추가(전부 추가만, feedforward/A2C/tuned-PPO/recurrent-A2C **byte-identical**): `recurrent_replay`(loss·테스트 공유 GRU 재생) / `make_recurrent_ppo_rollout`(hidden 스레딩 + h0 노출 + last_value bootstrap) / `recurrent_ppo_loss`(env-축 minibatch clipped surrogate) / `train_recurrent_ppo`. **correctness 먼저**(망가진 recurrent PPO=misleading "메모리 무용"): 결정론 게이트 2종 — (a) rollout logp/values == replay(tol 1e-4, clipped ratio가 1서 시작) + (b) **env-축 permutation 불변**(`replay(perm)==replay()[:,perm]`, 시간축 셔플 0 입증). **freeze 전 pilot**(--quick 2-run ~23s): correctness 통과·rec 학습·timing 현실적(ff 11% vs rec 34%, +0.44>0.16), falsify 없음. **실측**(공식 script, CPU·**3 seed·250 iter**): feedforward PPO(h256) **0.46±0.08=24% of oracle(1.94)** vs recurrent PPO(GRU **h128**) **1.02±0.19=53%**, memory effect **+0.56 > max(std) 0.19 → (a) recurrence-helps-PPO robust**. recurrent net이 *더 좁은데도*(h128<h256) 2배+ → **이득=memory(capacity 아님)**. **A2C 결과가 알고리즘 산물 아님 입증**(ff 18→24%, rec 46→53%; PPO가 양쪽 올려도 메모리 gap 유지). recurrent PPO도 **53%<oracle**(0.75·oracle 1.45 한참 아래) → **(c) headroom-CLOSES 미발동**(헤드라인 헤드룸 유지·사람보고 stop 불요; 메모리가 *일부만* 회복으로 qualify, A2C+PPO 둘 다 robust). **경계(과대 금지)**: PPO≠SOTA·CPU·3 seed·single config·param-match 아님(의도적 더 좁음)·oracle=scripted proxy·**A2C↔PPO는 config 다름**(#1 fixed-3-gym oracle 2.81 vs Q1 vary'd oracle 1.94 → within-config ff-vs-rec gap만 읽기). **다층 검증**: pilot + correctness 게이트 2종 + 4 테스트 + non-vacuity(FF 더 넓은데 floor) + L3 2/2 APPROVE. 423→**427**(+4, 회귀 0; 기존 경로 byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(recurrent-ppo Update)+competitive-analysis(gap register)+INITIATIVE(#2) 갱신. recurrence axis는 이제 **A2C+PPO 둘 다 settled**. Acceptance AC1–AC6.
- **2026-06-25** — [`recurrent-baseline`](_archive/2026-Q2/hard-benchmark/01-recurrent-baseline/) (standard, **메모리는 load-bearing인가 — 부분관측 하 recurrent vs feedforward (Q1 헤드라인 정직 보정)**): `headroom-baseline-strength`(Q1)가 "headroom이 cheap *feedforward* 스케일링에 robust"를 입증하며 남긴 명시 caveat **"recurrent 미배제"**를 직접 측정. 부분관측 scout(시야 줄이기 falsify → 지도-스케일 시사, grid16은 A2C 학습불가 inconclusive)를 거쳐 **정확한 질문=부분관측 하 메모리 load-bearing 여부**로 reframe. `jax_train`에 **recurrent(GRU) A2C** 정식 추가(`gru_init_params`/`gru_step`/`recurrent_policy_value`/`make_recurrent_rollout`/`recurrent_a2c_loss`/`train_recurrent`/`evaluate_gym_clears_recurrent` — GRU hidden을 rollout `lax.scan`에 thread, done시 per-env reset; loss는 in-order (T,B) trajectory에 hidden 재생[A2C는 full rollout·shuffle 없음→정확]). **feedforward 경로 전부 무변경**. `scripts/recurrent_baseline.py`가 부분관측 commit world(grid10·**5×5 view**·3 gyms)서 ff vs rec를 **matched greedy eval**(동일 protocol)로 비교 + 사전약정 규칙(load-bearing iff `rec−ff>max(std)`). **freeze 전 pilot**(3 seed matched): ff 9% vs rec 50%, +1.17 std-separated. **실측**(공식 script, CPU·3 seed): feedforward A2C(h256) **0.50±0.14=18% of oracle(2.81)** vs recurrent A2C(GRU **h128**) **1.29±0.33=46%**, memory effect **+0.79 > max(std) 0.33 → LOAD-BEARING robust**. recurrent net이 *더 좁은데도*(h128<h256) ~2.5× → **이득=memory(capacity 아님)**. **Q1 정직 보정**: robust headroom은 *feedforward 한정*이었고 **recurrence가 부분관측 headroom 크게 회복(18%→46%)** → headroom 상당부분이 *no-memory* 한계. env가 메모리 있는/없는 agent를 선명히 변별(벤치마크 virtue). **경계(과대 금지)**: recurrent도 **46%서 잔존**("풀림/이미 hard" 아님); **A2C 한정**(recurrent PPO=후속, Q1 PPO 깨끗 연결 미확정); 3 seed·CPU·단일 config·param-match 아님·oracle proxy. **다층 검증**: pilot(correctness+3seed matched) + 4 테스트 + non-vacuity(FF 더 넓은데 floor) + L3. 419→**423**(+4, 회귀 0; feedforward byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(memory Update)+competitive-analysis+difficulty-scaling INITIATIVE(Q1 보정) 갱신. 신규 이니셔티브 `hard-benchmark`. Acceptance AC1–AC5.

## difficulty-scaling
- **2026-06-25** — [`headroom-baseline-strength`](_archive/2026-Q2/difficulty-scaling/04-headroom-baseline-strength/) (standard, **강한 baseline에도 oracle headroom이 살아남나? — 절대 난이도 진단 Q1**): 헤드라인 "hard-and-learnable"(PPO≈oracle 21–28%)가 *tiny MLP·짧은 훈련* 산물인지, *강한 baseline*에도 살아남는 진짜 난이도인지 진단 — **비싼 Q2(부분관측 등 spec 변경·JAX 재포트) 전에 가장 싼 진단**(JAX 엔진이 강한 baseline을 싸게 훈련케 함). `jax_train`에 **default-preserving depth 노브**(`init_params`/`apply_policy`가 `depth`; depth=1 byte-identical → A2C/기존 PPO 무변경, depth≥2 trunk 레이어 compile-time 추론) + `PPOConfig.depth`. `ppo_baseline.py --strong`이 capacity×budget 스윕(width 64→256·depth 1→2)의 **best-of-sweep**를 강한 baseline으로, 동일 사전약정 `classify_headroom`(frac=0.75)로 3-branch((a)robust/(b)closes/(c)exceeds) + **non-vacuity 가드**(best-strong>tiny — 망가진 깊은 net의 공허한 robust 차단). **freeze 전 pilot**: config-only 스케일이 held-out 약 2배지만 ~28% plateau, branch (a) 신호, falsify 없음. **실측**(CPU·3 seed): best strong PPO(wide d1/h256)가 oracle의 **41%(default 0.76/1.84)/25%(hard 1.83/7.28)**에서 plateau(opt-bound 0.89/2.05 ≪ 0.75·oracle 1.38/5.46) → 양 config **(a) headroom-ROBUST**. **핵심 정직 발견**: depth 1→2는 **하락**(d2 0.33<wide 0.76)·budget은 i600서 plateau(**i4000≈20M steps도 i600보다 안 나음**) → **병목은 baseline의 capacity/compute 아님**(transfer-capacity-budget "bigger net hurts" 재현). non-vacuity 가드가 깊은 net의 vacuous robust 실제 포착. **경계(과대 금지)**: robust=*cheap feedforward 스케일링 한정* — recurrent/메모리(POPGym/Craftax)·대형·better-algo(RND/world-model)·HP-튜닝 **미배제**(닫을 수 있음), oracle=scripted proxy, 3 seed·CPU·1–2층. **함의**: headroom 실재·약한-net 산물 아님 → 현 baseline class엔 trivially toy 아님(헤드라인 강화); "SOTA agent에도 hard"는 미해결 → **Q2(부분관측)가 더 강한 agent 대비 절대 난이도엔 여전히 동기**(Q1이 "cheap 스케일링부터 배제"라는 순서 정직하게 닫음). **다층 검증**: pilot + budget-plateau 검증(i600–4000) + non-vacuity 가드 + 4 depth 테스트 + L3 2/2 APPROVE. 415→**419**(+4 depth, 회귀 0; depth=1 byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(PPO robust Update)+competitive-analysis(gap register)+INITIATIVE 갱신. Acceptance AC1–AC5.
- **2026-06-25** — [`ppo-headroom-rigor`](_archive/2026-Q2/difficulty-scaling/03-ppo-headroom-rigor/) (standard, **tuned PPO oracle-headroom을 multi-run robust로 — "hard-and-learnable" 헤드라인 신뢰도↑(자율런 KR1-hardening)**): `jax-ppo-tuned`의 single-run "PPO가 oracle의 15–32%만 도달"을 ≥5-run + 사전약정 std-across-runs 결정규칙으로 굳힘(과거 (B) 스레드가 single-run을 4회 노이즈로 교정한 학습 적용). 신규 `src/critter_gym/headroom.py` `classify_headroom`(순수·**numpy-only CI**, frac=0.75·k=1.0 데이터 전 고정: `mean+k·std≤frac·oracle`→`hard-and-learnable` / `mean−k·std≥frac·oracle`→`ppo-closes` / else `inconclusive`) + `HeadroomVerdict` + `ppo_baseline.py --runs` robust verdict(single-run fallback 보존). **freeze 전 pilot**: classify property 7종(3 verdict 경계+ratio+빈입력/oracle≤0 가드) + multi-run end-to-end 입증, 임계 데이터 전 고정. **실측**(CPU·5-run): default PPO **0.52±0.06=oracle 1.84의 28%**(낙관상한 mean+std 0.58 ≪ 임계 1.38), hard PPO **1.52±0.28=oracle 7.28의 21%**(낙관상한 1.80 ≪ 5.46) → 양 config **`hard-and-learnable` robust**(seed 노이즈 아님 입증; single-run 32%/15%이 multi-run 28%/21%로 일관). gap≈0(+0.20/+0.12)·R2 PPO≥A2C 양 config·hard서 PPO(1.52)<type_blind(2.03) 유지. **`ppo-closes` 미발동**(reframe 불요). **정직 caveat**: 5-run(대규모 sweep 아님)·작은 net·CPU·이 예산·oracle=scripted proxy. **다층 검증**: classify property 7 + L3 2/2 APPROVE. 365→372(+7 headroom 단위, CI numpy-only; jax_train/jax_env 무변경), mypy(28)/ruff/build clean. jax-throughput.md(robust 갱신)+DESIGN §3.1.1+INITIATIVE 갱신. Acceptance AC1–AC7.
- **2026-06-24** — [`difficulty-dynamic-range`](_archive/2026-Q2/difficulty-scaling/02-difficulty-dynamic-range/) (standard, **(A) hard 쪽 — 변별 *분해능*↑(동적 범위)**): #43이 *hard*(변별 난이도)로 옮긴 질문 공략. 먼저 `discriminating-difficulty` slug의 freeze 전 pilot이 그 메커닉("oracle 천장/스타터 다양화")을 **falsify**(winnability 정상=oracle이 존재 gym 거의 다 클리어, "천장 0.6"은 max-3 대비 분수·실제 평균 2 gym / 변별 이미 +1.0/gym / 다양화는 랜덤 토너먼트 구조상 무력=고정 챔피언 ~절반 우연 카운터 / 재출현↓는 in-episode 추론 불가하게 해 금지) → 정직 reframe·새 slug. **확실한 레버=동적 범위(gym 수)**. `region.generate_region(min_gyms=None)` opt-in(기본 None=byte-identical 무회귀, 명시 시만 검증, 재출현 pool 보존) + `critter_env min_gyms` + `difficulty_generalization` `discrimination_resolution`/`classify_resolution`(사전약정 spread@max≥2.0·단조·win≥0.70)/`--resolution`(scripted numpy)/`--range-gap`([rl]). **실측**(scripted, held-out, gym-clear-only): oracle−type_blind spread **+1.31(3)→+2.56(5)→+4.88(8)** 단조↑ + winnability 0.88 → **`resolution-up`**. 학습 gap@8(PPO 60k,3run): held-in 1.67/held-out 1.85/**gap −0.19±0.60=`gap≈0-signal`**(분해능↑하며 일반화 유지) — PPO 1.67≪oracle 7.06=변별 headroom 큼. **정직 범위 경계**: 변별 *분해능*(점수 범위↑)이지 "PPO 못 푸는 hard-benchmark"는 **명시적 범위 밖**(future work). **JAX 무변경**(jax_env 3-gym 하드코딩)→후속 `jax-difficulty-report`(R5). **다층 검증**: pilot(메커닉 falsify→reframe) + property 테스트 + L3. 287→294(+7, 회귀 0; min_gyms=None byte-identical), mypy(26)/ruff/build clean. DESIGN §3.1.1 + competitive-analysis 갱신. supersedes `discriminating-difficulty`(pilot-falsified, 미구현 정리). Acceptance AC1–AC8.
- **2026-06-24** — [`difficulty-gap-rigor`](_archive/2026-Q2/difficulty-scaling/01-difficulty-gap-rigor/) (standard, **(A) "hard-and-gap≈0" — #24 약한신호를 multi-run rigor로 정밀화**): M4 JAX 입증 후 갭 register 또 다른 축(난이도)으로 피벗. #24(difficulty-generalization)가 남긴 honest limit(난이도 점 gap이 전부 *한 run 내 per-seed std* 안=저예산 40k·단일run 약한 증거) 해소. `scripts/difficulty_generalization.py`에 `classify_gap`(사전약정 순수함수, `floor=0.3`·`k=1.0` 데이터 보기 전 고정) + `MultiRunGap`(std-**across-runs**) + `train_and_gap_multirun` + `--runs N`. **핵심 통찰**: "gap이 real이냐"의 불확실성은 run간 std지 #24가 쓴 한 run내 per-seed std 아님. env 무변경(JAX 재포트 없음). **freeze 전 pilot이 사전약정 임계 고정 + 타이밍(1.21s/1k step→100k×3×5run≈30분 feasible)**. **실측**(100k, 5run, N16): held-in **floor 훨씬 위**(1.10/1.21/1.54=정책 유능, generalist-mediocrity 아님) + 세 점 모두 **`gap≈0-signal`**(d0 −0.225±0.350/d1 −0.237±0.489/**d2 −0.400±0.896**), d2(12타입·강보스)서 held-in *상승*(1.54)하며 gap≈0 robust(5run). **#24 대비 진짜 업그레이드**(per-seed std→std-across-runs, 비floor held-in). **real-gap 미출현**. **정직 caveat**: gap 약한 음수(난이도 비대칭, std 안 부호 불안정)·**std가 난이도와 함께 커짐**(d2 0.90≈gap 2배, 작은 gap 정밀 배제 못 함)·held-out~1.9/3=현 knob 변별력 부족 가능. **AC7 분기 (a)**: gap-correctness 문제 아님→"hard-and-gap≈0"의 *hard* 쪽(변별 난이도)이 미해결 = 재설계 동기 재정의(단 env 메커닉=JAX 재포트). 281→283(+2: classify 단위[CI numpy-only]+multirun smoke, 회귀 0), mypy(25)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC8.

## jax-throughput
- **2026-06-26** — [`gpu-ec3-result`](_archive/2026-Q2/jax-throughput/13-gpu-ec3-result/) (standard, **M4-EC3 GPU 실측 달성 기록 + bench 배치 상한 정직 수정**): #12 Colab bench를 **사용자가 무료 NVIDIA T4서 실행 → M4-EC3(≥10M steps/s GPU) 실측 확보**. overworld fused-`lax.scan` rollout vmap: **75.9M(b1024) → 271M(b4096) → 952.8M(b16384) steps/s** = **≥10M EC의 95×**, batch와 단조 스케일(numpy 49k·jit-single 64k 참고) → **M4-EC3 달성**. `docs/reference/milestones.md` M4 `⬜→🟢`(EC1 hotpath 4/4 family 포트·EC2 parity 0·EC3 GPU 실측 전부 ✅; doc lag 정직화, 증거 task 포인터 동반) + `jax-throughput.md` §5 item3(GPU "미측정"→"✅ measured", T4 수치) + `competitive-analysis.md` matrix "Speed" 행(JAX vmap ~950M GPU)·tradeoff "Speed" bullet(채택-게이트 갭 "largely closed") 갱신. `scripts/gpu_bench.py` 기본 batch 상한 **65536→16384**(b65536이 65536 procgen 빌드 + 메모리 한계로 무료 T4 멈춤·full-episode OOM → 기본 제외; 16384가 이미 ~950M로 throughput 포화; 로직·시그니처 무변경·`--quick` 무영향). **정직 경계(과대 금지)**: overworld slice 한정·**single run**·**free T4**; full-episode env GPU 수치는 분기 많은 step의 free-T4 컴파일 한계로 미확보(단 **CPU 22M/s로 EC 이미 초과** → GPU도 초과 자명, 정밀치는 better-HW minor 후속); peer 압도 주장 안 함(동일 GPU-벡터화 class 진입). **다층 검증**: 사용자 실측 + CPU sanity + L3 2/2 APPROVE(정직성 축). src 무변경 → **442 무회귀**, mypy(28)/ruff/build clean. **M4 사실상 완료** — 남은 프로젝트 게이트=공개(M3-EC4/EC5·사람)·M5 eval 제품(전략·사람). Acceptance AC1–AC5.
- **2026-06-25** — [`gpu-bench-colab`](_archive/2026-Q2/jax-throughput/12-gpu-bench-colab/) (standard, **M4-EC3(≥10M steps/s GPU) enabler + 로컬 Apple GPU 비가용 박제**): GPU 측정은 하드웨어 게이트. 이 세션에서 **사용자 맥(M5 Pro·macOS 26.5)의 로컬 GPU를 jax-metal로 실측**: **0.1.0**(jax/jaxlib 0.4.26)·**0.1.1**(jax/jaxlib 0.4.34) 둘 다 `METAL(id=0)` 인식·단순 op(vmap+scan+scatter+gather+where) 정확 실행 OK지만, **fused `lax.scan(vmap(env_step))`(공정 GPU 측정·RL 학습에 필요한 패턴)에서 둘 다 NSException 크래시**(Metal PJRT 플러그인 op-coverage 한계, **우리 코드 문제 아님**); per-step 루프만 ~23k steps/s(dispatch-bound, CPU보다 느림) → **로컬 Apple GPU는 JAX로 EC 측정 불가**. 신규 **`scripts/gpu_bench.py`** — 기존 bench(`bench_throughput.py`)의 per-step python 루프(dispatch-bound, GPU 과소평가)를 **fused `lax.scan` rollout**(overworld + full-episode, batch 스윕, numpy/jax-single/jax-vmap 3행)로 교체한 GPU-공정 측정. 기존 `jax_overworld`/`jax_env`/`jax_train` 심볼만 import(**src 무변경**). CPU sanity: overworld vmap **~480M/s**·full-episode **~22M/s** @b1024(vmap≫numpy·전부 유한·크래시0). 신규 **`scripts/colab_gpu_bench.ipynb`** — 무료 Colab/Kaggle T4용 **15-cell 노트북**(GPU 확인→토큰[getpass]→clone[public+PAT]→`pip install "jax[cuda12]"`+repo 설치→backend 확인→`gpu_bench.py` 실행→`train_ppo` train throughput→복붙용 요약; **nbformat valid**·ruff clean). 측정 실행=사용자 Google 로그인(게이트). **정직 경계**: notebook은 GPU 부재로 **로컬 CPU-sanity까지만 검증**(Colab 실제 실행 미검증)·repo 공개전(clone 경로 안내)·**EC 달성은 사용자 Colab 숫자 회수 후 별도 기록**(도구+음성결과 박제이지 EC 달성 주장 0). **다음 세션 로컬 Metal 재시도 방지**를 위해 jax-throughput.md §5에 박제("do not re-attempt"). src 무변경 → **442 무회귀**, mypy(28)/ruff(ipynb 포함)/build clean. L3 2/2 APPROVE. Acceptance AC1–AC5.
- **2026-06-25** — [`v1-results-packaging`](_archive/2026-Q2/jax-throughput/11-v1-results-packaging/) (standard, **KR3 결과 패키징 / v1.0.0-rc 준비**): 흩어진 강한 결과를 front-facing 문서로 통합 + **1-command 재현** + 버전 1.0.0-rc. **제품 코어(src) 무변경 — docs/scripts/meta만**. (1) 신규 `scripts/reproduce_results.py [--quick] [--runs N]` — `bench_throughput.py`+`ppo_baseline.py` 오케스트레이션으로 throughput 표(numpy/jax-single/jax-vmap)+oracle-headroom 표를 **라이브 재생성**(수치 하드코딩 0·honest framing 보존). (2) `README.md` "What it measures"에 **competitively-fast(JAX vmap 27–1047×·4/4 family parity 0)** + **hard-and-learnable(tuned PPO 21–28% of oracle, 5-run robust·hard서 PPO<type_blind capability ladder)** 헤드라인 통합 + 재현 명령 + Release status(1.0.0-rc + 잔여 게이트 GPU[M4-EC3]·arXiv[M3-EC4]·OSS[M3-EC5]=사람). (3) `docs/paper/critter-gym.md`: §2 throughput 갱신 + **신규 §6 "Throughput: a parity-proven JAX port"**(parity 0·multiplier 표·4 family·trains·GPU 미측정 boundary) + §4 PPO oracle-headroom subsection(표·사전약정 R1/R2/R3) + §5 note + §8 limitations + §9 conclusion(JAX port=done) + §7/8/9 renumber. (4) `docs/paper/README.md` figure→source map 4 신규 행. (5) `pyproject.toml` 0.0.1→**1.0.0rc1**(build이 1.0.0rc1 아티팩트 생성; **태그/publish 안 함**). **정직성**: 모든 정량 주장에 caveat(CPU·vmap-only·single jit slower·oracle proxy·5-run·GPU 미측정), 수치 라이브 재생성으로 fabricate 0. **공개(OSS 리스팅·arXiv·태그)는 사람 게이트 — 직전 정지**. **freeze 전 pilot**: reproduce_results --quick 두 표 재생성·수치 라이브 검증. 415 무회귀(src 무변경), mypy(28)/ruff clean, build→1.0.0rc1. L3 2/2 APPROVE(정직성 축 포함). Acceptance AC1–AC6.
- **2026-06-25** — [`jax-duel-integration`](_archive/2026-Q2/jax-throughput/10-jax-duel-integration/) (standard, **duel(C) family를 jax_env에 통합 — 4/4 family 벡터화 완성(자율런 KR2 마무리)**): 마지막 family **duel(C)** = type-AGNOSTIC RPS/stamina 배틀(type chart 무용)을 별도 `duel_battle_branch`로 `make_jax_env(JaxEnvConfig(family=duel, commit=False))`에 통합, numpy `DuelEnv(commit_battles=False)` 미러. **duel 고유 3요소**: (1) **동시 데미지**(속도순 없음·양쪽 `take_damage` 무조건→한 턴 동시 기절=loss), (2) **raw stat 데미지** `floor(atk×(1+charge))`(defense/eff/min-1 clamp 없음·`_damage` 미사용), (3) **duel 전용 charge obs**(`player_charge`/`enemy_charge` 비-0 → `encode_obs` family-aware, 비-duel 0-mask byte-identical). 보스 결정론(echarge≥1→ATTACK else CHARGE·GUARD 안 함). overworld=family A CATCH 재사용. `JaxEnvState`에 `player_charge`/`enemy_charge` 필드 + `battle_turn`을 duel turn 카운터로 재사용(40턴 stalemate cap=loss; duel config서 noncommit branch 미도달이라 안전). **parity 0 mismatch**: 13 obs(charge 2키 포함)+reward+term+trunc, full 에피소드, fixed+vary, 5 정책(random·gym-seeking·charge-exploit·all-GUARD stalemate·**scripted-optimal**[보스 결정론 악용→win·evolve]) + **non-vacuity 가드**(`test_duel_battery_is_non_vacuous`: 3 액션·turn-cap loss·win·evolve 6신호 실자극). **freeze 전 pilot**: 19,200 steps 0 mismatch + **always-attack는 0승 포착**(탱키 보스가 charge-0 공격 out-trade→승리엔 RPS 필요)→scripted-optimal로 win 60·evolve 24. **실측**(CPU·single·vmap-only): numpy 123k/s · **jax vmap 4.96M(b=1024)=40× / 10.15M(b=16384)=83×**. **4/4 family(A/B/C/D) 전부 벡터화 = full breadth**. **정직 범위**: CPU·single·vmap-only·GPU(M4-EC3)만 남은 M4 항목. **다층 검증**: pilot(0 mismatch+RPS·동시기절·교착·win/evolve) + 19 테스트 + non-vacuity 가드 + L3 2/2 APPROVE(라인별 numpy↔JAX 대조). 396→**415**(+19 importorskip parity, 회귀 0; family A/B/D byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(duel Update)+DESIGN §4+competitive-analysis+INITIATIVE 갱신. Acceptance AC1–AC6.
- **2026-06-25** — [`jax-family-integration`](_archive/2026-Q2/jax-throughput/09-jax-family-integration/) (standard, **forage(B)+muster(D) family를 jax_env에 통합 — family 폭(자율런 KR2)**): family A(critter)만 JAX화돼 있던 `jax_env`를 `make_jax_env(JaxEnvConfig(family=…))`로 **forage**(B: contact-collect overworld·CATCH inert·collect시 gym-enter 상호배제)·**muster**(D: CATCH+party 공격 부스트)까지 미러. `JaxEnvConfig.family`(0/1/2) + `JaxEnvState.party_atk_boost`. **muster 핵심 상호작용**: 부스트는 attack→battle damage→enemy_hp obs로 흐르고 `evolve()`가 `attack=form.attack`로 **리셋**(creatures.py:97) → 단순 `base+12×caught` 아님 → `party_atk_boost` 누적기(catch +12 전멤버/evolve시 해당 멤버 0, `attack_of`/`reset_boost_on_evolve`)로 정확 미러, 양 battle branch(commit fight + noncommit) 적용. **parity 0 mismatch**: 13 obs+reward+term+trunc, full 에피소드, fixed+vary, random+gym-clearing+catch-then-gym 정책, **24 tests** + **non-vacuity 가드**(`test_muster_buff_actually_exercised`가 catch[부스트]·evolve[리셋] 양 경로 실자극 증명). 3/4 family(A/B/D=type-matchup 배틀) 벡터화. **duel(C)는 type-agnostic RPS/stamina 별도 배틀 엔진=후속 포트**(jax_battle 선례). **정직 범위**: family A/B/D·non-commit·CPU·vmap-only·GPU/duel 후속. **다층 검증**: pilot(parity 0+muster 상호작용) + 24 테스트 + non-vacuity 가드 + L3 2/2 APPROVE. 372→396(+24 importorskip parity, 회귀 0; family A byte-identical), mypy(28)/ruff/build clean. jax-throughput.md(family Update)+DESIGN §4+INITIATIVE 갱신. Acceptance AC1–AC8.
- **2026-06-25** — [`jax-ppo-tuned`](_archive/2026-Q2/jax-throughput/08-jax-ppo-tuned/) (standard, **tuned PPO 베이스라인 + oracle-headroom 정량화 — 벤치마크 "결과 표" 실체(자율런 KR1)**): `jax_train`의 A2C-lite(truncated returns·단일 grad step)를 **proper PPO**로 격상 — `gae`(순수함수·reverse scan·value bootstrap) + `make_ppo_rollout`(value·logp_old 수집) + `ppo_loss`(clipped surrogate ε + adv-norm + value MSE + entropy) + `train_ppo`(GAE(λ)→K epoch×minibatch jit update + Adam) + `evaluate_gym_clears`(final gyms_defeated=oracle와 동일 지표). A2C `train`/`evaluate`·`jax_env` **무변경**(추가 API). `scripts/ppo_baseline.py`가 default+hard(고-gym) commit-mode서 held-out PPO를 scripted oracle ceiling과 대조. **사전약정 결정규칙**(데이터 전 고정): R1 학습·R2 PPO≥A2C·R3 PPO<0.75×oracle⇒headroom. **freeze 전 pilot**: gae property 3종(numpy ref·γ1λ1↔MC·λ0↔1-step TD) + PPO 학습 + headroom 파이프라인 입증, falsify 0. **실측**(CPU·single·200iter): default PPO held-out **0.59=oracle 1.84의 32%**(A2C 0.78), hard PPO **1.06=oracle 7.28의 15%**(A2C 1.88 거의붕괴)·held-in≈held-out(gap≈0=일반화). **R1 학습·R2 PPO≥A2C·R3 hard-and-learnable 양 config**(reframe 미발동). **striking 정직 발견**: hard서 tuned PPO(1.06) < 비추론 type_blind(2.03) — capability ladder oracle 7.28 ≫ type_blind 2.03 > PPO 1.06 선명, 현 baseline은 oracle 추론 못 깸. **정직 caveat**: single-run·작은 net·CPU·이 예산 한정(더 큰 compute는 PPO↑→headroom은 이 예산 기준)·oracle=scripted ceiling proxy·gym-clear gap=single-run 노이즈→multi-run rigor 후속. **다층 검증**: gae property + PPO smoke + L3 2/2 APPROVE. 360→365(+5 importorskip, 회귀 0; A2C/jax_env 무변경), mypy(27)/ruff/build clean. jax-throughput.md(PPO Update)+DESIGN §3.1.1+competitive-analysis(gap register)+INITIATIVE 갱신. Acceptance AC1–AC8.
- **2026-06-25** — [`jax-noncommit-env-integration`](_archive/2026-Q2/jax-throughput/07-jax-noncommit-env-integration/) (standard, **non-commit full battle을 jax_env에 통합 — env 기본 경로까지 full-episode 벡터화**): `jax-battle-full`이 standalone(parity0)으로 포트한 non-commit 배틀(party+SWITCH+force-switch+party-wipe)을 **`jax_env`의 battle branch에 연결** — 이제 `make_jax_env(JaxEnvConfig(commit=False))`가 numpy `CritterEnv(commit_battles=False)`(env **기본** 경로 `CritterGym-v0`/`-procgen-v0`)를 미러. `JaxEnvConfig`에 `commit`/`potions`/`battle_max_turns`, `JaxEnvState`에 `items`/`battle_turn` 추가; `overworld_branch`가 진입 시 commit 여부로 commit_window/items/battle_turn 설정; 신규 `noncommit_battle_branch`(action 매핑 `<4`→MOVE·`4`→SWITCH[cyclic next-alive]·`5`→wasted ITEM + Phase1/2/3 + party-wipe/boss-dead/battle-trunc + 승리시 gym_defeated·레벨·evolve); `step`이 `config.commit`(compile-time)으로 정적 분기(commit=True default=byte-identical). **freeze 전 pilot이 두 가정 코드로 확정**(보스 항상-MOVE=`scripted_opponent` 구조적·battle-trunc 비발산) + **핵심 발견**: Phase1 SWITCH는 cyclic(`_next_alive_player`) vs Phase3 force-switch는 first-in-order(`_next_alive`)=서로 다른 순서 → JAX서 cyclic loop / `argmax`로 구분 미러(놓쳤으면 parity 깨질 off-by-one). **parity 0 mismatch**: 13 obs+reward+term+trunc, full 에피소드, fixed(num_types3)+vary(8), **4 정책**(random·gym-clearing·switch-heavy·never-attack[패배=force-switch+party-wipe+무보상 종료]) + **non-vacuity 가드**(`test_force_switch_actually_exercised`가 force-switch·wipe 실자극 증명; coverage probe 9battle·21switch·7win·5evolve). **실측**(CPU·single·vmap-only): numpy 139k/s · **jax vmap 5.08M(b=1024)=36× / 8.35M(b=16384)=60×**(commit full-env 31×와 동급=동일 overworld·다른 배틀 경제). **정직 범위**: family A·CPU·single·vmap-only(단일 jit는 손해)·potion inert(action space 유효 ITEM 미방출=정확 미러)·GPU/다른 family/tuned PPO 후속. **다층 검증**: pilot(가정·순서차) + 32 parity 테스트 + L3 2/2 APPROVE. 328→360(+32 importorskip, 회귀 0; commit byte-identical), mypy(27)/ruff/build clean. jax-throughput.md(§4 Update+§5 #1)+DESIGN §4+INITIATIVE 갱신. Acceptance AC1–AC8.
- **2026-06-24** — [`jax-battle-full`](_archive/2026-Q2/jax-throughput/06-jax-battle-full/) (standard, **non-commit full battle JAX 포트 — 핫패스 배틀 남은 절반**): `jax_battle.py`(commit 챔피언)에 이어 **non-commit full battle**(`Battle(commit_mode=False)`: party P + SWITCH + ITEM(potion) + 기절 force-switch + party-wipe 종료)을 functional JAX로. 신규 `src/critter_gym/jax_battle_full.py`(`FullBattleState`/`FullBattleParams` + `full_battle_step`[1턴: Phase1 switch/item, Phase2 speed-order moves(빠른쪽 먼저·tie A·기절 attacker skip), Phase3 force-switch, party-wipe terminal — 전부 branch-free `jnp.where`, 동적 party gather + `argmax` next-alive] + `params_from_parties`/`initial_state` bridge). **freeze 전 pilot이 parity 게이트 통과**: numpy `Battle(commit_mode=False)`(starter 3 vs boss 1) 대비 **0 mismatch** — 배터리(attack/switch/item-heal/force-switch/party-wipe/truncation) + random 40 seed(fixed+vary chart), 매 턴 party_a_hp·active_a·boss_hp·winner·turn·done 일치. **실측**(CPU·single run): numpy 96k/s · **jax vmap 43.5M/s(b=1024)=452×**(순수 산술이라 벡터화 효율↑). **한계효용 정직**: gym-boss 실경로는 commit-mode(이미 포트)라 본 포트=env 기본(non-commit) 경로 커버 + M4 배틀 완전성이지 load-bearing 교체 아님; non-commit full-env 통합은 별도 후속(jax_env 무변경, standalone). 310→328(+18 importorskip parity, 회귀 0; `__init__` 미import), mypy(27)/ruff/build clean. jax-throughput.md(§5 #1 ✅)+DESIGN §4 갱신. Acceptance AC1–AC7.
- **2026-06-24** — [`jax-difficulty-report`](_archive/2026-Q2/jax-throughput/05-jax-difficulty-report/) (standard, **R5 — jax_env config화로 고-gym 동적 범위 재포트 + parity + jax_train 가속**): `difficulty-dynamic-range`가 numpy서 입증한 고-gym(8) 변별 설정을 JAX 벡터화로. `jax_env`가 `_GRID/_MAX_STEPS/_MAX_GYMS=3/_BOSS_*` 하드코딩이라 못 돌리던 것을 **`JaxEnvConfig`+`make_jax_env(cfg)` factory**(JAX static-shape→cfg는 클로저 캡처 compile-time 상수)로 해소. **module-level `jax_env_step`/`jax_reset`/`encode_obs`/`make_env_step`=default-config 인스턴스로 보존**(기존 import·parity·bench byte-identical). `jax_train` config-aware(`EnvSpec`/`default_env_spec`/`difficulty_env_spec`, obs_dim 동적[patch 크기 의존]). **freeze 전 pilot**: 고-gym parity **0 mismatch**(grid6·8gym·patch 11×11>grid·num_types12·super_mult3·boss150/16, obs 13키+reward+term+trunc, random+gym-clearing, fixed+held-out) + 기존 parity 3종 무회귀(R2). **실측**(CPU·single run): 고-gym 학습 jit/vmap OK(곡선 상승) / **~196k env-steps/s vmap vs sb3 ~3.1k = ~63× FASTER**(고-gym 발산으로 default ~170×보다 배율↓, 정직). `jax_rl_demo --difficulty`. **정직 범위**: family A commit·고-gym 재포트지 difficulty 전체 JAX화 아님(scripted resolution arms는 env peek이라 numpy 유지)·GPU/tuned PPO 후속. **다층 검증**: pilot(R1 parity·R2 무회귀·R3 tracer·R4 speed) + 16 parity 테스트 + L3. 294→310(+16 importorskip, 회귀 0; default config byte-identical), mypy(26)/ruff/build clean. jax-throughput.md(R5)+DESIGN §4 갱신. Acceptance AC1–AC8.
- **2026-06-24** — [`jax-rl-demo`](_archive/2026-Q2/jax-throughput/04-jax-rl-demo/) (standard, **M4 마감 — JAX 벡터화 env로 실제 학습 1회 데모("속도 실재" 가시화)**): #40/#41/#42가 입증한 벡터화 surface를 *데모*로 전환 — 벤치 숫자 너머 "실제로 빠르게 학습됨". 신규 `src/critter_gym/jax_train.py`(JAX-native A2C: `flatten_obs`[OBS_DIM=38]+tiny MLP+manual Adam+region-bank auto-reset+`lax.scan` rollout+jit, 전부 on-device vmap+jit) + `scripts/jax_rl_demo.py`(학습곡선+throughput+sb3 비교+held-out eval) + `tests/test_jax_train.py`(importorskip smoke 4). **설계 핵심**: sb3로 감싸면 step마다 host↔device 넘나들어 vmap 속도 소실 → JAX-native 최소 루프가 정직한 데모. env/jax_env **무변경**(parity 보존). **freeze 전 pilot이 사전약정 결정규칙(R1 `mean_late−mean_early≥std_late`, `learning_verdict` 코드로 동결)으로 분기 (a) 기계적 확정 — 사후편향 차단.** **실측**(CPU·single run): 학습곡선 **상승**(reward/step 0.0068→0.0513, ep_return~1.8→~10.0, rise 0.041≫std_late 0.003=**분기 (a) 학습+빠름**) / 학습 throughput **~0.66M env-steps/s**(1.23M steps/~1.9s, 별 run ~1.1M) vs 기존 numpy/sb3 ~3.8k = **~170× FASTER**(R4 부등식 성립=on-device vmap lock-step) / held-out **1.44** vs held-in **1.00**=**gap≈0**(seed split, (A) no-memorization 일관). **정직 framing**: A2C-lite(tuned PPO 아님)·CPU·single run·reward/step proxy=신호, sb3 baseline=기존 단일-env 경로(다중프로세스면 비율↓ but vmap 미만)·GPU 미측정·env config 하드코딩. **다층 검증**: pilot(R1/R2/R4) + L3 plan-reviewer가 A2C 수식·Adam·returns 부호·auto-reset·OBS_DIM·evaluate 마스킹 코드 정합성 확인. 283→287(+4 importorskip smoke, 회귀 0), mypy(26)/ruff/build clean. jax-throughput.md(§4 Update+§5)+DESIGN §4+competitive-analysis 갱신. Acceptance AC1–AC8.
- **2026-06-24** — [`jax-env-integration`](_archive/2026-Q2/jax-throughput/03-jax-env-integration/) (standard, **M4 — 벡터화 full-episode env step (overworld+commit-battle 합성)**): `jax_overworld` + `jax_battle`를 **하나의 full-episode env step**으로 합성 — RL 루프가 JAX 엔진 실소비 가능. 신규 `src/critter_gym/jax_env.py`(`JaxEnvState` pytree + `jax_env_step(state,action)→(state,obs,reward,term,trunc)` `lax.cond` mode dispatch + `jax_reset` numpy bridge + `encode_obs` 13키[local_patch egocentric pad+dynamic_slice]) + `tests/test_jax_env_parity.py`(importorskip, 실제 `CritterEnv(commit_battles=True)` 대비 full parity) + bench full env 행. **freeze 전 pilot이 AC7 분기 (a) 목표 확정 + 버그 2건 포착**(jnp 인덱싱·battle중 NOOP/SWITCH champion 미공격). **실측**: jit OK / **full parity 0 mismatch**(obs 13키 전체[local_patch 포함]+reward+term+trunc, random+gym-clearing 정책, fixed+vary) / numpy 130k/s · **jax vmap 34×(b=1024)~73×(b=4096)**(full-episode 제어흐름 발산으로 슬라이스보다 배율↓, 정직 보고). **AC7 분기 (a) 완성**(pilot 미검증 local_patch까지 달성). **다층 검증이 버그 3건 포착**: pilot 2 + 구현중 가변gym(`gym_active` 마스크) + **L3 reviewer가 truncated 독립성 갭**(numpy term/trunc 독립계산=둘다 True 가능, 마지막 gym을 max_steps째 격파) → numpy 정확 미러링 수정 + edge 테스트. family A commit-mode·full obs(B/C/D·non-commit full battle은 후속). 263→281(+18, 회귀 0), mypy(25)/ruff/build clean. jax-throughput.md(§4·§5) + competitive-analysis 갱신. Acceptance AC1–AC8.
- **2026-06-24** — [`jax-battle-port`](_archive/2026-Q2/jax-throughput/02-jax-battle-port/) (standard, **M4 — commit-mode 챔피언 battle JAX 포트(핫패스 어려운 절반)**): `jax-hotpath-foundation`(overworld)에 이어 battle sub-MDP 포트. **commit-mode 챔피언 경로**(gym-boss 실제 경로, `CritterGym-commit-v0`)를 functional JAX로: 신규 `src/critter_gym/jax_battle.py`(`ChampionBattleState`/`ChampionBattleParams` pytree + `champion_battle_step`(lax.cond 속도순 + where faint/terminal) + `eff_matrix`/`params_from_creatures` numpy bridge) + `tests/test_jax_battle_parity.py`(importorskip, 실제 `Battle(commit_mode=True)` 대비 parity) + bench battle 행. **freeze 전 pilot이 AC7 분기 (b) 확정 + hp 음수 parity 버그 포착**(numpy `take_damage` `max(0,·)` 클램프 누락→`jnp.maximum` 교정). **실측**(CPU·single run): jit OK / **parity 0 mismatch**(fixed 45 + vary 24 config: champ_hp·boss_hp·winner·turn·done) / numpy 112k/s · **jax vmap 117M/s(b=1024)=1047×**(순수 산술이라 overworld 186×보다 벡터화 효율↑). **정직 framing: 이득 vmap 한정**. **AC7 분기 (b)**: commit-mode 포트 + full(switch/item/multi-creature non-commit)은 후속 `jax-battle-full` 분리(scope balloon 방지). overworld+commit-battle=핫패스 load-bearing 대부분. + `jax_overworld.py` 1줄(직전 task latent UP037 lint 정정; 프로세스 학습: mypy 수정 후 ruff 재실행 누락). 210→263(+53, 회귀 0), mypy(24)/ruff/build clean. DESIGN §4 정신 + jax-throughput.md + competitive-analysis 갱신. Acceptance AC1–AC8.
- **2026-06-24** — [`jax-hotpath-foundation`](_archive/2026-Q2/jax-throughput/01-jax-hotpath-foundation/) (standard, **M4(Throughput/JAX) 착수 — overworld 슬라이스 JAX 포트 토대 + de-risk**): (B) 종결 후 피벗(M4 override, (B)의 M5-enabler-before-M3 선례와 동일 functional-readiness-first). env step이 Python dict/mutable라 직접 jit 불가 → **overworld step(move+catch/contact-collect, battle 제외)만 functional JAX 포트**해 핵심 미지수 2개를 de-risk. 신규 `src/critter_gym/jax_overworld.py`(`OverworldState` pytree + branch-free `overworld_step` family A/B + `state_from_region` numpy bridge + `make_step_fn` jit) + `tests/test_jax_parity.py`(importorskip, 실제 `CritterEnv`/`ForageEnv` 전이 대비 parity) + `scripts/bench_throughput.py`(정직 framing) + `pyproject [jax]` extra(코어 numpy-only 유지). **freeze 전 pilot이 AC7 분기 (a) 확정**. **실측**(단일머신·single run·CPU): ① jit 컴파일 OK(family A·B) ② **parity 0 mismatch**(동일 seed→동일 trajectory, 재현성 북극성 #3 유지) ③ throughput — numpy ~410k/s · **jax single 55k/s(0.13×=더 느림)** · **jax vmap 26.5M(b=1024)→76.5M(b=16384)=186× numpy**. **정직 framing: 이득은 전적으로 vmap 벡터화, 단일 env는 손해**(헤드라인 금지). CPU vmap만으로 M4-EC3 ≥10M GPU 목표 7.6× 초과(GPU는 범위 밖). **feasibility verdict=양성**→후속 `jax-battle-port`. caveat: battle 미포트(M4-EC1 *토대*, 부분)·CPU·single run·int32(x64 off, env 범위 무해)·난이도 작업이 메커닉 바꾸면 재포트(R4). 199→210(+11 parity, 회귀 0), mypy(23)/ruff/build clean. DESIGN §4 + 신규 `explanation/jax-throughput.md` + competitive-analysis 갭 register 갱신. Acceptance AC1–AC8.

## env-core
- **2026-06-24** — [`duel-fewshot-adapt`](_archive/2026-Q2/env-core/33-duel-fewshot-adapt/) (standard, **(B) duel 제로샷 불가 *메커니즘 입증* + few-shot 적응 SLOW — (B) 스레드 종결**): #32가 국소화한 frontier(duel) 공략. 사전 탐색이 원인 발견 → 입증: duel RPS가 의존하는 `charge` obs가 train 3 family서 **항상 0(degenerate)** → gradient 0 → **제로샷 duel 전이 원리적 불가**. `charge_trace`+`test_charge_degenerate_in_train_families`(numpy-only, 결정론: train charge≡0/duel battle>0)로 박제. **일반 명제**: genre 전이는 새 메커닉이 *train서 degenerate한 obs*에 의존하면 제로샷 불가. + `fewshot_adapt_curve`+`--fewshot`(held-out duel fine-tune, held-out eval seed≠adapt seed). **실측 곡선**(base 150k, 5run): zero-shot **0.65±0.20** / 25k 0.59 / 50k 0.76 / **100k 1.45±0.48** → **사전약정 verdict=SLOW**(≤50k는 z₀+σ₀ 안=노이즈; 100k에야 ~2배 상승). 단일-seed pilot 50k≈1.6은 또 노이즈→**multi-run 4번째 교정**. **결론: duel은 적응으로 닿지만 *느림*(~100k=base 2/3) = RPS는 거의 새로 배우는 진짜 새 skill, 빠른 전이 아님.** **(B) 스레드 종결**: 메커닉 이웃 제로샷 전이 / 구조적 새 배틀시스템(duel)은 제로샷 *원리 불가*(degenerate)+느린 few-shot — sharply characterized partial result(open도 solved도 아님). 제로샷≠few-shot 구분. env 코어 무변경(scripts continue-learning). 197→199(+2: charge 가드 numpy-only + fewshot smoke, 회귀 0), mypy(22)/ruff/build clean. DESIGN §3.1.1 + genre-generalization.md 갱신. Acceptance AC1–AC7.
- **2026-06-24** — [`transfer-budget-recovery`](_archive/2026-Q2/env-core/32-transfer-budget-recovery/) (standard, **(B) 예산으로 held-in RECOVERY + confound-reduced gap — "전이는 메커닉 이웃 안, duel로는 실패"**): #31이 보류한 probe(더 큰 예산) 실행. `budget_ladder_configs`+`--budgets`(baseline-net, 용량 배제). **실측 사다리**(5run): 250k 2.44→**400k/500k 2.75±0.39**(held-in PLATEAU ≈2.75, 회복임계 2.5 통과=**사전약정 RECOVERY**; 단일-seed pilot 1.4~2.2는 또 노이즈→**multi-run 3번째 교정**). RECOVERY라 AC3 트리거 → 회복예산(400k)서 **full-LOO confound-reduced gap 재측정**(`loo_multirun`): held-out **critter −1.08±0.73·forage −1.48±0.51·muster −0.12**(≤0=전이 OK) vs **duel +1.73±0.61**(robust 양수, std≪gap). **held-in 회복(2.0~2.75=평범 아님) 상태라 generalist-mediocrity 아님 = 진짜 전이 실패**(duel만 구조적 다른 배틀시스템). **정직 결론: (B)는 real but structurally bounded — 학습 정책이 메커닉 이웃(수집+타입상성) 안에선 전이, *진짜 다른 게임시스템(duel)*으론 실패. open frontier를 cross-배틀시스템 전이로 국소화.** caveat: 음수 gap은 held-out 난이도 비대칭도 반영(forage≈4.0)·깨끗한 신호는 duel·held-in 천장 2.75<#26 2.94·단일 config·N16. 196→197(+1 smoke, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 + genre-generalization.md 갱신. Acceptance AC1–AC7.
- **2026-06-23** — [`transfer-capacity-budget`](_archive/2026-Q2/env-core/31-transfer-capacity-budget/) (standard, **(B) 용량×예산 동시 스케일 — PARTIAL 회복, 예산은 lever·용량은 아님**): #30(net만↑·50k=underfit)·#28(예산만↑·150k) 사이의 안 해본 점=용량+예산 *동시* 스케일. `held_in_sweep`+`SweepRow`+`--sweep`(muster anchor fold multi-run, 천장 기준선 표 + 사전약정 회복임계 2.5). **실측**(5 run): baseline-net **150k 2.07±0.62 → 250k 2.44±0.35**(std 조임, 2.5 근접) / big-net@250k **1.87±0.39**(robust 하락). **사전약정 verdict=PARTIAL**(2.44>2.07 천장,<2.5). 두 robust 발견: ① **예산이 held-in 계속 올림**(2.07→2.44)→**#28 "compute 병목 아님"을 부분 정정**(그건 저예산 조기결론) ② **용량(큰 net)은 robust 해로움**=lever는 예산이지 용량 아님. **가장 긍정적 점**: baseline@250k held-in 2.44(평범 아님)·held-out 2.49·**gap −0.05≈0** — held-in 비평범+gap~0인 첫 점(단일 fold/config·큰 std=주장 아님). AC3 조건부: held-in<2.5(RECOVERY 미달)→full-LOO gap 재측정 **보류**(다음 probe=더 큰 예산, 수확체감 감시). **경계 미종결**(예산 cheap-ish 아직 오름·용량 배제). 195→196(+1 smoke, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 + genre-generalization.md 갱신. Acceptance AC1–AC7.
- **2026-06-23** — [`transfer-skill-policy`](_archive/2026-Q2/env-core/30-transfer-skill-policy/) (standard, **(B/a') 정책·obs 개선으로 held-in 끌어올리기 — 정직한 음성**): #28이 좁힌 "남은 직접 경로"(정책/obs 표현 개선)를 직접 검증. `train_and_transfer`에 개선 노브 `net_arch`+`scale_obs`(+`--improved`) 추가 + `_ScaleObs`(결정론적 대형키 스케일). **pilot이 whole-obs `VecNormalize`가 범주형 obs(in_battle/local_patch/type) 망쳐 *해롭다*를 밝힘→제외**(AC7 분기), 개선설정=net256+선택스케일로 정련. **실측**(baseline vs improved, 50k×5run, N16): 개선설정이 widened held-in을 **못 올리고 4 fold 전부 오히려 낮춤**(muster 1.73→1.15·duel 1.74→1.10·critter 0.86→0.65·forage 0.92→0.68; drop 대부분 run-std 초과=robust) — net256+스케일이 50k서 **underfit**. **정직 음성 verdict**: compute(#28)도 이 정책/obs 레버도 held-in을 #26(2.9) 쪽으로 못 올림 → **generalist-mediocrity confound stubborn 잔존**, (B) 여전히 신호. 제거엔 더 깊은 작업(아키텍처/커리큘럼/용량+예산) 필요. AC3 조건부대로 **held-in 미상승→gap 재측정 불요** 정직 기록. 194→195(+1 smoke, 결정론 검증 포함, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7.
- **2026-06-23** — [`transfer-rigor`](_archive/2026-Q2/env-core/29-transfer-rigor/) (standard, **(B) widened-train 전이 신호 robust 재측정 — multi-run + 예산↑**): #27 양성 신호의 두 caveat(generalist-mediocrity·단일run) 메우기. `train_and_transfer_loo_multirun`+`--runs N`(per-fold run-간 **mean±std**) + `MultiRunFoldReport`. **사전약정 결정규칙**으로 사후 narrative 편향 차단(muster fold: 신호강화 held-in≥2.5∧gap≤+0.5 / 아티팩트 gap>+1.0 / 불확실 std≥gap). **실측**(50k·150k × 5 run, N16): (1) gaps가 #26 +2.56보다 **robust하게 훨씬 좁음**(유지). (2) 그러나 대체로 **held-in 하락**(widened 0.9~2.1≪#26 2.94); 예산 50k→150k는 held-in을 **약간만** 상승(muster 1.73→2.07, <2.5) → generalist-mediocrity **축소되나 제거 안 됨**(병목=정책/obs, compute 아님). (3) **#27 음수 muster gap은 robust 아님** — 5 run서 +0.22±0.45/+0.44±0.72, std>gap → 사전약정상 **불확실**(부호 불안정); #27 −0.25=run 노이즈. **단일-seed pilot의 "held-in 안 오름"도 노이즈→multi-run이 교정**(예산↑로 mean은 상승). (4) duel이 robust 최난 held-out(+1.15±0.11). **정직 verdict**: wider train이 gap 좁히나 *낮은 절대skill confound*+음수는 노이즈 → (B) 여전히 신호, 깨끗한 주장엔 **절대skill↑(정책/obs 개선, compute·seed 아님)** 필요. **pilot이 (a)예산↑로 held-in 끌어올리기 전제를 일부 falsify→AC7(ii)/R3 사전등록 분기로 정직 reframe**((a)=수정→발견). 193→194(+1 smoke, 회귀 0), core numpy-only, mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7.
- **2026-06-23** — [`genre-transfer-policy`](_archive/2026-Q2/env-core/28-genre-transfer-policy/) (standard, **(B) 전이하는 학습 정책 — widened-train LOO 전이 측정**): obs 조화(#27) 위에서 train 분포를 넓혀(**duel 포함**) 학습 정책의 unseen family 전이 gap을 측정 — #26 baseline(train{A,B}→muster, gap **+2.56**)과 **동일 metric**(전이 gap=held_in−held_out family, 리턴 단위). `scripts/genre_learned_transfer.py`(`[rl]`) `train_and_transfer_loo`+`--loo`: 4 family LOO(각 family held-out, 나머지 3 family 단일 PPO net 학습). **실측**(PPO 50k, N16/16, 단일run): unseen-muster gap이 #26 **+2.56(2-family) → −0.25(3-family, duel 포함)** 로 크게 좁아짐, 타 fold critter −0.92/forage −1.48/duel +1.08 — gap이 0 근처/음수로 붕괴 = **wider train이 unseen 전이를 돕는다는 양성 신호**. ⚠ **정직 caveat**: widened held-in 절대성능도 2.94→1.1~2.0 **하락**(1 net·동예산·3 family = generalist 평범화) → 좁아진/음수 gap은 *families 전반 균일 평범*도 반영(강한 전이의 증명 아님); 음수 gap(held-out>held-in)은 *낮은 절대skill + held-out이 더 쉬움*이 유력. (B)가 "측정상 미해결(#26)" → "wider train이 gap 좁힘(이 task)"으로 이동 = **신호지 증명 아님**(절대skill↑·multi-run 필요). 마일스톤 M5/층2(M3 공개보다 먼저). 192→193(+1 smoke importorskip, 회귀 0), core CI numpy-only 유지, mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7.
- **2026-06-23** — [`obs-harmonization`](_archive/2026-Q2/env-core/27-obs-harmonization/) (standard, **(B) 전이 정책 이니셔티브 선행 — 4-family obs 조화**): "전이하는 학습 정책"(competitive-analysis 갭 register 1순위, moat 층2)의 **하드 블로커 enabler**. #26이 obs 불일치로 **duel(13키)을 제외**(나머지 11키)하던 것을 해소 — 4 family(critter/forage/duel/muster)가 **단일 공유 obs 공간**을 노출하게 조화. `env_family.HARMONIZED_OBS_KEYS`(=`REQUIRED_OBS_KEYS` ∪ {`player_charge`,`enemy_charge`}) SSOT + `MAX_CHARGE_OBS`; base `CritterEnv`가 charge 키를 **0-마스킹**으로 노출(비-duel), `DuelEnv`는 실제값 override(중복 space 확장 제거). **A안(코어 최소 침습+superset/마스킹) freeze 전 pilot로 회귀 표면 실측**(base에 charge 추가→전체 테스트: 회귀 단 1건=AC5가 의도적으로 뒤집는 "duel 거부" 테스트)→A안 확정(B안 reframe 불필요). 패딩은 **행동 불변**(scripted 정책은 이미 `.get(charge,_ZERO)`): 신규 테스트로 *수치 증명*(charge 키 제거 시 액션 동일). `assert_obs_compatible`가 duel 포함 4 family 통과 + `_MultiFamilyEnv` duel 포함 구성 가능(smoke; **4-family 전이 *실험*은 다음 task**, gap 미축소). 마일스톤 override: M5/moat 층2 enabler를 M3 공개 EC보다 먼저(기능 준비 우선 방침). 185→192(+7, 회귀 0), mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC7.
- **2026-06-23** — [`genre-learned-transfer`](_archive/2026-Q2/env-core/26-genre-learned-transfer/) (standard, **(B) 학습-정책 전이 첫 측정**): (B) 장르 일반화의 핵심 미싱피스(competitive-analysis 갭 register 1순위) — 지금까지 scripted 대조였던 (B)를 **학습 정책의 unseen family 전이**로 첫 측정. `scripts/genre_learned_transfer.py`([rl]): train families {critter,forage}에 PPO 학습(reset마다 family 택1, 공유 obs) → **held-out family {muster}** 전이. obs 제약상 동일 11키 family만(**duel 13키 제외**=obs 조화 future, `assert_obs_compatible` 가드). held-in eval seed는 `split_train_pool`로 학습시드와 disjoint, held-out family는 학습 미노출(family-level split). **실측**(PPO 50k, N16/16): held-in **2.94 ±2.02** vs held-out-family **0.38 ±0.70**, **전이 gap +2.56**(std 훨씬 넘음) — **학습 정책이 unseen family로 전이 안 됨**(muster의 수집-강화 메커닉 미학습) = **정직한 "(B) 학습 genre 전이는 어렵다/미해결" 신호**(실패도 증명도 아님). 한 train-set→한 held-out family=첫 측정이지 증명 아님; 닫는 것=M5/moat 층2. core CI numpy-only 유지(PPO `[rl]` 뒤), 183→185(+2 smoke importorskip, 회귀 0), mypy(22)/ruff/build clean. DESIGN §3.1.1 갱신. Acceptance AC1–AC6.
- **2026-06-23** — [`difficulty-generalization`](_archive/2026-Q2/env-core/25-difficulty-generalization/) (standard, **M3 신뢰성 + (A) hard-and-gap≈0**): DESIGN §3.1.1 로드맵("toy서 gap≈0은 능력 예측력 약함"). **pilot이 "깨끗한 단조 scripted 난이도 사다리"를 falsify**(난이도 다차원: num_types=추론난이도는 blind엔 *쉬움*/보스 stat=cliff/scripted oracle 천장 ~0.6) → **학습정책 gap 실험**으로 정직 reframe(typechart-depth식). `scripts/difficulty_generalization.py`(`[rl]`): 난이도 **점** 3종(calibrated 사다리 아님) × PPO held-in 학습 → held-in vs held-out gap(기존 `measure_generalization` 재사용, held-in eval은 `split_train_pool`로 학습시드와 disjoint=gap 부풀림 없음). **scripted gap≈0은 trivial(암기 불가)이라 학습정책이 옳은 측정**. **실측**(PPO 40k, N=16/16): d0 gap +0.375(±0.86), d1 +0.438(±0.43), d2_hard +0.062(±1.64) — **모든 점의 gap이 per-seed std 안**(가장 어려운 d2 포함), 학습정책에서도 gap≈0과 *consistent*. **정직 한계**(L3 honesty 반영): 큰 std·저예산서 "gap-within-std"는 *약한 증거*(작은 real gap을 zero와 구분 못 함, "입증" 아님)·단일run·N16 = **신호**. DESIGN §3.1.1 "Toward hard-and-gap≈0" 문단 추가. core CI numpy-only 유지(PPO `[rl]` 뒤), 181→183(+2 smoke importorskip, 회귀 0), mypy(22)/ruff/build clean. Acceptance AC1–AC6.
- **2026-06-23** — [`competitive-analysis`](_archive/2026-Q2/env-core/24-competitive-analysis/) (standard, docs-only, **공개 전 갭 탐지기**): OSS RL 벤치마크(Procgen/Craftax/XLand/NetHack) 대비 **정직한 비교 분석** `docs/explanation/competitive-analysis.md` — 사용자 방침(공개 맨 마지막, 기능 준비+분석 먼저) 지원. capability 매트릭스(9축×5열) + 트레이드오프 산문(**열위 먼저**: Craftax/XLand 속도, Procgen/NetHack 성숙·채택, NetHack/Crafter 난이도, XLand meta-RL 폭) + peer verify-list + **갭 register**(우리가 주장 못 하는 것 → 필요 기능 → 마일스톤). **정직성**: peer 사실은 학습지식 기반이라 전부 `[verify]` 라벨(공개 전 1차 출처 검증), 속도는 numpy CPU/core ≠ JAX GPU basis 명시(동일행 수치비교 금지), 우리 수치는 논문/DESIGN 일치(날조 0), DESIGN §9 "moat prospective" 자기평가 준수("전 축 우위" 0). **결론**: 공개 전 leverage 최대는 **난이도 스케일(toy→hard)** + **family 확장+학습정책 held-out(B 토대→주장)**, **JAX 포트**(채택 enabler). 제품 코드 무회귀(181 passed 불변), broken-link 0. L3 APPROVED(2/2). Acceptance AC1–AC6.
- **2026-06-23** — [`oss-release-prep`](_archive/2026-Q2/env-core/23-oss-release-prep/) (standard, **M3-EC5 전진(준비)**): 오픈소스 릴리스 **로컬 산출물** 준비. (1) MIT `LICENSE` 파일 신규(pyproject 이미 MIT 선언, 누락 파일 보강; copyright "CritterGym contributors" 교체 가능). (2) stale `README.md`("Phase 0 — Nothing built yet") → **실제 빌드 상태**로 재작성: install(`pip install -e .` + `[rl]`/`[viz]`/`[render]`) + quickstart(`register_envs`+`gymnasium.make` **env id 6종** 표, family A–D) + "What it measures"(A 측정/B 토대, 논문 링크) + 정직 positioning(**Pokémon=메타포 비경쟁**, Procgen/Craftax/XLand 대비) + 재현(seed split) + citation + Release status. (3) `CONTRIBUTING.md` 신규(dev 셋업·4 체크·정직성 규율·lifecycle). **수치 verbatim**(held-in 40%/held-out 45%, ~266k, gate ≥0.20/≥0.10, C +3.9 vs +0.2)는 논문/DESIGN과 일치(L3 코드·논문 대조 APPROVE, 날조 0). **정직 범위 경계**: 실제 외부 발행(Prime Intellect Hub 리스팅·repo public)은 **사용자 게이트**(M3-EC6 선례), README "Release status"에 미수행 명시 — EC5 자동충족 주장 0. 제품 코드·테스트 무변경(181 passed 불변), LICENSE↔pyproject 정합, broken-link 0. M3-EC5 **준비**(외부 발행은 사람 결재 후 충족). Acceptance AC1–AC6.
- **2026-06-23** — [`arxiv-writeup`](_archive/2026-Q2/env-core/22-arxiv-writeup/) (standard, docs-only, **M3-EC4 전진**): 측정 인프라+결과를 **arXiv 논문 초안**으로 패키징 — `docs/paper/critter-gym.md`(abstract/intro+positioning/env design/측정 결과 A·load-bearing·learnability·B/related work/honest limitations/conclusion 8섹션) + `docs/paper/README.md`(수치↔모듈/테스트 재현 맵). **정직성 = 자산**: 모든 정량 주장이 코드/측정 근거(날조 0), **CI-reproducible(test-frozen gate)** vs **run-derived(run 평균)** 구분 라벨. Pokémon=plain-language 메타포(비경쟁), Procgen/Craftax/XLand 대비 정직 벤치. (A) 인스턴스 일반화(held-out 45% vs held-in 40%, gap≈0) 측정 / (B) 장르 4-family 토대(C gap +3.9 vs +0.2, D muster 1.42≫rush 0.00, B forgiving — within-family 대조=skill-structural)는 **토대지 증명 아님** 명시. honest limitations 전 caveat(B=토대/D confound/단일run·N/learnability 천장·oracle==infer/C win 예측가능성/재현 tier). L3 reviewer 코드 대조로 수치 정확성 검증, honesty SUGGEST(duel 예측가능성 caveat) 반영. docs-only 무회귀(181 passed 불변), broken-link 0. M3-EC4 **초안 산출**(LaTeX·제출은 후속). Acceptance AC1–AC6.
- **2026-06-23** — [`family-d-muster`](_archive/2026-Q2/env-core/21-family-d-muster/) (standard, **M3 신뢰성 + (B) 토대 4-family 확장**): (B) 장르 일반화 토대를 **네 번째 family**로 확장 — 세 번째 구조 축(**진행 의존**). family D `MusterEnv`(`CritterGym-muster-v0`)는 **수집이 배틀력 좌우**(CATCH 성공 시 파티 공격력 +12) + 강한 보스(hp300/def24) → 보스를 이기려면 **먼저 muster**(수집)해야 함 = 수집·배틀 의존 결합. `CritterEnv` 서브클래스로 `_step_overworld`만 override(family A/B/C 무변경). `genre_generalization`에 obs-only 레퍼런스 `rush_policy`(직행·수집안함)/`muster_policy`(수집-우선) 추가, LOO 4-family 자동 확장. **측정**(within-family 대조, gym-clears): family D muster **1.42 ≫ rush 0.00**(스킬 load-bearing), family A muster 1.08 ≤ rush 1.58(스킬 **무용**, catch 버프 없어 수집은 step 낭비) → gap이 **skill-structural**(family C와 평행한 정책-특정 대조; D는 muster로 winnable이라 "그냥 어려움" 아님). **정직성**: family D는 강한 보스(다른 calibration=family 정체성)라 **raw cross-family LOO 평균 gap은 난이도 confound** → 헤드라인은 within-family 대조(같은 config, 정책만 변주)로 통제, DESIGN §3.1.1 명시. 4 family도 **토대지 장르 일반화 증명 아님**(많은 family 필요). family A/B/C 무회귀(174→181, 신규 7), check_env **6종**, mypy(22)/ruff/build clean, honesty 가드 pass. Acceptance AC1–AC6.
- **2026-06-23** — [`learnability-precision`](_archive/2026-Q2/env-core/20-learnability-precision/) (standard, **M3 신뢰성 + (A) learnability 정밀화**): #17의 정직한 caveat 1번(episode return = 격파+진화 **합산** → learned가 oracle을 "넘는" 착시) 해소. **gym-clear-only 메트릭** 도입 — `EpisodeOutcome`(return/gyms_cleared/evolutions)으로 보스 격파와 진화 stream 분리, `run_episode`가 outcome 반환, `LearnabilityReport`에 `*_gyms`(gym-clear-only means) combined와 **병기**, `measure_learnability` 양 메트릭. **측정**(held-out, num_gyms=8): gym-clear-only oracle/infer **4.19** ≫ type_blind 1.81 > probe 1.06 — 진화 인플레(~1.44) 제거 후 **load-bearing 순서 유지**. `scripts/learnability.py`에 gym-clear 중심 보고 + `--runs N`(다중 PPO seed 평균, 단일run caveat 완화). **정직성**(L1 measurement reviewer 2-SUGGEST 반영): gym-clear-only는 (i) **num_gyms 천장**으로 강한 arm 간 gap 압축, (ii) **oracle==infer 구분불가**(이 config는 한 번 보면 추론 자명 → 추론이 *suffices*지 이 메트릭만으로 load-bearing 증명 아님, 증명은 scripted gate 몫), (iii) 단일config·N modest·다중run [rl]비CI — 모두 DESIGN §3.1.1 명시. 공개 API 무회귀(`measure_learnability` 시그니처+combined·`arm_mean` 보존), PPO smoke pass. 171→174(신규 3, 회귀 0), mypy(21)/ruff/build clean, honesty 가드 pass. Acceptance AC1–AC6.
- **2026-06-23** — [`battle-system-family`](_archive/2026-Q2/env-core/19-battle-system-family/) (standard, **M3 신뢰성 + (B) 장르 일반화 토대 강화**): DESIGN §3.1.1 (B)의 정직한 약점(family B가 thin=수집 1축, A-튜닝 정책이 gap≈0 전이=관대한 축)을 메우는 **세 번째 env family**. family C `DuelEnv`(`CritterGym-duel-v0`)는 **배틀 시스템 자체를 교체** = family B가 결여한 *더 강한 구조 축*: **타입-무관 stamina/commit RPS 결투**(ATTACK>CHARGE>GUARD>ATTACK, charge가 공격 배율; 타입표·무브선택·스위칭 없음 → family A의 load-bearing 스킬인 타입추론이 무용). `CritterEnv` 서브클래스로 배틀 경로만 override(family A/B 무변경), duel charge를 **extra obs 키**로 노출(`conforms` ⊇ REQUIRED → 계약 무위반, 특권접근 없이 공정 플레이). `genre_generalization` **다-family leave-one-out** 측정 + obs-only 레퍼런스 정책(`type_attacker`/`duel_aware`) 추가(2-family API 무회귀). **측정**(LOO {critter,forage,duel}, held-out N=12): held-out=duel에서 **A-튜닝 0.583/gap +3.917 ≫ C-적합 4.333/gap +0.167** = gap이 **skill-structural**(틀린 스킬, C는 winnable)이지 난이도 confound 아님 — **family B(gap≈0)가 못 만든 정책-특정 신호**(L1 measurement reviewer 요구한 anti-confound 대조). **정직성**(헤드라인 과대 금지): 3 family도 토대 강화지 **장르 일반화 증명 아님**(많은 구조-상이 family 필요); gap=signal; 캐비엣(결정론 보스+charge-in-obs readability, N=12 단일run) DESIGN·report 명시(L3 SUGGEST 반영). freeze 전 pilot로 5조건(conforms/구조적상이/winnable/A-비전이/skill-structural 대조) 검증 후 진입. family A/B 무회귀(160→171, 신규 11)+check_env **5종**, mypy(21)/ruff/build clean, honesty 가드 pass. Acceptance AC1–AC6.
- **2026-06-22** — [`genre-generalization-foundation`](_archive/2026-Q2/env-core/18-genre-generalization-foundation/) (heavy, **M3 신뢰성 + (B) 장르 일반화 토대**): DESIGN §3.1.1 (B) environment-level(구조-상이 env 패밀리 간) 일반화의 **측정 머신을 end-to-end로 세움**(이니셔티브급의 토대 첫 슬라이스). (1) **env-family 추상화** `critter_gym.env_family`(공유 obs/action 계약 `conforms` + family registry + `trajectory_signature`); CritterEnv(family A) **무변경** 만족. (2) **family B** `ForageEnv`(`CritterGym-forage-v0`) = 구조-상이 **contact-collect**(creature 타일 진입 시 자동 수집, CATCH inert) vs family A action-collect; **same-seed·same-actions→A≠B trajectory** 테스트로 시드변형 환원불가 입증. (3) **env-level 측정** `critter_gym.genre_generalization`(numpy-only, `generalization.evaluate` 재사용): train-family→unseen-family 갭. **측정**: random gap −0.35, greedy(A-튜닝) gap +0.00(A-정책이 B로 전이 — 머신 작동 입증). **정직성**(헤드라인 과대 금지): 2 패밀리=토대지 장르 주장 아님 / gap=신호(threshold 아님) / family B 최소성(수집 1축, gap≈0은 축이 관대해서) — 신뢰할 장르 주장은 다수 구조-상이 패밀리 후속. family A 무회귀(151→160, 신규 9만) + check_env **4종**(fixed/procgen/commit/forage). Acceptance AC1–AC6, mypy(20)/ruff/build clean.
- **2026-06-22** — [`learnability-measurement`](_archive/2026-Q2/env-core/17-learnability-measurement/) (standard, **M3 신뢰성 + (A) learnability 완성**): `reasoning-load-bearing`의 정직한 follow-up — **학습 정책(PPO)이 commit-v0에서 추론을 *학습*하는가** 측정. (1) commit 모드 **챔피언-선택 액션 UX**(`critter_env._commit_window`: 보스전 turn-0 commit window — action 4 cycle=무턴/무피해, 첫 move lock; "유리하면 attack 아니면 switch" 전략이 동역학에 암묵 내장돼 **M1 obs 무변경**). (2) `critter_gym.learnability`(numpy-only): env-level reference arm 4종(oracle/infer/type_blind/probe) + `measure_learnability`(held-in/out split 가드) — 액션 UX 경유로도 `oracle≥infer>probe` 재현. (3) `scripts/learnability.py`(`[rl]`): PPO commit-v0 학습→reference 대조. **PPO 100k 측정: learned held-out 4.00 ≫ type_blind 1.88·probe 1.56, infer-ref 3.25 수준 이상 = 양성 learnability 신호** — (A) 차별점이 구조뿐 아니라 *학습 에이전트 수준*에서도 작동. **정직 caveat**(헤드라인 과대 금지): return=격파+진화 합산(learned>oracle는 진화 착시)·N16·단일run → 정밀 벤치 아닌 *신호*로 보고. acceptance=*측정 산출+정직 보고*(성능 freeze 아님, §4 교훈). DESIGN §3.1.1 follow-up 갱신. M1·procgen·commit-v0 무회귀(48 passed+check_env ×3). Acceptance AC1–AC6, 151 passed/2 skipped(140→151), mypy(17)/ruff/build clean.
- **2026-06-22** — [`reasoning-load-bearing`](_archive/2026-Q2/env-core/16-reasoning-load-bearing/) (standard, **M3 신뢰성 + DESIGN §3.1.1 해소**): `typechart-depth` 가 불가 입증→descope 했던 **"infer-the-meta provably load-bearing"** 을 **team-commit 보스 경제**로 *scripted-arm 실증*. freeze 전 achievability pilot 통과 후 진입(§4 함정 회피). `Battle(commit_mode=True)`: 보스전 **크리처 1마리 commit** — switch no-op + faint 시 force-switch 순회 차단 + active-faint=즉시패배 → (a) 다중파티 brute-force 제거 + (b) 배틀내 probing 구조적 불가. 난이도 knob `TypeChart.super_mult`·`gym_boss(hp/atk/df)` env/registration config 노출(상수 X), `CritterGym-commit-v0` 등록(num_types12/super_mult3/boss140·18/commit). **게이트(`test_reasoning_gate.py`, 42 고정 held-out 시드, product API): Gate0 oracle 1.000≫type_blind 0.521=0.479≥0.2, Gate1 infer 0.836>probe 0.473=0.363≥0.1** — 추론이 load-bearing. **정직성**: scripted-arm 으로 *task 구조* 가 추론을 강제함을 입증(M1 경제가 결여했던 선결조건); *학습* 정책이 추론을 실제 *학습* 하는지는 미측정 **follow-up**(DESIGN §3.1.1 + honesty 가드 재정의로 learnability 과대표현 차단). **M1 완전 무회귀**(commit_battles/super_mult 기본 off; 42 passed + check_env ×3). Acceptance AC1–AC6, 140 passed/2 skipped(128→140, 회귀 0), mypy/ruff/build canonical clean. `typechart-depth` 가 불가 입증→descope 했던 **"infer-the-meta provably load-bearing"** 을 **team-commit 보스 경제**로 *scripted-arm 실증*. freeze 전 achievability pilot 통과 후 진입(§4 함정 회피). `Battle(commit_mode=True)`: 보스전 **크리처 1마리 commit** — switch no-op + faint 시 force-switch 순회 차단 + active-faint=즉시패배 → (a) 다중파티 brute-force 제거 + (b) 배틀내 probing 구조적 불가. 난이도 knob `TypeChart.super_mult`·`gym_boss(hp/atk/df)` env/registration config 노출(상수 X), `CritterGym-commit-v0` 등록(num_types12/super_mult3/boss140·18/commit). **게이트(`test_reasoning_gate.py`, 42 고정 held-out 시드, product API): Gate0 oracle 1.000≫type_blind 0.521=0.479≥0.2, Gate1 infer 0.836>probe 0.473=0.363≥0.1** — 추론이 load-bearing. **정직성**: scripted-arm 으로 *task 구조* 가 추론을 강제함을 입증(M1 경제가 결여했던 선결조건); *학습* 정책이 추론을 실제 *학습* 하는지는 미측정 **follow-up**(DESIGN §3.1.1 + honesty 가드 재정의로 learnability 과대표현 차단). **M1 완전 무회귀**(commit_battles/super_mult 기본 off; 42 passed + check_env ×3). Acceptance AC1–AC6, 140 passed/2 skipped(128→140, 회귀 0), mypy/ruff/build canonical clean.
- **2026-06-22** — [`typechart-depth`](_archive/2026-Q2/env-core/15-typechart-depth/) (standard, **DESCOPED**, M3 신뢰성): procgen 타입 시스템 심화 — `ElementType` 3→15(F/W/G=인덱스 0/1/2 유지), procgen-v0 `num_types=12`(시드별 숨은 차트 66 정향관계, 3-cycle보다 외우기 훨씬 어려움) + 보스 타입을 시드별 소수 풀서 **반복 추출**(에피소드 내 재출현, was 0/40→34/40) + winnability 구조 보장. **M1 고정월드 완전 무회귀**(num_types=3 기본, 풀리힐 유지). **정직성 디스코프**: 원래 목표("infer-the-meta provably load-bearing")는 G1 후 pilot이 *달성 불가* 입증(no-heal에서 switch 비용이 매치업 이득 압도 → type-blind 1위) → 사용자 승인 (가)로 no-heal keystone·infer>probe 게이트 드롭, "추론 load-bearing=future work"를 DESIGN §3.1.1에 명시(코드 honesty 가드 테스트 포함). 포기 아닌 falsification(가설을 G2 전에 반증, transparent 축소). Acceptance D1-D10, 128 passed/2 skipped, mypy/ruff/build canonical clean.
- **2026-06-22** — **M3-EC6 충족 (결재)**: `scripts/killer_demo.py` 실제 실행(PPO 100k 학습) → held-out seed 1000000(새 맵+새 타입표) **보스격파 GIF** `docs/assets/killer_demo.gif`. 단일 일화 아님 — **held-in 8/20(40%) vs held-out 9/20(45%) 보스격파율, 일반화 갭 ≈ 0**(암기 아닌 진짜 일반화 = 우리 moat 실증). 절대성능 45%는 향상 여지(데모 주장=일반화는 입증). milestones M3-EC6 `[ ]`→`[x]`.
- **2026-06-22** — [`killer-demo`](_archive/2026-Q2/env-core/14-killer-demo/) (standard, **M3-EC6 수단**): 킬러데모의 녹화 파이프라인 — `critter_gym.demo.record_episode`(정책 rollout→`env.render()` 프레임 수집 + 보스격파 감지 `remaining_gyms==0`, **numpy-only**, render_mode 가드) + `EpisodeRecording` + `save_demo`(render.save_gif 위임). `scripts/killer_demo.py`(`[rl]`+`[render]`): train 64시드 학습 → **held-out 시드** 녹화(deterministic=True)→GIF + 격파 리포트. **정직성**: CI 가 검증한 건 파이프라인·감지(type-aware scripted 가 seed=3 **train 영역**에서 gym 격파 녹화)이지 *일반화 아님* — "학습 에이전트가 held-out 보스격파"는 학습품질 의존 비CI. **milestones M3-EC6 `[ ]` 유지**(world-render 선례; `[x]`=실제 held-out GIF 확인+결재). 측정 eval rollout 과 직교(프레임 vs 점수). Acceptance 8/8, 118 passed/3 skipped(113→118, 회귀 0).
- **2026-06-22** — [`world-render`](_archive/2026-Q2/env-core/13-world-render/) (standard, **M3-EC6 토대**): 킬러데모의 render 레이어 — 월드 상태→픽셀 프레임 `critter_gym.render.draw_frame`(**numpy-only**, 색칠 셀: agent·creature·active/defeated gym·배틀 틴트; 배경→creature→gym→agent 그리기 순서로 set-순서 무관 byte-identical 결정론). `CritterEnv` Gymnasium 표준 `render_mode="rgb_array"` 통합(read-only `render()`, obs/step/reset 무변; metadata render_modes/render_fps). GIF 인코딩(`save_gif`)만 `[render]` extra(imageio) 지연 import 격리. **rgb_array=새 의존성 0**. `test_compliance` 가 `skip_render_check` 제거 → check_env 가 rgb_array render 실제 검증. **EC6 를 전진(토대)만, 충족은 후속 `killer-demo`**(보스격파 에이전트+GIF, CI비검증). render 도메인 첫 vertical. Acceptance 7/7, 113 passed/2 skipped(102→113, 회귀 0).
- **2026-06-22** — [`metrics-viz`](_archive/2026-Q2/env-core/12-metrics-viz/) (standard, **M3-EC3**): 측정 viz 4종 차트(학습곡선·일반화 갭·baseline spread·시드 분포) — **연구자용 메트릭 플롯**(게임 렌더링 아님). numpy-only `critter_gym.viz`: plot-ready 데이터 정형(`spread_data`/`gap_data`/`seed_distribution_data`/`LearningCurve`)은 core CI 검증, matplotlib 드로잉은 **함수 내부 지연 import + `[viz]` optional extra** 격리(측정 모듈은 viz/matplotlib 미import → 단방향 의존, import 순수성 테스트). `Agg` headless. `Leaderboard.from_score_table` 분리로 `benchmark.py --plot` 가 ScoreTable 1회로 리더보드+4차트(per-seed 분포) 생성, `train_ppo.py` 가 실제 학습 루프에서 `LearningCurve` 누적+저장. 측정 인프라(EC1·EC2·EC3) 완성 — 남은 M3 는 데모·공개. Acceptance 8/8, 102 passed/1 skipped(92→102, 회귀 0).
- **2026-06-21** — [`leaderboard`](_archive/2026-Q2/env-core/11-leaderboard/) (standard, **M3-EC2**): 리더보드 포맷 + 재현 configs — numpy-only `critter_gym.leaderboard`(`BenchmarkSpec` 핀 고정 eval 프로토콜 + `Leaderboard.to_json`(sort_keys, spec embed)/`to_markdown` + `run_benchmark`). held-out 평균 내림차순 랭크(결정적 tie-break gap/name) = unseen 일반화 철학. **공개 점수 스키마 개명** 완료(generalization-harness L3 forward-note 이행): `train_mean`/`test_mean`→`heldin_mean`/`heldout_mean`(+`n_heldin`/`n_heldout`) — held-in/held-out *eval* 평균임을 정직히 표현, 단일 위임(GapReport.to_dict)이라 부채 0, 리터럴 grep=0. `benchmark.py` 리더보드 소비자화(재현 spec 헤더). viz·OSS·킬러데모 공개 포맷 토대. Acceptance 8/8, 92 passed/1 skipped(86→92, 회귀 0).
- **2026-06-21** — [`baseline-suite`](_archive/2026-Q2/env-core/10-baseline-suite/) (standard, **M3-EC1**): 베이스라인 4종(random/scripted/PPO/recurrent) train+test 점수표 — M2 의 갭 하네스를 N-베이스라인으로 확장하는 **numpy-only** `critter_gym.scoreboard`(`score_baselines`→`ScoreTable.to_markdown`/`to_dict`). 베이스라인마다 `measure_generalization` 1회, seeds tuple materialize 로 동일 split 공정 비교, 누수 가드는 위임 상속. 빌더는 torch/sb3 무의존(core CI 가 random+scripted 로 spread 가드 보존), PPO/recurrent(`sb3_contrib.RecurrentPPO`)는 `[rl]` extra 뒤 `benchmark.py` 소비자 + `importorskip` smoke 로 격리. `benchmark.py` dead-bug 3종 정정(`target_catches` AttributeError·`range(50_000)` train영역 오칭·`vary=False`) + sb3 미설치 graceful. 리더보드·viz·킬러데모 데이터 토대. Acceptance 9/9, 86 passed/1 skipped(81→86, 회귀 0).
- **2026-06-21** — [`generalization-harness`](_archive/2026-Q2/env-core/09-generalization-harness/) (standard, **M2-EC4 → M2 완성**): train-vs-test 일반화 갭 측정 하네스 — 정책-비의존 **numpy-only** `critter_gym.generalization`(`rollout`/`evaluate`/`split_train_pool`/`measure_generalization`/`GapReport`/`format_report`). `gap = train_mean − test_mean`(Procgen 관례), held-in 은 `split_train_pool` 로 학습 시드와 **구조적 disjoint**(낙관편향 방지), 양방향 split 누수 가드(ValueError). 측정 로직은 torch/sb3 무의존 → core CI 상시 검증, PPO 학습은 `[rl]` extra 뒤 `scripts/train_ppo.py` 소비자로 격리(+`vary=True` procgen, 기존 `target_catches` dead-kwarg TypeError·하드코딩 50_000 split 버그 정정). **마일스톤 M2(procgen + train/test moat) 완성** — 4 EC 충족, 킬러 데모·리더보드 토대. Acceptance 9/9, 81 tests green(71→81, 회귀 0), check_env(fixed+procgen).
- **2026-06-21** — `procgen-typechart` (standard, **M2-EC2/EC3**): moat 핵심 novelty — 시드별 내부정합 타입 상성표 `generate_typechart`(antisymmetric per-pair, 결정론) + `TypeChart` 데이터주도화(기본=FIXED, M1 무회귀). Region 이 시드별 차트 보관, env 가 Battle+scripted_opponent 에 전달(scripted 차트 누락 버그 수정). 차트는 obs 미노출 → **infer-the-meta**(고정표 암기 불가). held-out 시드가 새 맵+새 타입표 생성(EC3). Acceptance 8/8, 71 tests green, check_env(fixed+procgen).
- **2026-06-21** — `procgen-region` (standard, **M2-EC1**): 우리 moat 의 첫 벽돌 — 시드→절차 생성 region(creature/gym 수·위치·보스 타입) 순수 생성기 `generate_region` + train/held-out 시드 구조적 분리(`TEST_SEED_OFFSET` offset + overrun ValueError 가드 + split-safe 무시드 reset). `CritterGym-procgen-v0`(vary=True) 등록. obs 형태 불변(차원 고정·내용만 변주·경계=max). vary=False 기본으로 M1 무회귀. Acceptance 8/8, 64 tests green, check_env(fixed+procgen) 통과. 일반화 측정 토대 마련.
- **2026-06-21** — `creature-evolution` (standard, **M1-EC2 → M1 완성**): 진화 = long-horizon 투자 — 배틀 승리 시 active creature 레벨업, 임계(evolve_level=2)서 자동 진화(강화 stats), 진화=boolean subgoal 리워드(+1, RLVR). obs `evolved`/`player_level` 추가, num_gyms 기본 3 + 보스 [GRASS,GRASS,WATER]로 진화 payoff non-vestigial. **마일스톤 M1(고정월드 full subgoal chain) 완성** — 탐험→잡기→gym 배틀→보스 격파→진화가 한 에피소드에서 verifiable subgoal로 동작. Acceptance 9/9, 54 tests green, check_env 통과, 회귀 0.
- **2026-06-21** — `gym-boss-progression` (standard, **M1-EC3/EC4**): 배틀 엔진을 `CritterEnv.step`에 연결 — 월드 gym=gated checkpoint, OVERWORLD↔BATTLE 모드 머신, 보스 격파=boolean subgoal 리워드(+1, RLVR). obs 확장(in_battle+양측 active hp·type+gyms_defeated), action_space Discrete(6) 모드별 재해석. 종료 진화(catch 비종료, 전 gym 격파→terminated). 처음으로 "탐험→배틀→보스 격파"가 한 에피소드에서 동작(scripted seed3 2 gym 격파 14스텝). Acceptance 9/9, 45 tests green, check_env 통과, 회귀 0.
- **2026-06-21** — `battle-system` (standard, **M1-EC1**): 턴제 배틀 sub-MDP 엔진 — 고정 타입표(rock-paper-scissors) + 타입 상성 데미지 + 스위치 + 아이템(potion) + scripted 상대. 완전 결정론(랜덤 없음, RLVR/재현성). `types.py`/`creatures.py`/`battle.py` 독립 모듈(env 통합은 M1-EC3). Acceptance 9/9, 35 tests green. 활성 마일스톤 M1 첫 EC 충족.
- **2026-06-21** — `product-roadmap` (standard, docs-only): 제품 마일스톤 SSOT 신설 — `docs/reference/milestones.md`(M0–M5 goal·exit criteria·구성 task·DESIGN 매핑) + `docs/explanation/roadmap.md`(순서 근거·킬러 데모·master-plan↔roadmap 구분). "매 task 는 활성 마일스톤의 미충족 EC 에서 내려온다" 규율을 CLAUDE.md(auto-load)에 hook-up. 즉흥적 task 생성 차단. Acceptance 7/7, broken-link 0.
- **2026-06-21** — `env-validation` (standard): 벤치마크 성립성 검증 레이어 — Gymnasium `check_env` 통과 + 베이스라인 spread(random 0.38 ≪ greedy 2.44) + train/test held-out 일반화 + throughput 회귀 가드(실측 ~266k steps/s/core, 목표 50k의 5배). `random_policy`/`greedy_policy` 패키지 동봉, PPO 학습 데모(`[rl]` extra, trained 2.90 vs random 1.49). 무거운 deps는 core에서 분리. Acceptance 7/7, 18 tests green.
- **2026-06-21** — `scaffolding` (standard): CritterGym 제품 코드 첫 줄 — `src/critter_gym/` src-layout 패키지 + hatchling 빌드 + ruff/mypy/pytest 툴체인 + 10×10 catch-only `CritterEnv` (Gymnasium API, seeded 결정론, RLVR boolean subgoal 리워드, `gymnasium.make("CritterGym-v0")` 등록). Acceptance 8/8, 12 tests green, HARNESS-PORT-MANIFEST §(c) 커플링 #1–#4 정합 확정.

